<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <RootNamespace>BattleSystemECS</RootNamespace>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <AssemblyName>BattleSystemECS</AssemblyName>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

  <ItemGroup>
    <!-- Config files -->
    <Compile Include="Configs\*.cs" />

    <!-- Core files -->
    <Compile Include="Core\*.cs" />
    <Compile Include="Core\GAS\*.cs" />

    <!-- Components -->
    <Compile Include="Components\*.cs" />

    <!-- Systems -->
    <Compile Include="Systems\*.cs" />

    <!-- Specific System files needed (GoldSystem) -->
    <Compile Include="System\GoldSystem.cs" />

    <!-- Main program -->
    <Compile Include="Program.cs" />
  </ItemGroup>

  <ItemGroup>
    <!-- Copy JSON config to output directory -->
    <None Update="game_config.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
