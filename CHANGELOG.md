# 更新记录 (Changelog)

### 2026-09-05（门禁复核：H1/H2/H3 + M1–M6）
- **H1 排期本热路径**：`effect.tick` 不再对每个未过期 effect 每帧 `SyncSchedule` → `ClearEffect`（带闭包线性删 5 个 clock 列表）+ `Upsert`。E=10K 时那是 O(E²)+每帧分配。Tick 恢复 float `RemainingTime -= dt` 与 `TickAccumulator` Periodic 到期；`GameplayScheduleBook` 只给 Timed Ability 虚拟时钟、Apply/Remove/`RebuildSchedule`。`CollectDue` 仍无生产调用者。
- **H2 生产播种**：`GameManager.Initialize` 开局 `Determinism.Reset` 一次并打 `[BOOTSTRAP] match seed=`（构造器 `matchSeed` / 环境变量 `BATTLE_MATCH_SEED` / 否则 `Environment.TickCount`）。Crafting / Reforge / TowerModifier 构造器删除 `seed` 改种参数；测试直接 `store.Determinism.Reset`。
- **H3 Activate scratch**：`CostKeyScratch` / `CostAmountScratch` / `RuntimeTargetScratch` 搬进每 store 一份的 `AbilityCommitReservation`，去掉进程级 static。
- **M1 soak**：`ProductionGraphFixedSeedSoak` 用 `ProductionSystemInstaller` 完整图跑 200 帧两遍比 `StateDigest` + `GameplayEventSequenceDigest`。原 `FixedSeedFissionSoak` 保留。
- **M2 虚拟时间边界**：Periodic/时长到期不再用 `NextTickAtVirtual <= (double)now`（dt=0.1f、period=0.3f 会从第 3 帧推到第 4 帧）。CHANGELOG 单列：与 P6 短暂引入的 double 虚拟比较相比，边界帧回到 float 累加。SkipMissed 死分支已删。
- **M3 RestackLedger**：已有等长 modifier handle 时 `RefreshModifier` 原地改幅度（CaptureOnApply 也更新 Captured）；先改 aggregator 成功再写 `StackCount`。NaN capture 不再覆盖旧捕获。
- **M4**：P1+P2 仍在历史 commit `190873d`，无法独立回滚；规划已改「逐条 Release」措辞。
- **M5**：复查失败仍不 `CommitPlan`。复查通过后的残余半状态（多目标 Plan 中途 -1、Plan 后 Spend 失败）写入终态与本条，未改支付顺序。
- **M6**：`TimeRewindSnapshot` 去掉对玩家 HP/Mana/Shield 数组的手工回滚；失败只 `return -1`，写入仍走 Resolver。
- **L1**：`ResolveAdoptModifierCapture` 对 Multiply 走与 adapter 相同的 `m−1`。**L2**：`Release` 夹负计数时累加 `ReleaseUnderflowCount`。**L4**：删除 `PreFightBuffSystem.SystemRandomSeed`。**L5**：检查清单 EXE 构建写 `dotnet build BattleSystemECS.csproj`。
- **L3** 未改代码：敌方玩家伤害 `Commit` 返回 -1 仍会以 `QueueOverflow` 发 Cancelled（原因不准确）；与 M5 残余同类。
- mode 2/4/5 与 Unity 保持 DEFERRED/UNAVAILABLE。本轮补跑 **mode 4**（`dotnet run --project BattleSystemECS.csproj -- 4`）：10K 敌 × 500 帧生产 FrameGraph，Debug **155 FPS**（6.47 ms/frame），Release **233 FPS**（4.30 ms/frame）。不写入 PASS manifest。

### 2026-09-05（Lumio P6：时长 Ability 态 / 排期本 / 墓碑 / 摘除式抑制 / 表现原因）
- **时长 Ability 态**：`AbilityPhase` 为 None / Executing / Completed / Cancelled / Expired，只给 `AbilityDurationKind.Timed`。瞬发 `Activate` / 生产 `ActivateCore` 相位保持 None。不引入 RolledBack，不套八态机。现有内容无 GAS channel 技能；测试用最小时长定义走完四态。
- **排期本**：`GameplayScheduleBook` 按每个 `clockId` 的虚拟时间取件，可从 ActiveEffect 全量重建。挂在现有 `GameplayEffectRuntime.Tick`（effect.tick），未改 FrameGraph / 根哈希。子弹时间下 Enemy clock 与 Combat clock 到期解耦。
- **墓碑查询**：`QueryEntityTombstone` 区分 NeverExisted / Dead / Alive / PendingDeath。实体 ID 仍回收 + generation。过期 generation 的伤害命令继续丢弃并累加 `StaleHandleRejectedCount`。
- **摘除式抑制**：`TryInhibit` / `TryUninhibit` 摘除或重建 modifier 与 granted tag 贡献，效果槽仍在；`Inhibited` 是槽位 bool，不是新状态机枚举。
- **表现原因**：`GameplayEvent.Cause`（Mutation / Initial / Replay）加在比较键之后；`GameplayEventOrdering` 不读 Cause；sequence digest 只累计 Mutation。
- 半实体泄漏未核实、未开卡。KeepPerLayer 未做。mode 2/4/5 与 Unity 保持 DEFERRED/UNAVAILABLE。

### 2026-09-05（Lumio P5：同帧顺序 A4 + 随机领号 A3，⚠️ 可见随机变化）
- **同帧顺序**：保持 AI 组 `RemoveDispellableEffects` 为批外 remove-first；`effect.commit` 批内堆叠 → 溢出 → Replace 捕获 → 时长/周期 → 过期/Remove 垫后。禁止「施加又移除 = 抵消」，`EffectApplied` 与 `EffectRemoved` 都进 digest。未引入 `RemoveRequests` 尾节点，未改 FrameGraph 根哈希。生产仍只有这一处显式 Remove。
- **随机领号**：新增 Frame 注入的 `DeterminismContext`，生产 Tick 仅 CommitSerial 等价语义取数。下列模拟路径不再用 `Rng.Shared` / 无种子 `new Random()`：EchoClone / EnemyTeleport / PointDefense / Skill 冰冻 / Upgrade / Weather / Fission / Clone / AutoSkill / Pickup / Affix / RandomEvent / WaveSpawning（含路径偏移相位）/ Crafting / PreFightBuff / Reforge / TowerModifier。Portal 未使用的 `Rng.Shared` 字段删除。ShopReroll 保留固定种子私有流（BuildPhase，不进 digest）。Benchmark `new Random(42)` 不迁。
- **可见随机变化（单列）**：天气切换与强度、分裂/克隆落点、波次出生 X、词缀分配、掉落与随机事件、回声分身、传送落点、点防拦截、技能冰冻判定、升级随机 buff、自动技能洗牌、合成（seed=0）、路径偏移相位，以及出战 buff / 重铸 / 塔修饰现在走同一场比赛流，不再用墙钟或系统私有种子。同一固定种子下 soak digest 应一致。
- V1 `stackSnapshotPolicy` 仍是 Replace；未实现 KeepPerLayer。mode 2/4/5 与 Unity 保持 DEFERRED/UNAVAILABLE。

### 2026-09-05（Lumio P4：Tag 层级 A2 + 准入序对齐 A5）
- **准入原因对调用方可见**：`ActivateCore` 第二段改为 `PhaseNotAllowed` → `Cooldown` → `Cost` → 实体 `NoTarget` → `TagRequirementsNotMet` → `UnsupportedDefinition`（时长合同 / 内容 CanCommit / 未知 execution）。形状类 `NoTarget` / ForbidEffects·heal-only 仍在冷却前。建造期战斗技能在冷却未转好时仍报 `PhaseNotAllowed`，不再先看到 `Cooldown`。空目标列表仍是 `NoTarget` 且发生在冷却检查之前。队列满仍是 `QueueOverflow`。
- 内容 `CanCommit` 失败与时长合同失败从 `InvalidRequest` 改为 `UnsupportedDefinition`。
- **Tag**：`GameplayTagVocabulary` 带 parent；授予 `Stun` 时 `Control` / `Debuff` 计数各 +1，移除对称。`stackKey` 与 `EffectApplied.Tag` 仍是叶 id。未知 Tag 启动硬失败（补测试）。

### 2026-09-05（Lumio P3：属性公式 A1，⚠️ 数值语义变更）
- **同一 AttributeKey 公式**：`aggregated = (Base + ΣAdd) × max(PercentFloor, 1 + ΣPercent)`；若存在 winning Override，则 `computed = Override`（Add / Percent 全部忽略），再 Clamp。Override 不再换聚合起点，也不另设 `FinalOverride`。同 priority 时 `Sequence` 更大者胜。
- **Percent 是加项**：`+30%` 配 `0.30`。两个 `Percent(0.30)` 得到 `1.60`，不是 `1.69`。`PercentFloor` 进 `AttributeSchema`，默认 `0`：两个 `Percent(-0.60)` 因子为 `0`，不是 `-0.20`。
- **生产映射**：CatalogCompiler BuffAllies `Multiply(1+x)` → `Percent(x)`；`SkillSystem` Attack+10% 仍写 `Multiply(1.1)`，`LegacyEffectAdapter` 运行时映射为 `Percent(0.1)`。`MaxStacks=1` / 无其它乘区时与旧 `ΠMultiply` 数值一致（允许 ulp）。
- **残留 Multiply**：Catalog 校验拒绝；`AttributeAggregator.AddModifier` 运行时拒绝。`product(Multiply)` 已从 Aggregate 删除；枚举值暂留作 legacy 输入，无法进入聚合。
- **叠层**：Percent 走既有 P1 乘数（`stackCount × magnitude`，一份 handle），不再保留 ΠMultiply 的不乘层数行为。Override 仍不乘层数。
- combo 仍走 `DamageOutputMultiplier` 的 Add；跨通道 `computed(AttackDamage) × computed(DamageOutputMultiplier)` 不变。
- **shipped 内容预期零可见变化**：BuffAllies `MaxStacks=1`；SkillSystem AttackBoost 生产仍靠 `BuffType.AttackBoost` 位旗 ×1.1（Instant GE 不写 Aggregator）。golden / 既有面板断言应无 diff。

### 2026-09-04（Lumio P2：Commit 预留 F11 + 预校验后禁止 throw F12）
- **少付钱不再生效**：同帧多条 `AbilityRequest` 入队时按 sequence 预留 Spend 资源与容量；后请求看到的可用 = 当前值 − 已预留。Commit 每条先复查冷却 + 预留，失败整单 `AbilityCancelled`，不调用 `CommitPlan`。支付只走 `ResourceOperation.Spend`（不足即拒、原子、实际扣减必须等于请求）。
- **容量满报 `QueueOverflow`**：入队满槽与 `ValidateCapacityPlan` 失败不再混进 `InvalidRequest`。`GameplayEventType.AbilityCancelled` 加在枚举末尾。
- **commit 竞争失败不炸帧**：`Core/` 与 `Systems/` 中 `prevalidated * during commit` throw 改为拒绝事实。dispel Remove 失败跳过该 slot；玩家伤害未 Accepted 记拒绝并 Cancelled；预警/召唤容量失败跳过；群体复活容量耗尽停止并保留已成功单位；回放写入失败或快照不可用拒绝该次 Restore。通用 `ApplyMana` 夹紧未改（负 `AttributeKey(3)` 夹到 0 仍 Accepted）。
- 预留是当帧瞬态，释放钉在 `ClearAbilityQueue`（`CommitQueuedAbilities` / `RejectQueuedAbilities` / `frame.begin`）。

### 2026-09-04（Lumio P1：账本叠层 F10）
- **P1 无数值变化**：生产 Multiply 仍是 CatalogCompiler BuffAllies（`MaxStacks=1`）与 `SkillSystem` Instant；golden / 既有面板断言无 diff。`AttributeAggregator.Aggregate` 仍是 Override 换起点 + ΠMultiply（P3）。
- `TryApply` 删除「每层 `Array.Copy` 扩 modifier handle」。`stackCount` 是该 ActiveGameplayEffect 对 ΣAdd 的乘数；Override / Multiply 不乘层数。
- `TryApply` 与 `TryRestack` 的 MaxStacks 路径共用 `RestackLedger`：Replace 捕获、按定义条数重建派生缓存。
- `periodicMagnitude`（写入 `CapturedMagnitude`，供 tick）与 `modifierCapture`（`CapturedModifierMagnitude`）拆分；V1 `stackSnapshotPolicy` 只实现 Replace，且只作用于 modifier 捕获。
- 容量按定义计：每个 ActiveEffect 只占一份 `Modifiers.Count`；`CanApplyPlan` / `CountPlanOccupancy` / `ValidateCapacityPlan` / `TryApply` restack 不再按层数要槽。

### 2026-09-04（文档：Lumio P0 终态合同冻结，文档-only）
- 终态写入：§3.1 Tag 计数+parent 词汇+祖先展开边界；§5.2/§5.3 账本无独立生命周期与 `ModifierPool` 派生缓存；§6.1 整张准入表与 Commit 复查/`Spend`/`ClearAbilityQueue`；§6.3 同帧顺序、禁止抵消、V1 默认 `Replace`；§6.5 瞬时能力不背状态机；§8.2 `AbilityCancelled`；§9 排期本按 clock、随机只在 `CommitSerial` 领号；§11 墓碑；§13 明确 `Rng.Shared` 不是确定性资产；§14 不变量 12–14。
- 对照计划 §2.1 文档状态改为已写入；缺口计划 §9.3 改为已写入。
- **未改生产代码**。§7 公式保持 Lumio 整段，未改回 Override 换起点 / `FinalOverride` / `ΠMultiply`。

### 2026-09-04（文档：Lumio 对照收口计划）
- 新增 [docs/plan/ecs-gas-lumio-contract-alignment.md](docs/plan/ecs-gas-lumio-contract-alignment.md)：P0 终态冻结 → P1 账本（F10）→ P2 Commit 预留（F11）+ 预校验后禁止 throw（F12）→ P3 Lumio 公式 → P4 Tag 层级/准入序整表 → P5 同帧顺序与随机领号。
- 审查修订：`stackSnapshotPolicy` V1 默认 `Replace`（`KeepPerLayer` 不做）；支付走 `Spend` 不改通用夹紧；复查硬性置于 `CommitPlan` 之前；A1 补 `SkillSystem` Multiply；A3 补九处无种子 `new Random()`；A4 收窄为「一处 dispel remove-first」；准入冻结补形状类与 `QueueOverflow`。
- 缺口计划登记 F10 / F11 / F12；不改生产代码。

### 2026-09-04（终态：属性公式整段收成 Lumio，文档-only）
- **公式拍板**：`aggregated = (Base + ΣAdd) × max(PercentFloor, 1 + ΣPercent)`；若存在 Override，则 `computed = winningOverride`（配表 priority，同级后写赢），Add / Percent 全部忽略。不再把 Override 当成换聚合起点，也不另设 `FinalOverride`。
- `Percent` magnitude 是加项（`+30%` 配 `0.30`）；`PercentFloor` 默认 `0`。跨通道 `computed(AttackDamage) × computed(DamageOutputMultiplier)` 不变，通道上的 Override 只盖该通道。
- **未改代码**：`AttributeAggregator` 仍是「Override 换起点 + ΠMultiply」。切实现时必须单列数值条目，并把 `Multiply(1 + x)` 迁成 `Percent(x)`。

### 2026-09-04（ECS+GAS：能力 granted effect 延到 effect.commit，⚠️ 有时序变化）
- **CommitPlan 不再同步 TryApply**：能力 granted effect 只 `EnqueueApply` 进 `EffectRequests`，与世界 DoT（Firewall/岩浆/尸体）同在 Combat 后的 `effect.commit` 才 `TryApply`。伤害/治疗/CC/预警执行项仍在 `ability.commit` 当场提交，技能伤害请求继续先于塔攻入队。
- **Combat 段读不到当帧能力 modifier/tag**：AI 入队 → PreCombat 只执行载荷并入队 GE → Combat（无当帧 granted 修饰）→ SkillBuff `effect.commit` → `effect.tick`。全帧结束后的 tag/投影断言仍成立。
- **图声明**：`skill-buff.skill.update` 去掉假 `DamageRequests`；`ability.commit` / `build.ability.commit` 改写 `EffectRequests`，不再声明 `ActiveEffects`/`AttributeModifiers`/`EffectEvents`；`ai.enemy-ability.execute` 只写 `AbilityRequests`；`non-wave.ability.reject` 与 build 侧对齐清队列。
- **兼容 Execute\***：敌方技能 typed 失败即拒绝，不再 AI 当场改血/控/预警。非 strict 的 `SkillSystem` shape handler 仍保留给测试夹具。
- 仍不宣称整个 M5/M6/F4–F9 完成：生产 DoT 仍是空 modifier Periodic；未按占用做稀疏 Ability 池；未删 `FrameResource.EffectRequests` 枚举位。

### 2026-09-04（ECS+GAS 终态收口续：Ability/Effect 真 buffer + PreCombat commit，⚠️ 有数值与时序变化）
- **AbilityRequests 真 buffer**：`ActivateCore` 校验后入队；Seal 后 `DeferAbilityAndEffectCommit`，由 `ability.commit`（Combat 前）/ `build.ability.commit` drain。满槽拒绝且不提交。`frame.begin` 对未消费队列记 `UnconsumedAbilityRequests` 并清空。
- **EffectRequests 活管线**：`ApplyDot` / catalog Periodic 入队，`effect.commit`（`effect.tick` 前）与 `post-death.effect.commit` 走 `TryApply`。`TryAdopt` 挂槽后补 `ApplyModifiers`，失败撤槽。
- **时序**：技能/全局/塔主动/英雄技能在 Spatial 之后、Combat 之前只入队；技能伤害请求先于塔攻入队，同帧仍由 primary `damage.commit` drain。走 catalog `Activate` 的敌方技能延到 PreCombat commit；兼容 `Execute*` 仍在 AI 当场结算。预警带在 `ability.commit` 才创建，当帧 `spatial.telegraph.update` 已过，首次倒计时从下一帧开始。
- **数值：None DoT 同 key 不叠槽**。`Stacking.None` 重复 Apply 不刷新、不新开槽 → 尸体区 / 多塔 Firewall / 岩浆 DPS 相对脉冲重挂下降。技能 DoT 现在会在同帧 `effect.tick` 跳（以前 `skill.update` 在 tick 之后）。
- **稀疏 AbilityState**：`AbilityPool` / `ActiveAbilityStore` 为 owner；`AbilityInstances[]` 仍是同帧 facade，Unity 可见形状不变。burrow / leap / totem 冷却并进 `MechanismCooldownColumn`（允许 -1 哨兵）。
- 仍不宣称整个 M5/M6/F4–F9 完成：生产 DoT 仍是空 modifier Periodic；未把十几族机制冷却全部并进 AbilityState；未删 `FrameResource.EffectRequests` 枚举位。

### 2026-09-04（ECS+GAS 诚实化：AbilityRequests 日志 / Rally writes / 脉冲 DoT 并存，⚠️ 有数值变化）
- **AbilityRequests**：不是 command buffer。`ActivateCore` 仍同步 `CommitPlan`；只在 **accept 之后** 写入当帧日志，`frame.begin` 清空。冷却 / plan 拒绝不再占 256 槽；日志满不得回滚已提交激活。`build.skill/auto-skill/global-skill.update` 补 `AbilityRequests` 写声明（Build 治疗/护盾可走到 ActivateCore）。没有 consume/commit 节点。
- **Rally 图声明**：`combat.rally.consume` / `skill-buff.rally.update` 的 writes 从假 `TowerCombatCache` 改为真实的 `TowerState`（`TowerRallyAtkSpdBonus`）+ `PlayerAttributes`。重算批准根与 topology hash。
- **数值：脉冲 DoT 可并存多份**。`ApplyDot` 的 None 改回 `TryAdopt`（每次新槽），不再走 `TryApply` 同 key 不刷新。尸体区每脉冲、多座 Firewall、岩浆每帧重挂恢复为多份 Periodic（受 `MAX_ACTIVE_EFFECTS_PER_ENTITY = 8` 上限）。这是对 2026-09-03「None 不再多开槽」的生产语义纠正；GAS `TryApply` None 本身仍是同 key 不刷新。
- 仍不是终态：没有 AbilityRequests consume/commit 节点；`TryAdopt` 仍不 `ApplyModifiers`；`AbilityState` 仍嵌在 `AbilityInstance`；burrow/leap/totem 等机制 SOA 冷却未并；`EffectRequests` 仍是死 token；生产 DoT 仍是空 modifier Periodic，不是带 `ENEMY_HEALTH` 的 catalog 伤害修饰。

### 2026-09-03（ECS+GAS 缺口续：Rally 拆通道 / AbilityRequest buffer / cooldown 合并 / catalog DoT，⚠️ 有数值变化）
- **Rally**：不再订阅 `EventBus.PlayerDamaged`，只读消费 `ResourceResolver.Events` 的 `DamageApplied`。新增 `combat.rally.consume`（`tower-attack` 之前）重写塔加成；`skill-buff.rally.update` 再消费一次以覆盖战斗段伤害。四站点仍可发 `PlayerDamaged`（渲染），但不再激活 Rally。
- **数值：原静默站点也会 Rally**。thorns / trample / leap / projectile（及一切 `ApplyPlayerDamageAuthority`）现在会触发 Rally。战斗段伤害同帧要等到 `skill-buff.rally.update` 才激活，该帧塔攻已结束；下一帧 `combat.rally.consume` 才让塔吃到加成。近战等战斗前伤害可在同帧 `tower-attack` 前生效。持续帧因 consume 在清零之后、塔攻之前重写，塔攻速加成不再只活在触发当帧的 SkillBuff 段。
- **F4 进度**：`ProductionDotCatalog` 在 Compile 末尾登记 `Firewall_Burn` / `prod.dot.lava` / `DemolishBurn` / `DemolishPoison` / `corpse_zone_tick_*`。`ApplyDot` 优先物化后 `TryApply`（Periodic **空 modifier**，不带 `ENEMY_HEALTH`）；非 None 仍 `TryRestack`。Skill 有 catalog Periodic 时直接 `TryApply`，否则仍 adapter + 空 modifier `TryApply`。
- **数值：None 叠层**。同 catalog key 的 None 不再走 `TryAdopt` 每次新开槽；`TryApply` 遇已有 key 且 Refresh=None 时不新开槽、不刷新 timer。
- **F9 进度**：`ComponentStore.AbilityRequests` 为 `CommandBuffer<AbilityRequest>`；`ActivateCore` 校验后入队；`Activate(AbilityRequest)` 按句柄/AbilityId 解析槽位（含 AbilityId(0)）。多目标仍构造 `AbilityActivationRequest`，但同样经 `ActivateCore` 入队。敌方 / 英雄 / 全局技能 / 塔主动技能冷却并进 `AbilityState` 或 `AbilityCooldownColumn`。
- `TryRestack` 在 StackKey 命中后还要比 `LegacySnapshot.Name`，不同名不合并。
- 仍未宣称 F4–F9 终态收口：`AbilityState` 仍嵌在 `AbilityInstance`（不是稀疏池）；burrow / leap / totem 等机制 SOA 冷却列未并；`FrameResource.EffectRequests` 仍是死 token（保留以免枚举移位）；`TryAdopt` 仍不 `ApplyModifiers`。

### 2026-09-03（文档：ECS+GAS 计划收进 docs/plan）
- 将 `ecs-gas-migration-plan.md` / `foundation` / `combat` / `orchestration` 从 `docs/` 根目录移入 `docs/plan/`，与缺口修正计划同目录。证据与终态架构仍留在 `docs/`。

### 2026-09-03（ECS+GAS 缺口收口续：Adopt 校验 / TagState / 死亡回调声明）
- `TryAdopt` / `TryRestack` 与 `TryApply` 共用 BlockedTags（reason 8）和 Periodic payload / 非正 magnitude（reason 2/4）校验。
- `HasTag` 只读 `TagState` 贡献计数，不再在未命中时扫槽。
- `primary-death.callback-dispatch` / `cascade-death.callback-dispatch` 去掉不存在的 `AbilityRequests` 写声明；重算 Gameplay/FixedPopulation 批准根与 topology hash。
- 仍未做：`PlayerDamaged` / Rally 拆通道；`AbilityRequest` command buffer；敌方/英雄 cooldown 列合并；5 个 ApplyDot 创建点 catalog 化。

### 2026-09-03（ECS+GAS 缺口修正 F4–F9 进度 + 负血量旁路，⚠️ 有数值变化）
- **F4 进度**：`ApplyDot` 的 None 走 `TryAdopt`，DurationRefresh/MaxStacks/MaxStacksRefresh 走 `TryRestack`；该路径的 timer 写入只在 `GameplayEffectRuntime`。BuffSystem 对 RuntimeOwned 不再 tick。`TryAdopt`/`TryRestack` 仍不跑 BlockedTags / Periodic payload 校验；5 个生产创建点尚未 catalog 化。
- **BossTrail / SuicideBomb** 改为 `ApplyPlayerDamageAuthority`；首次吃护盾/下限/复活。负 `AttributeKey(3)` 玩家写入白名单清空。
- **F5 进度**：`SourceDeathPolicy.Transfer`；Catalog 修饰器可读 `Clock` / `FirstTick` / `CatchUp` / `SourceDeath`。
- **F6 进度**：`TryApply` 读 BlockedTags（reason 8）；`TagState.AddGranted` 在 `TryAddGameplayEffect`。HasTag 计数未命中仍扫槽。
- **F7 进度**：Periodic / Ability `MagnitudeSource.Attribute` 不再当常量。
- **F8**：`Explicit` 不再被每帧 `ResetFrame` 清掉，只在 `ResetCounters` 时清。
- **F9 进度**：`AbilityState` 嵌在 `AbilityInstance`；`FindSlot` 优先 AbilityId；激活里构造 `AbilityRequest` 做合法性检查。主入口仍是 `AbilityActivationRequest`；敌方/英雄 float[] 与 SOA cooldown 列未合并。
- 未做：`PlayerDamaged` / Rally 拆通道；死亡回调节点上的 `AbilityRequests` 声明。

### 2026-09-03（ECS+GAS 缺口修正 — overflow 预检与剩余假 Request 声明）
- `CanApplyPlayerDamageAuthority` 增加 `CanAccept(0,2)`：队列溢出时近战预检失败，不重置 stealth、不 Commit 冷却。
- `build.skill.update` / `build.auto-skill.update` / `build.global-skill.update` 去掉不存在的 `AbilityRequests`；`post-death.corpse.update` 去掉不存在的 `EffectRequests`；重算批准根与 topology hash。
- 裂变/克隆继承父体 `AttackInterval`；死灵召唤写入怪物表 `AttackInterval`。

### 2026-09-03（ECS+GAS 缺口修正 Phase 3 — 玩家伤害权威收口，⚠️ 有数值变化）
- **近战攻击冷却首次生效**：怪物 `AttackInterval` 写入 SOA 并门控 `attack_melee` / `can_attack`；相对旧实现（每帧可打）接触战 DPS **显著下降**，同时把 Resource 事件量从 O(接触敌人数/帧) 压到冷却节奏。
- 10 个生产绕过点统一走 `ApplyPlayerDamageAuthority`；`EnemyAI`/`EnemyAbility` 的 legacy `PlayerDamaged` 改为发布 `applied`（含护盾消耗）；thorns/trample/leap/projectile 保持静默（Rally 不新启用）。
- `EnemyProjectile` 命中目标改为 `PlayerEntityId`（修硬编码槽位 1）；`ProjectileSystem` thorns 独立 ping-pong，避免把 `GameplayCommitLock` 嵌进 damage 锁。
- `ai.enemy.update` adapter 闭包文案更新后重算 Gameplay/FixedPopulation topology hash（批准根未变）。
- `CombatGoldenReplayTests` 手工 Direct composition 在 Tick 前补 `SealGraphComposition`，修复图封印门禁回归。

### 2026-09-03（ECS+GAS 缺口修正 Phase 2 — FrameGraph 声明诚实化）
- 移除 `effect.tick` / `build.effect.tick` / `skill-buff.skill.update` / ability.reject 节点上与真实执行不符的 `AbilityRequests`/`EffectRequests` 声明；删除死代码 `CombatWrite`。
- `combat.player-attack.update` / `combat.tower-attack.update` 补齐 `PlayerResources`（及 player-attack 的 `ResourceRequests`）漏声明。
- 节点更名：`effect.commit`→`effect.tick`、`build.effect.commit`→`build.effect.tick`、`ability.commit`→`skill-buff.skill.update`；同步 BindingFacts / AdapterCatalog / AccessReview / spec / manifest，并重算 Gameplay/FixedPopulation 批准根与 topology hash。

### 2026-09-03（ECS+GAS 缺口修正 Phase 1 — 属性键与 combo 语义，⚠️ 有数值变化）
- **玩家 combo**：由 `(1+b)^n`（Multiply on AttackDamage）改为 `1+n*b`（Add on DamageOutputMultiplier），与 `maxStacks` 公式及 legacy `GetComboDamageMultiplier` 一致；相对旧实现**变弱**。
- **塔首次获得 combo 增伤**：塔 base 迁到 AttackDamage（key 0），投影为 `computed(key0)*computed(key8,1)`，HitConfirmed combo 挂到塔源后 `GetTowerAttackDamage` 生效；相对旧实现**变强**。
- **Ability magnitude**：`ResolveMagnitude` 对敌人/塔/玩家统一读三条 projection，Multiplier 能力伤害首次吃 `DamageOutputMultiplier`；相对旧实现**变强**。

### 2026-09-03（ECS+GAS 缺口修正 Phase 0 — Periodic magnitude 编译丢失）
- curated Debuff+DoT 技能（Poison Nova / Dragon Breath / Meteor Strike）编译 Periodic effect 时补传 `periodicMagnitude`；CatalogValidator 启动期拒绝 Magnitude≤0 的非 GameplayEvent Periodic，避免激活时帧内抛异常。

### 2026-09-03（M8 稳定观察与存储决策，有界候选）
- 死亡 ping-pong 提交只清 prepared read bag，回调重入排入 write bag 的级联死亡可在同帧再次 resolve 或跨 `BeginFrame` 精确提交一次；blocked batch、新死亡、奖励、`KillConfirmed` 与 pending guard 均有回归覆盖。
- `OnEnemyKilled` / `OnTowerKill` 改为注册期构建 subscriber array，dispatch 无 `GetInvocationList()` 分配；单个订阅者抛错不再阻断后续订阅者、销毁或事实 commit，完成整批副作用后重抛首异常。
- Gameplay event queue 新增 1/2/3-fact 无分配提交，Damage/Resource/Shield 成功循环 warmup 后 256 轮实测分配为 0 bytes。原子 reservation 先安全租 buffer 再按统一锁序占 slot；受控第二次 rent 失败会归还首 buffer 且不泄漏容量，Commit/Dispose 幂等。
- 新增只读 `GameplayObservation`，聚合 Resolver/Effect/Trigger/pool 的 high-water、overflow、stale、按 reason 拒绝、abort、unconsumed、publication failure 与 legacy apply；生产 Tick 不自动采样。
- 观察 snapshot 记录实际 `Consume` trigger definition 数量、Damage/Resource publication failure 次数，以及显式开启后无分配的状态/Gameplay event sequence digest；队列容量不足在状态写入前返回明确 `RequestQueueOverflow`，fresh capture 对 storage、production soak、lifecycle soak 做三轮稳定签名比较并复核仓库内容哈希。
- `EffectPool` 保持固定逻辑容量和 generation handle interface，在内部改为 256 槽按需分页。800K 槽同进程 A/B 的初始元数据分配由 7,200,072 bytes 降至 75,160 bytes；跨页、满载、释放复用和 stale generation 均有回归测试。
- 10K 敌 × 500 帧 sealed production soak 与 128 轮 Periodic death/entity recycle soak 提供结构化仓外 JSON；M0/M1/M3/M4 focused evidence 在同一 capture 中回收。
- Archetype 未获授权：没有稳定签名 CPU/cache hotspot 证据；Trigger 利用率远低于 cap。公开 Ability/legacy Effect/Boss-phase dense 候选保留，等待 Unity/外部兼容验证。
- mode 2/4/5 按用户决定继续 `DEFERRED`，Unity smoke 为 `UNAVAILABLE/BLOCKED`；本轮不宣称完整 M8 退出，未提交或推送。

### 2026-09-02（M7 typed installer registration）
- `ProductionSystemInstaller` 成为唯一生产组装入口；schema v3 typed recipe、稳定依赖顺序和 graph↔manifest 双向 Seal 校验取代自由 C# 注册语句。
- M7 commit 为 `4bebc43024a74fd52462d6cb31a19ed0aa34efa3`，保护分支 `codex/installer-registration-boundary-protected` 固定于该提交；fresh evidence 见 `docs/ecs-gas-m7-evidence.md`。

### 2026-09-01（FrameGraph 与统一 TimeContext，待复审）
- 11 个 SystemGroup 中实际配置的 `SetTurn` / `Update` / `Resolve` 调用已拆成稳定 system-node adapter；节点声明真实资源、`Before/After`、phase、time domain、执行策略与 required/optional dependency。拓扑平局按 `FrameNodeId` ordinal 排序，不依赖注册顺序。
- `FrameScheduler.Tick` 仍是唯一帧入口，生产 composition 在 Registry 接线完成时 Build/Validate/Seal。`AbilityCommit`、`AttributeAggregate`、`EffectCommit`、Damage/Resource/Event commit 以及 primary/cascade death resolve 均有单一真实执行节点；legacy 仅能在启动构造时选择。
- 每帧复用执行上下文并冻结不可变 `TimeContext`，统一 raw/real/enemy/combat/effect/build/global delta、turn/frame/phase。`SkillBuffGroup` 的可变 delta fallback 与 `PostDeathGroup.Phase` 已移除；Blinker 的 Pathfinding 缺失策略显式为 `NoOp`，Sapper 改由 Registry 构造并接线。
- benchmark composition 合同以真实执行测试锁定：mode 2 保留 manual merged loop 作为性能下界，不充当 graph wiring 证据；mode 4/5 共用 `BenchmarkCompositionFactory`，经 `SystemRegistry.CreateAll/WireDependencies/AssignToGroups` 生成 sealed `ProductionRegistry` graph，并输出稳定 topology fingerprint。
- mode 5 的 Build/Wave/Intermission/LevelComplete/GameOver/Victory 只经 `StateMachine.TransitionTo` 推进，不再直写 scheduler phase；可执行 harness 锁定 CurrentState、OnEnter 次数和 graph phase gate。`SetLevel` 不再预排首波 callback，首次 `WaveSpawningSystem.Update` 成为唯一发布 owner。RandomEvent callback 改为预分配固定容量有序 batch，同帧 apply→expire 按 player/update 顺序 exactly-once 提交。
- `frame.input.publish` 改为真实 publication seam：读 persistent SOA、执行 memory barrier、只写 `PersistentStatePublished` 令牌与可观察 frame id；validator 要求 persistent reader 位于覆盖该资源的 publisher 之后，不再把无 mutation 节点伪装成 SOA writer。
- gate4 返修后强类型 `FrameAccessProfile` 覆盖 193 个 enabled 节点与 33 个 disabled diagnostic，共 226 个唯一 NodeId。原三份审计报告按 NodeId 去重并集为 207，另有 19 个 supplemental 节点完成独立源码复核；新增持久状态 publish 与 random/wave callback dispatch 边界，普通 SOA reader 不再被 broad external-seed allowlist 放行。Beam 与 LifeLink 保持 `DisabledUnsafe`。完整 topology hash 同时覆盖 enabled/disabled metadata、required/optional dependency policy、binding、owner、review evidence 与 composition kind。
- review fingerprint 现覆盖 `Before/After`、required/optional dependency 与 composition kind，最终 root 再覆盖 available dependency/scenario；disabled metadata 也执行 phase/time/resource/semantics/依赖格式基础校验。Gameplay 与 FixedPopulation 分别固定独立 topology/review root，场景误传无法复用 sealed identity。
- mode4 改为不可变 `FixedPopulationBenchmark` composition：仍执行完整 sealed graph，但 `spawning.wave.update` 经 scheduler graph gate 禁止真实生成和 WaveStart。500 个真实 Tick 逐帧保持 10K 人口且 callback/presentation 为零；mode5 保持 Gameplay 波次生成，marker 明确记录 scenario、wave policy 与 population policy。
- 并行 collect 收口到 owner buffer：EnemyAI attack/phase/lifesteal/decoy death facts、Burrow emerge、AI LifeLink、SuicideBomb、Hero、Bleed、Frostbite、BossTrail 和 ManaBurn 均按 batch/active-index/hero-slot 独占写，屏障后稳定串行 drain；移除对应共享 `ConcurrentBag`、`ThreadLocal.Values`、lock append、共享 Random 与每次 Hero 攻击的 100K 候选数组分配。Protector 同步改为 batch-owned buffer，但当前未进入 Registry/226 production roster。
- TowerAttack 的塔并行体统一只写 active-tower index 独占 buffer；护盾层消费、同敌 recent-damage RMW、元素状态/计时器与 Vanguard 转移全部在屏障后串行提交。新增 16 塔同敌真实攻击回归，连续 12 轮锁定累计伤害、元素合并、护盾消费和转移结果；复扫全部 28 个含 `Parallel.For` 的 system，Beam 仍保持 `DisabledUnsafe`，未扩大其业务范围。
- 恢复 turn-based 时间合同：Burrow duration/cooldown、Fear duration、enemy channel CastTime 均按每 Tick 减一并声明 `TimeDomain=None`，不会因 `0.016` delta 或 bullet-time 产生约 60 倍持续时间漂移；秒制 cooldown 继续消费统一 EnemyDelta。
- 修复 `EnemyMovementSystem` Palisade 接触伤害在 `Parallel.For` 内对同一 towerId 共享 `+=`/flag 的竞态：并行段只写 per-enemy 预分配 scratch，屏障后按活跃敌人顺序串行合并。4096 敌人 × 8 帧压力测试与 internal 纯逻辑合并 seam 的精确累计、flag、固定顺序测试共同锁定回归；同步纠正 TowerSynergy/HealAura/ThornsAura 的串行 metadata。
- effect clock 改为 scheduler/GameManager 构造期固定配置，Seal 后无 setter；`TimeContext` 同时携带 `EffectDelta` 与真实 `EffectClock`，graph/legacy 对非 Combat clock 等价且每个定义时钟只推进一次。mode 2/4 composition 合同改为真实执行一帧生产 benchmark 循环的 harness，不再只检查枚举标签。
- 上一轮 `47220...` / `9768...` / `6204` 性能窗口对应旧生产 DLL；本轮生产代码已变化，证据全部失效且不复用。最新非性能门禁已通过（定向 `96/96`，Core/EXE 0 warning，全量 `1579/1579`，test rules 0 违规，diff-check exit 0），mode2/4/5 未运行，Spec/Standards 双轴复审仍待执行。

### 2026-08-29（第四批：架构审查阶段 0 —— 7 项正确性修复 + ElementalReactionSystem 接线）
- 承接 `docs/skill-combat-arch-review.md` 的阶段 0。每项都做了「撤销修复 → 对应测试变红 → 恢复 → 全绿」的回退验证，确认测试真能捕获 bug 而非假绿（这是本轮唯一的防假绿手段：`DeathMarkSystem.GetDamageMultiplier` 就是靠直接调 getter 而长期绿着的反例）。生产代码 +87/-7 行（不含第 8 项接线），测试 1298→**1325**。第 8 项（接线）是本轮唯一有战斗语义变化的改动，单列在下方。
  1. **沙暴 DoT 击杀不入队（本类唯一可达的实例）**：`WeatherSystem.cs:111` 裸写 `EnemyHealth -= dmg` 后从不 `QueueEnemyDeath`（该文件计数为 0），而全局没有「扫描 HP<=0」的兜底 sweeper（唯一兜底 `MovementGroup.cs:182` 只覆盖自身那条 DoT）。系统确实在跑：`SystemRegistry:424` 构造、`:791` 接入 PreGame、`FrameScheduler:150 → PreGameGroup:21` 每帧调用，且 `weather.json` 的 `Sandstorm` 带 `enemyDotPct: 0.005`，`TransitionWeather` 以 70% 概率在 5 种天气中随机选中。后果不是「永久僵尸」而是更具体的：不给金币、不计击杀、**继续行走**（`EnemyMovementSystem` 只按 `EnemyActive` 门控）、期间免疫再次击杀，最后走到底由 `GameManager:461` **白扣一条基地命**。补入队时不重复夹血（`:109` 已调 `ClampDamageToHealthFloor`）。
  2. **Meteor 击杀不入队**：`GlobalSkillSystem.cs:187` 同类问题一并修。**当前配置不可达** —— `GameConfig.GlobalSkills` 生产端 0 处写入（loader 无 global-skill 解析、`Data/` 无对应键），所以这是防止将来接线时带病上线。
  3. **GAS 槽计数不清零（回收 ID 继承 DoT/CC）**：`DestroyEntity` 共享段补 `AbilityCount/ActiveEffectCount` 归零 —— 此前只有 `ResetPlayerAbilities` 清，而实体 ID 走 `freeEntityIds` 回收。已验证 `AddEnemy` 不会独立清零（排除假绿）。真正生效的是 `ActiveEffectCount` 那半（可达路径为 `BuffSystem.ApplyDot ← TowerAttackSystem:2094` 的 Firewall 分支，`tower_061/086/111.json` 确有该类型）；`AbilityCount` 那半是防御性的，且已验证不会误清玩家技能栏（`nextEntityId` 从 2 起、`AddPlayer` 不走 `CreateEntity`、全部 `DestroyEntity` 调用点只传敌人/陷阱/回声克隆/塔）。
  4. **元素状态/计时器不清零**：`EnemyElementStatus` + `EnemyElementTimer`（4 槽/敌人）补重置。它们由 `ApplyEnemyDamage:2184-2187` 破盾路径与 `TowerAttackSystem:1903` 附魔路径写入，但**唯一的衰减/清除逻辑在从未构造的 `ElementalReactionSystem`**（见下），所以回收 ID 会永久带着上一任的元素位 —— 而 `TowerAttackSystem:1820` 的元素亲和加成是**活跃读者**（`tower_elemental_affinity.json` 设了 `ElementalAffinity`+`Bonus:0.30`），会据此给新敌人白送伤害倍率。
  5. **`_pendingShieldBreaks` 无界增长**：`BeginFrame` 补 `Clear()`。该 List 由 `ApplyEnemyDamage:2189` 每次破元素盾追加，唯一消费者（含 `Clear`）同样在从未构造的 `ElementalReactionSystem` → 整个会话只增不减。shipped 数据可达（`monster_shield.json`/`monster_enforcer.json` 带 `Shield`+`ShieldElement`，由 `WaveSpawningSystem:993-1001` 接线）。已确认 5 个 `ApplyEnemyDamage` 调用点全在串行段，故此前是纯泄漏而非竞态。
  6. **构造顺序制造 null —— 实为两处**：`ReflectTower`（原 `:399`）与 `TowerStealth`（原 `:402`）都排在消费者 `SuicideBomb:396` 之后被当参数传入，存进 `readonly` 字段且无补注 setter。两者构造函数都只要 `(store, playerId)`，无自身顺序约束，已一并前移到 `:376`。**审查原稿只记了 `ReflectTower` 一处**，漏了 `TowerStealth`。影响面修正：`EnemyAISystem` 里那个字段除构造赋值外零使用（死参数），真正的消费者 `SuicideBombSystem` 整条链被零写入的 `EnemyIsSuicide` 门死 → 修复正确且必要，但当前并非「已发货机制失效」。
  7. **塔回收字段漏清 10 项**：`DestroyEntity` 塔分支补 chrono/patrol/selected 共 10 个字段。这是塔侧**真正的** ID 复用面：`RemoveTower` 清 208 个、该分支清 89 个、差集 150，其中 140 个由 `AddTower` 重新初始化（照抄会白写 140 次/塔销毁，故**不委托 `RemoveTower`**），只有这 10 个两处都不写。默认值逐个对齐 `RemoveTower`，其中 `TowerPatrolAttackSpeedPenalty` 必须是 `1f` 而非 `0f`（它是攻速乘数，写 0 会让回收槽位的塔永远打不出攻击）。目前是防御性修复：写入者被 `tc.IsChronoTower`/`tc.IsMobile` 门控，而没有任何 shipped `Data/Towers/*.json` 设这两个键。
  8. **`ElementalReactionSystem` 接线（本轮最重要的一项，⚠️ 有战斗数值变化）**：该系统 395 行、CHANGELOG 记录过两轮功能交付，但**从未被构造** —— 全库 `new ElementalReactionSystem(` 0 次、`SystemRegistry` 无字段、11 个 `*Group.cs` 无属性、连测试都没有。它独占三项职责，因此三项全是死的：①元素计时器衰减（`EnemyElementStatus`/`EnemyElementTimer` 由 `ApplyEnemyDamage:2184-2187` 破盾路径与 `TowerAttackSystem:1903` 附魔路径写入却永不清除）；②`PendingShieldBreaks` 消费；③`EnemyExposureMask`/`EnemyExposureTimer` 写入 → `TowerAttackSystem:1838` 与 `PlayerTowerAttackSystem:596` 读的 **+30% 异元素易伤永不触发**。上面第 4/5 项只是它缺席的止血。现已补齐 registry 字段 + 构造（紧随 `Buff`）+ `SkillBuffGroup` 槽位与调用。**执行相位由两个硬约束定死**：`Update()` 必须晚于本帧全部 `ApplyEnemyDamage`（才看得到 Combat/Phase 8 追加的破盾），`ResolveReactionDamage()` 必须早于 Phase 10 死亡结算 —— `SkillBuffGroup`（Phase 9）是唯一同时满足的窗口，且已核验 Phase 10–12 无任何系统再追加破盾。**注意这不是 no-op**：接线激活了 ③，即元素亲和塔（`tower_elemental_affinity.json` 设 `ElementalAffinity`+`Bonus:0.30`）之外又多一条 +30% 易伤通道，会改战斗数值。**这也是审查原稿最大的单点事实错误** —— 它从头到尾把该系统当活的引用（A 表的元素破盾反应、B 节的 `ModifierOp`、D-2 的"敌人会被加 effect"证据链都经由它）。
- **压测覆盖盲区（本轮发现，未改）**：`BenchmarkSystem` 对 `SystemRegistry` 的引用数为 **0** —— 三个压测模式全部手工接线系统，`SkillBuff` 只设 `Skill`/`Buff` 两个槽。所以**压测根本不经过 registry 的装配路径**，第 8 项的接线在压测中槽位为 null、两次调用退化为 null 检查。这意味着「压测数字没变」对本轮接线不构成性能证据。为拿到真实成本，临时把 `ElementalReactionSystem` 接进 mode5 量了 5 跑：中位 ~4224，落在未接线时同会话观测带 4002–4489 内，无可辨识回归；随后已还原 `BenchmarkSystem`（md5 校验一致、`git status` 视为未修改）。建议后续让压测复用 `SystemRegistry`，否则任何「只在 registry 里接线」的改动都不受压测保护。
- 验证：`dotnet build BattleSystemECS.Core` 与 `dotnet build BattleSystemECS.Tests` 均 0 warnings/0 errors；`dotnet build`(EXE) 0 errors + 3 warnings（均为既有 `NETSDK1138`：`BattleSystemECS.csproj` 的 `net6.0` 已 EOL，与本轮改动无关）；`dotnet test` **1325/1325 PASS**；`git diff --check` 干净。测试静态规则：环境无 `pwsh`（`tools\check-test-rules.ps1` 是 pwsh 脚本，Windows PowerShell 5.1 会把其 UTF-8 中文读成解析错误），改用等价规则复现全部 50 个新增/改动测试方法 —— 零断言 0 项、恒真/恒假断言 0 项。压测（`--no-build`，Release）：mode2 11134–12807、mode4 5149–5560、mode5 3808–4489。**本会话绝对值漂移严重，不能与记录基线直接比**：字节完全相同的源码（md5 校验）两次测得 mode2 12591/12757 与 11542/11134，落差 13%。因此改用同会话 A/B 判断回归：把 4 个生产文件换成 HEAD 版本后 mode5 中位 ~4286，本轮改动 ~4429，**改动侧不低于 HEAD**；第 8 项接线单独量得 mode5 中位 ~4224，落在同会话噪声带 4002–4489 内。三模式均远高于 §7.1 硬门禁（3000/3000/2500）。

### 2026-08-29（第三批：技能/战斗系统可维护性重构）
- 架构分析 + 6 项针对性修复（零战斗语义变化；1298 测试全过；生产代码净 -177 行）：
  1. **技能 id 归一化约定集中到 GameConfig**："[0, SkillDefs.Count) 索引共享表、其后偏移索引 Skills 回退"的约定此前只存在于 HeroSkillSystem 的私有实现与注释里，而 TowerActiveSkillSystem.ResolveSkillName 用裸索引分别查两张表 —— 同一 skillId 在两个系统解析结果不同（回退区 id 会被误读或落入歧义）。新增 `GameConfig.GetSkillIdByName / TryGetSkillById / GetSkillDisplayName` 作为唯一约定载体，两个系统全部委托，后续消费方（TowerConfig.ActiveSkillId 等）不会再各自实现走样。
  2. **SkillSystem 圆形 AoE 谓词收敛**：9 处逐字重复的「存活 + 非玩家 + 半径内」过滤谓词（Single/Circle/Freeze/Slow/Polymorph/GroundTarget/AoeStun/AoeRoot/AoeKnockback）收敛为一个 `CollectCircleHits`；4 处相同的串行段「入队伤害 + 逐敌日志」循环收敛为 `QueueDamageForMergedHits`。新增命中规则（如新的门控检查）从改 9 处变为改 1 处。
  3. **ExecuteAbility switch 去魔法数字**：case 0..21 裸数字改为 `AreaShapeType.*` 命名常量（常量表早已存在）；ChainHeal / MassResurrect 两个内联参数推导块提取为 `CastChainHealAbility` / `CastMassResurrect` 私有方法，与其他 case 对称。
  4. **SkillSystem.Update 冷却不变量外提**：CDR clamp + Adrenaline 倒数是每玩家不变量，从每槽位重算改为每帧一次（乘法结合顺序保持原样，位级一致）。
  5. **TowerAttackSystem 朝向 dot 数学共用**：Round 174 每塔 backstab 与全局 PositionalDamage 两段各自手写的「归一化敌朝向 + 攻击方→敌向量 + dot」提取为静态 `TryComputeRearDot`（零分配纯函数），第三层朝向效果出现时不再复制。
  6. **文档修复**：HandleKill 顶着从 CastFreezeArea 复制来的错误 doc、CastFreezeArea 的 `<summary>` 缺闭合标签（XML doc 损坏）、TowerAttackSystem 攻击主循环内重复两行的分隔注释。
- 验证：build ×2 均 0 warnings/0 errors；`dotnet test` 1298/1298 PASS；check-test-rules 0 违规；`git diff --check` 干净。压测：mode2 7709 / mode4 5313（3 跑中位）/ mode5 ~4590（3 跑），硬门禁全过；mode5 当日噪声带宽 4269–4791（stash A/B：HEAD 两跑 4269/4791，重构后 4567–4600 落在带内），无重构回归；mode2/4 与重构前同会话分布持平。

### 2026-08-29（第二批：死配置接线）
- 三处死数据全部接线（`GameConfig.SkillDefs` 共享技能表 + TowerOvercharge/PositionalDamage 段解析；测试 1281→1298，战斗行为默认零变化）：
  1. **SkillDefs 共享技能表**（此前仅存在于 HeroSkillSystem/TowerConfig 注释中的 "SkillDefs[]"，实现缺失）：新增 `GameConfig.SkillDefs`，`LoadSkillDefs` 用 System.Text.Json 加载 `Data/Configs/skills.json`（20 个精选技能，含完整 shape/DoT/CC 字段与此前无处安放的 `Modifiers` 数组 → 新增 `SkillModifierDef` 模型）+ `Data/Skills/*.json`（150 个静态定义），按名去重合并（精选优先）。Core csproj 补 `Data/Skills` 的 CopyToOutputDirectory（该目录此前从未随产物分发——死数据的又一症状）。
  2. **HeroSkillSystem 槽位修复**：名称解析此前只在 150 个占位玩家技能里找 → `hero_skills.json` 引用的 "Cross Slash"/"Guardian Heal"/"Cold Nova" 等精选技能名解析恒失败 → 槽位恒 -1、系统休眠。现按 SkillDefs 优先 → Skills 回退的归一化索引空间解析，4 槽位真实配置生效（集成测试断言 `HasAnyConfiguredSkill()==true`，接线前该断言必失败）。
  3. **TowerOvercharge 段解析**：此前 JSON 有段无解析代码，`TowerOverchargeSystem` 一直跑代码默认值（两处数值恰好一致，纯配置正确性修复）。新增 `ParseTowerOverchargeConfig`（ExtractFloat 风格，与 ParseComboConfig 一致）。
  4. **PositionalDamage 段接线（门控消费）**：此前无模型无解析。新增 `PositionalDamageConfig`（Backstab/Flank 角度+倍率+Enabled 总开关），`TowerAttackSystem` 消费点与 Round 174 每塔 opt-in backstab 层相互独立（新增隔离测试验证不叠加）：背刺锥 dot>cos(B/2) ×BackstabMult、侧袭带 ×FlankMult、正面无加成，cos 阈值 SetGameConfig 时缓存；角度非法（B∉(0,180] 等）视为关闭防 NaN。**JSON 未提供 Enabled 键 → 默认关，热路径一次 bool 读短路，零行为变化**；要启用需在 JSON 显式加 `"Enabled": true`。
  5. TowerActiveSkillSystem 的 TriggerTowerActive log 行现从 SkillDefs/Skills 解析技能名（原只打 skillId 数字）。
- 测试：+17（loader 注入式解析单测 5 / HeroSkillSystem SkillDefs 优先与回退 2 / 集成 4（含 hero 槽位端到端）/ PositionalDamage 真实攻击路径 6：背刺/侧袭/正面/关闭/非法配置 Theory + 层隔离）。修正一处测试设计：正面场景须用"朝向塔的敌人"而非"塔下方的敌人"（塔索敌有 Y 方向性）。
- 验证：build ×2 均 0 warnings/0 errors；`dotnet test` 1298/1298 PASS；check-test-rules 0 违规；`git diff --check` 干净。压测：mode2 8116 / mode4 5075 通过；mode5 4405–4792（4 跑），stash A/B 基线 4329/4330 —— 接线后分布不低于基线，无回归（单次 4405 为当日噪声低端；当日 mode5 波动带本身 ±5%）。

### 2026-08-29（第一批：审查核实 + 低风险修复）
- 架构审查结论核实 + 第一批低风险修复（审查 7 大项全部证实；本批 5 项改动零战斗语义变化，1281 测试全过）：
  1. `game_config.json` 结构修复：原文件根对象在 6278 行提前闭合，`Combo`/`TowerOvercharge`/`PositionalDamage` 三段被拼接在根对象之外（严格解析器报 `Extra data`，整个文件不是合法 JSON，只有手写字符串扫描 parser 能读）。外科手术式修复：三段移入根对象成为正常成员，文件现可用标准 JSON 解析器加载，为后续迁移 `System.Text.Json` 解除阻塞。`ParseComboConfig` 的 `IndexOf` 扫描行为不变（键首次出现位置语义一致）。另核实：三段中仅 `Combo` 有解析代码，`TowerOvercharge`/`PositionalDamage` 为死配置（`GameConfig.TowerOvercharge` 一直用代码默认值）。
  2. `SkillSystem` 四处遗漏的串行循环 → `CollectHits` 两阶段收敛：`CastFreezeArea`/`CastAoeStun`/`CastAoeRoot`/`CastAoeKnockback` 原为纯串行 foreach（与类注释宣称的 "parallel collect → serial apply" 不符，是 8 处统一改造的遗漏项）。收敛后命中序 = 敌人索引序，RNG 逐敌顺序与原实现一致（AoeCcTests/Freeze 测试全过）；Stun/Root 的 turns 计算为循环不变量，提出循环外。
  3. 每帧 `new ParallelOptions` 漏网清理（上轮 24 处清扫的补遗）：`BenchmarkSystem` mode2 合并循环（→`ParallelOptionsCache.HotPath`）、`ManaBurnSystem` 并行段（新增 `ParallelOptionsCache.Capped4`，保持 DOP=4 语义不变）。
  4. `SkillSystem` 死代码清理：`POISON_NOVA_*` 三常量定义后从未被引用，删除。
  5. 核实记录（无代码改动）：mode2/4 压测中 `skill.SetTurn` 从未被调用且 `BenchmarkSystem` 丢弃 `LoadConfig` 返回值（用空 `GameConfig`），技能施放在 mode2/4 实际为 no-op，仅 mode5 真实驱动技能链路；`Data/Configs/skills.json`（13KB）与 `Data/Skills/*.json`（150 文件）无任何代码加载（AGENTS.md 所述"静态定义"与实际不符，待决策删除或接线）。
- 验证：build ×2 均 0 warnings/0 errors；`dotnet test` 1281/1281 PASS；check-test-rules 0 违规；`git diff --check` 干净。压测：mode4 5164 / mode5 4721 通过；mode2 当日本机噪声带 6791–8309（stash A/B 对照：基线两跑 6791/8026，改动后三跑 7496/7995/8309，分布无法区分，无回归；8333 记录基线为 08-27 较快机器状态下取得）。

### 2026-08-27
- 并行热路径性能优化（4 项生产代码改动；1281 测试全过、三模式压测全部提升、战斗语义零变化）：
  1. `PlayerTowerAttackSystem`：并行阶段每次命中抢全局锁（`_damageQueueLock`）把并行收集打回串行 —— 重构为按批分区无锁收集（256 敌/批、每批独占缓冲、帧末按批序合并、确定性顺序），并新增敌数 <500 的纯串行快速路径。mode4 PlayerAttack 27.0→12.1ms（−55%）。修复批缓冲初始槽位为 null 导致 10K 压测 NRE 的问题（槽位按需实例化）。
  2. `SkillSystem`：8 处 `Parallel.ForEach` + 每命中加锁的 AoE 命中收集 → 统一 `CollectHits`（<500 敌串行 / 否则无锁批分区并行）；删除 7 个每站点命中列表与 8 把锁对象；`_skillDamageQueue` 确认仅串行访问后移除全部锁包裹。
  3. `TowerAttackSystem`：塔数 <8 时塔循环由 `Parallel.For` 改为串行调用同一 lambda 体（少量塔时 TPL 启停 + 跨核同步开销大于并行收益）。mode4 TowerAttack 19.4→15.6ms（−20%）。
  4. 全仓 24 处每帧 `new ParallelOptions { MaxDegreeOfParallelism = Environment.ProcessorCount }` → 新增 `Core/ParallelOptionsCache.HotPath` 静态缓存（消除每帧分配；ManaBurnSystem 专用 MaxDegreeOfParallelism=4 保持不变）。
- 新增 DOTS 迁移可行性评估：`docs/dots-migration-evaluation.md`（结论：不做全量迁移 —— Entities/Burst 无法脱离 Unity 工具链编译运行，CLI 基准/测试门禁工作流无法保留；模拟侧仅占 60FPS 帧预算 ~1.2%，性能不构成迁移理由；推荐"维持 SOA + 定点优化"，可选 Unity 侧局部 Burst 实验路线）。
- 验证：`dotnet build BattleSystemECS.Core` 与 `dotnet build` 均 0 warnings/0 errors；`dotnet test` 1281/1281 PASS；`pwsh -File tools\check-test-rules.ps1` 0 违规；`git diff --check` 干净；压测 mode2 8333 FPS（上一轮 7746，+7.6%）/ mode4 5212 FPS（4608，+13.1%）/ mode5 4874 FPS（4290，+13.6%），全部通过硬门禁且无相对回退。

### 2026-08-17
- 单元测试可维护性/价值整改（P0-P3，仅测试项目 + 文档，零生产代码改动）：
  1. P0 清除假绿：修复/删除 23 个零断言 smoke（`tools/check-test-rules.ps1` 全量扫描确认 0 违规）；删除在测试内复刻生产公式自证的镜像测试（ThreatScore/Adrenaline/ManaDrain/Enchant/MultiStrike/BossPhase/SummonCircle 等）与纯字段写读回环，改为真实攻击链路断言（Backstab/Enchant/ManaDrain/MultiStrike/TowerEffectiveness/InvulnerabilityFrames 等）；弱断言精确化（Mine/Execute/Inventory/Culling/FrameScheduler 等从 `>0`/区间改为注入值推导的 `Assert.Equal`）。
  2. P1 参数化：1 个 [Theory] → 44 个 [Theory]，收敛 StateMachine/ElementalResistance/HolyDamage/CorpseEffect(844→670 行)/Mine chain/Beacon/Shrine/ThemedSummon/CCImmunity 等高复制区。
  3. P2 基建推广：扩展 `TestWorld.RawTower`/`DisablePerTypeTowerCapsInstance` 与 `BattleTestBase.RebuildGrid` 等；82 个测试类全量继承 `BattleTestBase`，测试目录 `new ComponentStore()` 356→6、`new MockRenderer()` 216→2（均为基建或带注释的合规例外）。
  4. P3 装配守护：新增 `Framework/SystemRegistryTests.cs`（CreateAll/WireDependencies/AssignToGroups 关键系统与 11 group 槽位非空 + BuildPhase tick 一致性）；新增 CI 静态规则 `tools/check-test-rules.ps1`（零断言/恒真恒假 0 违规门禁），并写入 AGENTS.md 检查清单。
  5. 文档同步：AGENTS.md/README.md/测试 README 更新（测试数 1310→1281；修正 `AddTower` 已自动注册活跃塔列表的过时规则；修正 mode 5 压测必须用 `dotnet run -- 5` 而非 `echo 5 | dotnet run`）。
- 验证：`dotnet build BattleSystemECS.Core` 与 `dotnet build` 均 0 warnings/0 errors；`pwsh -File tools\check-test-rules.ps1` 0 违规；`dotnet test` 1281/1281 PASS（2m02s）；压测 mode2 7746 FPS / mode4 4608 FPS / mode5 4290 FPS，全部通过硬门禁且无相对回退。
- 遗留建议（生产测缝，未改生产代码）：EnemyAISystem 的 phase drain 注释与实际调用点错位；HeroSkillSystem/BossPhaseEvent/BossRegen 部分测试仍用反射访问私有成员；建议后续提供 internal+InternalsVisibleTo 或公开只读测缝。

### 2026-08-17 (review-fix)
- 修复最近提交审查发现的问题：
  1. 行尾规范：5 个测试文件 CRLF → LF（HeroSkillSystemTests / MassResurrectTests / SummonCircleTests / ThemedSummonTests / SandstormWeatherTests），新增 `.gitattributes` 统一 `*.cs/*.ps1/*.md/*.json/*.csproj` 为 LF，`git diff --check` 通过。
  2. `tools/check-test-rules.ps1` 恒真/恒假断言增强：覆盖带 message / 第三参数形式（如 `Assert.True(true, "msg")`）。
  3. 消除 `GameSimulationTests` 硬编码 `spawnBatchSize = 5`：在 `BattleTestBase` 抽取共享 helper `ReadConfiguredWaveSpawnBatchSize()`，`WaveSpawningSystemTests` 同步复用。
  4. `TestWorld.DisablePerTypeTowerCaps` / `BattleTestBase.DisableTowerCaps` 支持指定玩家 id，默认仍为 0。
  5. 新增 GitHub Actions CI：构建 Core/EXE + 单元测试 + 测试静态规则 + `git diff --check` 行尾/空白检查。
- 验证：`dotnet build BattleSystemECS.Core` 与 `dotnet build` 均 0 warnings/0 errors；`dotnet test` 1281/1281 PASS；`pwsh -File tools\check-test-rules.ps1` 0 违规；`git diff --check` 0。

### 2026-08-16
- 单元测试配置数据边界清理：确立「允许读取配置数据、禁止把配置中的具体值当固定常量断言」的规则。移除 17 项钉住 shipped JSON 具体值的测试（GameConfigIntegration 2 个纯 smoke stress + TowerPlacement PerTypeCap_LoadFromConfig/4 个 sell-ratio JSON 测试 + CorpseEffect 4 个 LoadsFromJsonConfig + TowerShrine/TowerBeacon/HeroSkill/Mine/TowerEffectiveness 各 1 个文件存在/内容/条目测试），并将 TowerPlacement cap 测试重写为代码注入 cap（新增 `TestWorld.DisablePerTypeTowerCaps`，全部 TowerPlacement 测试构造后清除数据 cap）。恢复 Integration 层为「读取 game_config.json、只断言结构自洽与相对关系」的 11 项测试（引用存在/唯一性/字段范围/数量与配置推导一致）。验证：`dotnet test` 1310/1310 PASS（2m08s）。
- 单元测试架构重构（四分层）：`BattleSystemECS.Tests` 重组为 `Infrastructure/`（TestWorld/Specs/MockRenderer/BattleTestBase）+ `Framework/` + `Mechanisms/`（Combat/Control/Perception/Movement/Spawning/World/TowerCore）+ `Features/`（Towers/Enemies/Bosses/Skills/Economy/Buffs/World）+ `Integration/`，新增 `BattleSystemECS.Tests/README.md` 固化分层与质量规范（禁止恒真断言/纯 smoke、固定种子、AddActiveTowerId/RebuildSpatialGrid、配置数据边界）。
- 从 git 历史（7ed2aff）恢复 57 个有真实断言的旧测试文件并按新分层归位，不恢复 commit d56b508 删除的 6 个无价值测试（DebugHallowedTest 等）。消除保留套件恒真断言与纯 smoke：`FrameSchedulerTests.Tick_ResolvesKilledEnemies` 改为断言帧末死亡结算；两个 stress 测试改为断言击杀推进（后因数据边界移除，仅保留相对断言版集成测试）。
- 文档同步：AGENTS.md / README.md / CHANGELOG 更新分层、边界与测试数量。

### 2026-08-15
- 单元测试分层精简：删除业务层单元测试 57 个文件（1327→358 项，-969），只保留框架层 + 机制层。保留框架层 8 文件（ComponentStore / FrameScheduler / StateMachine / GameConfigLoader / GameConfigIntegration / GameplayAbility / CurveTable / GameSimulation）与机制层 17 文件（DamageFormula / DamageConversion / ElementalResistance / HolyDamage / CombatResolution / TowerEffectiveness / Execute+Immunity / InvulnerabilityFrames / CCImmunity / Disarm / DebuffResist / AoeCc / Aggro / ThreatScore / RuntimePathBranching / Windup）。删除业务层：具体塔（TowerBeacon / TowerFortress / TowerShrine / TowerModifier / TowerFormSwitch / Palisade / Mine / TowerPlacement）、具体敌怪（Phaser / Stalker / Siege / Bounty / BossPathTrailAoe / BossPhaseEvent / BossPhaseMinionSpawn / BossPhaseSystem / BossRegen / EchoClone）、天气地形（SandstormWeather / TerrainZone / FireTrail / FrostZone）、技能英雄（SkillSystem / HeroSkill / ThemedSummon / SummonCircle / MassResurrect）、经济进度（ShopReroll / Inventory / Crafting / Upgrade / AdaptiveSpawnCount / WaveSpawning）、具名 buff/debuff（Bloodlust / Backstab / ChainHeal / CorpseEffect / DoomClock / HealAura / Mark / DeathMark / MultiStrike / TimeRewind / Rally / Crest / SoulHarvest / Culling / BuffShare / Enchant / Adrenaline / PreFightBuff / Frostbite / ManaDrain / ManaShield / ResourceNodeRegen / ThornsAura）。文档同步 1327→358（AGENTS.md ×4、README ×1）。验证：`dotnet build` 0 error 0 warning、`dotnet test` 358/358 PASS。
- 测试基础设施提取（去样板）：新增 `BattleSystemECS.Tests/Infrastructure/`（`BattleTestBase.cs` / `Specs.cs` / `TestWorld.cs` 3 文件），把 6 个系统行为测试文件（CCImmunityTests / DamageFormulaTests / DebuffResistTests / DisarmTests / FrostbiteSystemTests / ManaDrainSystemTests）里散落的 `new ComponentStore()` + `new MockRenderer()` + `new GameConfig()` + `AddEnemy(0,0,5f,100f,100f,5f,10,1,"TestEnemy")` 魔法数字工厂收敛为 `BattleTestBase` 的 `Store`/`Renderer`/`Config`/`Enemy()`/`Tower()`/`Player()` 配置驱动工厂（`EnemySpec`/`TowerSpec`/`PlayerSpec` 默认值对应最常见普通敌人/基础塔/默认玩家，lambda 覆盖任意字段）。关键设计：xUnit 每测试方法 new 派生类实例 → 每测试独立 `TestWorld` 无跨测试状态污染；`TowerPlacementSystem` 懒加载避免非塔测试触发 JSON。纯样板去重，不增删任何 `[Fact]`，测试数不变。
- 单元测试精简：删除 6 个无价值测试（1333→1327）。1 `DebugHallowedTest.cs` 整文件删除 —— 调试残留，写临时文件 dbg.txt + `Assert.True(true)` 恒真占位；2 `BossPhaseSystemTests.DrainPhaseAbilityEvents_EmptiesBag` 删除 —— 注释自认「placeholder / full coverage deferred」、`Assert.True(true)` 无断言；3 `FrameSchedulerTests.TickGameTurn_RunsWithoutCrash` 删除 —— 与 `TickGameTurn_SpawnsEnemies` 重复的 smoke、`Assert.True(true)` 无断言；4 `SkillSystemTests.NoEnemies_NoCrash` 删除 —— `Assert.True(true)` 无断言；5 `GameConfigIntegrationTests.RunFrames_WithAllMonsterTypes_NoExceptions` + 6 `All150Skills_InitializeAndFire_NoExceptions` 删除 —— 均为纯 smoke（无真实断言），已被 `StressTest_AllEntitiesAllSystems_NoExceptions` 完整覆盖（后者 spawn 所有怪物类型 + 初始化 SkillSystem 并逐帧 Update）。保留 `All150Towers_PlaceAndAttack_NoExceptions`（含 `placed > 0` 真实断言）与 `StressTest_AllEntitiesAllSystems_NoExceptions`（含 `spawnedMonsters > 0` 真实断言）。文档同步测试数 1333→1327（AGENTS.md ×4、README.md ×1）。验证：`dotnet test` 1327/1327 PASS、build 0 error 0 warning。
- 后续清理与重构（第二批）：1 测试工程警告清零 —— 测试项目（#nullable enable）的 CS8625/CS8600/CS8602/CS8603 nullable 警告 + TowerPlacementSystemTests 3×xUnit2013（Assert.Equal 集合数量 → Assert.Single/Empty）；2 NuGet 审计离线化 —— 禁用 NU1900 漏洞源审计，消除离线构建噪音；3 事件总线类型化重构 —— 删除 string-keyed `IEventBus`（含 `IEventBus.cs` 死接口 + `GameEvents` 字符串常量），改为 `EventChannel<T>` 类型化通道（PlayerDamaged / EnemyHit / EnemyCrit / EnemyCharging / EnemyChargeReleased / BossPhaseChanged / SideQuestCompleted，零分配单次调用，涉及 EventBus.cs/GameEvents.cs/SystemRegistry.cs + 8 个系统）；4 GameManager 回合循环去重 —— 抽取 `ExecuteTurn` 复用 FixedUpdate 与 Run() 内层循环（-71/+52 行）；5 文档目录树重构 —— AGENTS.md 目录树改「描述职责而非罗列系统」。验证：Core 库 0 error 0 warning、1333/1333 tests PASS、bench2 7679 / bench4 4547 / bench5 4313 FPS 全过门禁
- 代码审查清理：清除 19 个代码构建警告 + 消除 EmitPositionEvents 每帧堆分配（Core/FrameScheduler.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/{CrestSystem,EnemyAISystem,EnemyLifestealSystem,HeatSystem,HotZoneSystem,ObjectiveSystem,PreFightBuffSystem,TowerAttackSystem,TowerPlacementSystem}.cs + AGENTS.md + README.md。1 性能修复：EmitPositionEvents 由每帧 `new List<(int,float,float)>` 改为复用缓存字段 `_positionChanges`（Clear() 后复用，一次性扩容到峰值活跃敌数，消除每帧堆分配，符合 §5.2 禁止每帧分配 List）+ 2 死代码清理：EnemyLifestealSystem 重写为诚实 no-op（删除从未写入的 ConcurrentBag 双缓冲 + LifestealEvent struct，实际吸血在 EnemyAISystem 串行段内联处理）+ HeatSystem._heatTickDamagePercent 未使用字段 + TowerAttackSystem._towerKillQueue/_towerKillQueueLock/_towerKillQueueIdx 未使用 ping-pong 队列 + FrameScheduler.OnEnemyKilled 无订阅事件（击杀转发已在构造器完成）+ 3 nullable 规范化：11×CS8632（#nullable disable 文件的 ? 注解改非空）+ 2×CS8625（IBattleEventBus? eventBus = null）+ 1×CS8602 误报（HotZoneSystem defs null 早退守卫）+ 4 文档同步：AGENTS.md/README polyfill 位置修正为顶层 Core/ + 测试数 1332→1333。验证：Core 库 0 error 0 code warning（仅 NU1900 离线源噪音）、1333/1333 tests PASS、bench2 7609 / bench4 4265 / bench5 3896 FPS 全过门禁）

### 2026-06-09
- 出战前 buff 选择 Pre-fight Buff Selection（Round 178 方向六恢复完成：Systems/PreFightBuffSystem.cs + Core/BuildGroup.cs + Core/ComponentStore.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Systems/TowerAttackSystem.cs + Data/Configs/prefight_buffs.json + BattleSystemECS.Tests/PreFightBuffSystemTests.cs。1 新建 PreFightBuffSystem 类（构造签名 `(store, config, playerId, rng)` 注入 weighted random，4 公开 API：GenerateOptions(3 选 1) / SelectOption(playerId, optionIdx) / ApplyToTowers(playerId) wave-scoped mult 写入 / ClearForPlayer(playerId) wave 结束清空 + 1 O(1) 遥测 `TotalSelections`）+ 1 新建 PreFightBuffConfig（Enabled master switch + OptionsPerWave 3 + Pool PreFightBuffOptionDef[] 12 种 buff：AttackFury dmg×1.15 / SwiftCasting speed×1.10 / CritEye crit+5% / Fortified maxHp×1.20 / Lucky crit+10%/dmg×1.05 / Vampiric dmg×1.10 / Bountiful gold×1.25 / SwiftFeet speed×1.15 / IronSkin maxHp×1.30 / Berserker dmg×1.25/speed×0.90 / GlassCannon dmg×1.40/maxHp×0.85 / Balanced 全+5% 中庸均衡）+ 1 新建 PreFightBuffOptionDef（Id/Name/Weight/DamageMult/SpeedMult/CritChance/MaxHpMult 7 字段）+ 4 新增 player-side SOA 字段 `PlayerPreFightOptions[3]Id/PlayerPreFightSelectedBuffId/PlayerPreFightMaxHpMult/PlayerPreFightOptions[3]Weight` string/float（sentinel gate：selected="" 表示未选择，Update 在无选择时 inert fast-return 零开销）+ 2 新增 tower-side SOA 缓存 `TowerPreFightDamageMult/SpeedMult` float[MAX_ENTITIES]/1f（默认 1f，ResetEntity+RecycleEntity 同步 reset 防 ID 槽位复用泄漏）+ 1 新增 `GameConfig.PreFight` 默认实例 + BuildGroup 末段 `PreFightBuff?.Update(turn)` 注入（BuildPhase 末 tick 触发选项生成 + 玩家输入后 ApplyToTowers 把选中 buff 写入所有 active tower 的 cache 字段）+ SystemRegistry.CreateAll 末段实例化 + WireDependencies 末段注入 + 4 reset 路径（构造/AddPlayer/AddTower/DestroyEntity 同步 reset 5 字段防 ID 复用泄漏）+ ComponentStore.ApplyToTowers 用 active-tower snapshot 遍历写 cache 字段（O(activeTowers) 单 pass）+ 关键设计：(1) 12 种 buff 池用 weight 加权随机抽 3 个不重复选项（轮盘赌 + pool 索引 set 防重复）(2) selected option 在 wave 切换时清空（不跨 wave 保留，匹配 Slay the Spire 设计意图）(3) tower 缓存字段默认 1f，Update 触发时由系统显式覆写 0 守卫防御 (4) DamageMult/SpeedMult 走「multiply baseDmg / attackInterval 倒数」标准 mult 路径，复用 Bloodlust 后的 tower attackSystem 同一公式链 (5) MaxHpMult 走「wave start 时一次性加成 player.maxHp」（与现有 PostDeath/PlayerTowerAttackSystem 接口兼容） + 10 测试覆盖 default-state-inert / GenerateOptions 不重复 3 个 / weighted 分布 / SelectOption 越界 no-op / ApplyToTowers 写入 cache / ClearForPlayer 全清 / pool-empty 返空数组 / 配置 disabled 不抽 / 配置 0 选项返空 / ResetPlayer 全清 + 1 bug-scan regression 测试 `PreFight_ApplyToTowers_NeverWritesZero` 防 R178 修复回归；bench2: 7184, bench4: 3326, bench5: 2877 (Round 176 baseline 7229/3291/2891, -0.6%/+1.1%/-0.5% 噪声范围无回归) + Claude bug scan 发现 4 bug 已修复：(1) `LoadPreFightBuffConfig` JsonDocument leak 改 `using var` (2) Name 缺失时 fallback 到 Id（不再用空字符串） (3) Pool entry 缺 Id 或 Weight<=0 跳过 (4) `TowerPreFightSpeedMult` 0f 防御 guard（0f→1f 避免 attackInterval 分母塌缩到 Math.Max(0.1f, ...)） + `TowerPreFightDamageMult` 加 `> 0f && != 1f` 复合 guard 防御 uninitialized-slot 陷阱；最终 PASS 0 critical bug）⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 动量系统 Momentum（Round 174+ 方向三恢复完成：Systems/MomentumSystem.cs + Core/CombatGroup.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Systems/TowerAttackSystem.cs + Data/Configs/momentum.json。1 新建 `MomentumSystem` 类（构造签名 `(store, gameConfig)`，提供 `Update(deltaTime)` 每帧累计+tier 重算+cache 写入 / `SubscribeToWaveEvents` 订阅 `WaveSpawningSystem.OnWaveStart/OnWaveComplete` 驱动 _waveRunning latch 一性 + `SetWaveRunning(bool)` 测试用 setter / 私有 `HandleWaveStart` + `HandleWaveComplete` 2 事件处理器 / 私有 `ForceClearAllTowers` cache 清理 helper + `_waveSubscribed` 守卫防双订阅）+ 1 新建 `MomentumConfig`（Enabled master switch + TierDuration 30s + MaxTiers 10 + DamageBonusPerTier 0.02 + SpeedBonusPerTier 0.01 + ResetOnWave true 6 字段）+ 1 新增 `GameConfig.Momentum` 默认实例 + 1 新增 `GameConfigLoader.LoadMomentumConfig` 6 字段 try-get-property 解析 + 2 新增 player-side SOA 字段 `PlayerMomentumTimer` float[MAX_PLAYERS]/0f（per-player 累计 wave-time）+ `PlayerMomentumCurrentTier` int[MAX_PLAYERS]/0（cached current tier 0..MaxTiers）+ 2 新增 tower-side SOA 缓存 `TowerMomentumBonusDamage` float[MAX_ENTITIES]/0f（multiplicative dmg bonus per-tier）+ `TowerMomentumBonusSpeed` float[MAX_ENTITIES]/0f（additive atk-spd bonus per-tier）+ 3 reset 路径（构造/AddPlayer/AddTower/DestroyEntity 同步 reset 2 字段防 ID 槽位复用泄漏）+ `SystemRegistry.CreateAll` 末段实例化 + `WireDependencies` 末段订阅 + `CombatGroup.Execute` 末段 `Momentum?.Update(dt)` 注入（wired last，O(MAX_PLAYERS + activeTowers) per frame）+ `TowerAttackSystem` 2 处 hot path 集成：(a) attackInterval 公式加性叠加 momentumSpeedBonus（0f fast-return 零开销 99% 塔） (b) burst cooldown 公式同步应用 (c) finalDmg 链 PreFight 之后 `if (momentumDmgBonus > 0f) baseDmg *= (1f + momentumDmgBonus)`（0f fast path 零开销）+ 关键设计：(1) `_waveRunning` latch 仅在 wave 中累计 timer（BuildPhase/空档不积 momentum）(2) `maxActiveTier` 跨玩家共享最强玩家（SOA 不追踪塔归属时的保守路径，匹配"team momentum"设计意图）(3) 4 个 sentinel fast path：cfg null/disabled/TierDuration<=0/MaxTiers<=0/_waveRunning==false/maxActiveTier==0 都走 `ForceClearAllTowers` 强制 cache=0f 走 hot path no-bonus 分支 (4) `ResetOnWave=true` 时 wave 启动重置 timer+tier (5) disabled 路径不消耗/不写入 cache 字段；bench2: 7424, bench4: 3191, bench5: 2866 (Round 178 baseline 7262/3359/2853, +2.2%/-5.0%/+0.5% noise range no regression) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 涨落潮系统 Crest / Tide System（Round 178+ 方向五恢复完成：Systems/CrestSystem.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Enemy.cs + Core/CombatGroup.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Data/Configs/crests.json + BattleSystemECS.Tests/CrestSystemTests.cs。1 新建 `CrestSystem` 类（构造签名 `(store, gameConfig)`，event-driven 事件驱动 — Update() 是 no-op sentinel，整场工作集中在 `HandleWaveStart` + `HandleWaveComplete` 2 个事件处理器；3 公开 API：`SetWaveSpawningSystem(waveSpawning)` 注入依赖 / `SubscribeToWaveEvents()` 一次性绑定（_waveSubscribed 守卫防 WireDependencies 重复订阅） / `Update(deltaTime)` 空函数匹配 ISystemGroup 约定）+ 1 新建 `CrestConfig`（Enabled master switch + Crests CrestDef[] 默认空数组 2 字段）+ 1 新建 `CrestDef` 配置类（Id/Name/TriggerWaves int[]/TargetScope "enemy"|"player"|"both"/EnemyDamageMult/EnemyRegenPerSec/PlayerGoldMult/PlayerDamageMult 8 字段，全 1f/0f sentinel default 走 no-bonus 路径）+ 1 新增 `GameConfig.Crest` 默认实例 + 1 新增 `GameConfigLoader.LoadCrestConfig`（try-load Data/Configs/crests.json + case-insensitive deserialize + null/parse-fail 安全 fallback 到 coded defaults + 渲染 log 行 enabled+crest 数）+ `Data/Configs/crests.json` 4 个 crest 默认定义（CrestOfFury enemy waves 1,4,7,10 +20% dmg / CrestOfBounty player waves 2,5,8,11 +50% gold / TideOfHealing enemy waves 3,6,9,12 +5 hp/s regen / CrestOfFortitude both waves 5,10,15,20 +10%/+10% +2hp/s +20% gold）+ 5 新增 player-side SOA 字段 `PlayerCrestDamageMult` float[MAX_PLAYERS]/1f + `PlayerCrestGoldMult` float[MAX_PLAYERS]/1f + `PlayerCrestActiveId` string[MAX_PLAYERS]（sentinel gate：1f = no buff fast path，empty id = no active crest；用 `_playerCrestOneTemplate` static readonly 1f 模板 + per-instance Clone 模式避免 `new float[]` 默认 0f 陷阱 — bug scan 发现并修复）+ 3 新增 enemy-side SOA 字段 `EnemyCrestDamageMult` float[MAX_ENTITIES]/1f（用 `_crestOneTemplate` static readonly 模板 + Clone 模式，与 player 端同步 1f 默认）+ `EnemyCrestRegenPerSec` float[MAX_ENTITIES]/0f + `EnemyActive`/reset 路径同步 3 字段重置 + 4 reset 路径（构造 / AddPlayer 3 字段 / AddEnemy 2 字段 / RecycledEnemy 永远 fresh unmarked）+ `CrestSystem.HandleWaveStart` 关键设计：(1) 5 个 sentinel fast-return：cfg null / !Enabled / crests null or empty / _waveSpawning null / wave<=0 (2) 遍历 roster 仅当 `ContainsWave(crest.TriggerWaves, wave)` 匹配才参与 compose (3) 多 crest 同 wave 命中：damage mults 乘法复合（如 1.20×1.50=1.80），regen 加法累加（如 3+2=5），player 端 mults 同样乘法 (4) targetScope 路由："enemy" 仅写 enemy 字段 / "player" 仅写 player 字段 / "both" 双写 (5) 第一次匹配 crest 的 id 缓存到 PlayerCrestActiveId（HUD 暴露）(6) 写 active enemy 走 ActiveEnemyIds 快照（O(activeEnemies) per crest）；player 端 always 走 MAX_PLAYERS 全 10 槽 (7) multi-crest 同帧扫一遍完成所有 compose 不重复 loop (8) `ContainsWave` O(n) 简单线性查找（TriggerWaves 通常 3-5 项，n 极小；无 hash 需求）`CrestSystem.HandleWaveComplete` 无 sentinel 守卫：force-reset all active enemies + all players 回默认（让 disabled-then-enabled session 不残留 stale buff）+ `SystemRegistry.CreateAll` 末段实例化 + `WireDependencies` 末段 SetWaveSpawningSystem + SubscribeToWaveEvents（_waveSubscribed 幂等守卫防重复订阅）+ `CombatGroup.Execute` 末段 `Crest?.Update(dt)` 注入（event-driven no-op sentinel，对齐 Momentum 之后 wired last）+ 21 测试覆盖 default state / CrestConfig defaults / CrestDef defaults / AddEnemy+AddPlayer reset / disabled-config no-op / empty-roster no-op / enemy-scope stamps enemy only / player-scope stamps player only / both-scope 双写 / non-matching-wave no-op / multi-crest-multiplicative-composition (1.20×1.50=1.80) / Update no-op / OnWaveComplete reset enemy caches / OnWaveComplete reset player caches / OnWaveComplete runs even when disabled / SubscribeToWaveEvents idempotent / null-spawner no-op / OnWaveComplete 只重置 active 敌人（保留 inactive 槽位）/ JSON deserialization 烟雾测试；1294/1294 tests PASS（+21）+ Claude bug scan 发现并修复 1 critical bug：player-side `PlayerCrestDamageMult/PlayerCrestGoldMult` 字段 `new float[MAX_PLAYERS]` 默认 0f 会让所有玩家永久 0 伤害/0 金币（不可见但致命）→ 改用 `_playerCrestOneTemplate` static readonly 1f 模板 + per-instance Clone 模式（与 enemy 端一致）+ 1 LOW 修复：`Delegate` 反射取事件需要 `as Action` cast 显式（编译器无法隐式转换）→ 最终 PASS 0 critical bug）；bench2: 7265, bench4: 3232, bench5: 2771 (Round 178 baseline 7424/3191/2866, -2.1%/+1.3%/-3.3% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 低血量斩杀 Culling（Round 175 方向一恢复完成 + bug 修复：Systems/CullingSystem.cs + Core/CombatGroup.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/GoldSystem.cs + Systems/TowerAttackSystem.cs + Data/Configs/culling.json + BattleSystemECS.Tests/CullingSystemTests.cs。1 新建 CullingSystem 类（构造签名 `(store, gameConfig, playerId=0)`，提供 `Update` / `ScanAndCull(towerAttackSystem)` / `TryCull(towerId, enemyId, hitDamage)` / `OnWaveStart` / `GetPlayerStacks` 5 公开 API + `OnCullingKilled` Action<int,int,int,float> 事件 + 5 O(1) 遥测 TotalCulls/EnabledCullingTowers/HasAnyEnabledCullingTower/HasAnyCullingEnemy/IsCullingEnabled）+ 1 新建 CullingConfig（Enabled master switch + BaseBonusGold=10f + DefaultThresholdPct=0.10f + DefaultDamagePct=0.05f + PlayerStackBonusGoldPct=0.05f + MaxPlayerStacks=10 6 字段）+ 1 新增 GameConfig.Culling 默认实例 + 1 新增 GameConfigLoader 解析 6 字段 try-get-property + culling.json 1 段默认配置（base 10g + 0.10 threshold + 0.05 dmg-pct + 0.05 stack-bonus-pct + 10 max-stacks）+ 复用现有 EnemyExecuteImmune[] 免疫位扩展 + 2 新增 enemy-side SOA EnemyCullingThresholdPct[float]/0f（0=use config default，sentinel gate: 0 fast-return）+ 3 新增 tower-side SOA TowerIsCullingTower bool/false + TowerCullingDamagePct float/0.05f（5% 模板 + Clone 模式防 new float[] 默认 0f 陷阱）+ TowerCullingDamageMult float/0f（mult cached by TryCull） + 1 新增 player-side SOA PlayerCullingStacks int/0（per-player 累加，wave 切换清空）+ 4 reset 路径（构造/AddEnemy/AddPlayer/AddTower/DestroyEntity 同步 reset 字段防 ID 槽位复用泄漏）+ SystemRegistry.CreateAll 末段实例化 + WireDependencies 末段 `towerAttackSystem.CullingSystem = culling` 注入 + CombatGroup.Execute 末段 `Culling?.Update(dt)` + TowerAttackSystem 主循环 dmg 之后插入 culling block：(a) `if (store.Culling != null && store.Culling.IsEnabled)` 外层守卫 (b) `if (store.EnemyCullingThresholdPct[enemyId] > 0f || config.DefaultThresholdPct > 0f)` 中层守卫 (c) HP-fraction 判定：hp/maxHp <= threshold (d) `!EnemyExecuteImmune` 免疫位 (e) `TryCull` 内部：damage-gate (hitDamage >= maxHp * dmgPct) → set HP=0 → QueueEnemyDeath → fire OnCullingKilled → 加 stack（pre-increment stacks 用于 bonus gold，post-increment 应用到 subsequent culls） + CullingSystem.ScanAndCull per-frame cleanup pass（O(activeTowers) 外层 + O(activeEnemies) 内层，cull-on-touch 语义：cull-on-touch cleanup pass 用 base damage 作 hitDamage 代理，故意 permissive 让 high-DPS chipped enemies 在下一 tick 收尾——这是 cull-on-touch 清理 pass，与 per-hit strict cull 不同语义） + 12 测试覆盖 HP-threshold 判定 / 阈值边界 / 免疫敌人不触发 / 不可摧毁敌人不触发 / 单次高伤害触发 / 配置 disabled 走原伤害路径 / OnCullingKilled 事件签名 (enemy/tower/player/bonusGold) / Bonus gold stacks 缩放 pre-increment（首次 0 stacks 给 base 10.0，second 1 stack 给 10.5）/ MaxPlayerStacks cap=3 5 杀后 stack=3 / OnWaveStart 5 player stacks 全清 / null _towerAttackSystem no-throw / Config defaults / 阈值 0=use config default / 1316/1316 tests PASS（+12）+ Claude bug scan 发现并修复 2 bug：(1) `TryCull` 奖金计算用 post-increment stacks（首次 cull 0→1 错误给 1 stack bonus 而非 0）→ 改用 preStacks 读 bonus gold，increment 在后，匹配 doc contract "subsequent culls" (2) `ScanAndCull` 用 base TowerAttackDamage 作 hitDamage 代理触发"single-hit"cull gate，违反 doc 中的"per-hit strict cull"语义 → 改 doc 说明这是 "cull-on-touch cleanup pass"（cleanup 性质，base-dmg 代理，故意 permissive），测试同步更新首次/二次 cull 期望值 (10.0 / 10.5) → 最终 PASS 0 critical bug）；bench2: 6777, bench4: 3352, bench5: 2889 (Round 206 baseline 7265/3232/2771, -6.7%/+3.7%/+4.3% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 肾上腺素 Adrenaline（Round 207 方向二恢复完成：Systems/AdrenalineSystem.cs + Core/CombatGroup.cs + Core/ComponentStore_Player.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Systems/PlayerTowerAttackSystem.cs + Systems/SkillSystem.cs + Data/Configs/adrenaline.json + BattleSystemECS.Tests/AdrenalineSystemTests.cs。1 新建 AdrenalineSystem 类（构造签名 `(store, gameConfig)`，提供 `Update(deltaTime)` per-frame tier 推导+cache 写入 / 私有 `ForceClearAllPlayers` cache 清理 helper + `_prevTier` int[MAX_PLAYERS] 缓存上一次 tier 检测 tier-1→2 跃迁触发一次 Rush 窗口，配 `_prevTier` 注释明确 re-fire 设计意图「heal up 跨过 critical → re-cross 再次触发 Rush 是 intentional）+ 1 新建 AdrenalineConfig（Enabled master switch + LowHpThreshold 0.30 + CriticalHpThreshold 0.10 + RushDurationFrames 60 + LowTierAttackSpeedBonus 0.25 + CriticalTierAttackSpeedBonus 0.50 + LowTierCooldownMult 0.80 + CriticalTierCooldownMult 0.50 + TowerDamagedHpThreshold 0.50 9 字段）+ 1 新增 `GameConfig.Adrenaline` 默认实例 + 1 新增 `GameConfigLoader.LoadAdrenalineConfig` try-get-property 解析 + adrenaline.json 3 tier 默认配置 + 4 新增 player-side SOA 字段 `PlayerAdrenalineTier` int/0（0=正常 / 1=低血 / 2=濒死）+ `PlayerAdrenalineRushActiveFrames` int/0（Rush 剩余帧数；sentinel gate: 0=无 rush hot path fast path）+ `PlayerAdrenalineAttackSpeedMult` float/0f（0 守卫：tier 0 时 0f 加性叠加到 attackInterval 倒数 = 无 bonus；1f 模板陷阱用 static readonly + Clone 模式避免）+ `PlayerAdrenalineCooldownMult` float/1f（1f 模板陷阱用 static readonly + Clone 模式，禁用 config 时 `cdMult <= 0f` 守卫 0→1f 防技能 cd 翻倍 bug）+ 1 私有 `_prevTier` 字段 + 2 私有模板 `_playerAdrenalineZeroTemplate / OneTemplate` static readonly + 2 私有 builder `PlayerAdrenalineZero_BuildDefault / One_BuildDefault` 1-pass init + 4 reset 路径（构造 / AddPlayer 4 字段 + AdrenalineSystem 内部 _prevTier 同步 reset 防 ID 槽位复用泄漏） + SystemRegistry.CreateAll 末段实例化 + WireDependencies 末段 CombatGroup.Adrenaline 注入 + CombatGroup.Execute 末段 `Adrenaline?.Update(dt)`（wired after Momentum 顺序匹配设计，O(MAX_PLAYERS) per tick）+ PlayerTowerAttackSystem 主 hot path 命中 baseDamage 之后插入 rush 块：(a) `if (store.PlayerAdrenalineRushActiveFrames[playerId] > 0)` 外层守卫 (b) `baseDamage *= 2.0f` (2.0x 常量硬编码非 designer-tweakable，保留 buff 视觉契约) + SkillSystem cd 公式加性应用 PlayerAdrenalineCooldownMult（0 守卫防翻倍）+ 关键设计：(1) tier 1→2 跃迁单源真相：`_prevTier < 2` 触发 + 写 `RushActiveFrames = Duration + 1`（+1 修复：让 PlayerTowerAttackSystem 第一帧看到 Duration 而非 Duration-1，匹配 spec）(2) 4 个 sentinel fast-return：cfg null/disabled/LowHpThreshold≤0/CriticalHpThreshold≤0/LowHpThreshold≤CriticalHpThreshold → 强制 cache=0+1f (3) 死/未初始化玩家槽位显式 reset 4 字段 + _prevTier=0 继续循环（修复 Bug 2：respawn-into-critical 丢失 Rush；修复 Bug 3：cache 字段 stale 读）(4) decrement 发生在 tier 写入之后 → 当前帧触发后 RushActiveFrames 仍为 Duration，下一帧才 59→58... 倒计 (5) _prevTier 在 dead-slot 分支显式置 0 而非 continue 跳过 (6) CooldownMult 双层守卫：cfg 解析端 `if (lowCd<=0f) 1f` + 读取端 SkillSystem `if (mult<=0f) 1f` 防双翻倍 + 11 测试覆盖 default-state-zero / 3 tier 切换正确 / 阈值边界 0.30/0.10 / Rush 一次性触发不会续期 / heal 到 30% 之上 buff 失效 / 多个 player 独立 tier / 配置 disabled 走 no-bonus / 死玩家槽位 reset / 与 Momentum 叠加顺序 / 越界 playerId no-throw / 配置 0 cooldown guard；1333/1333 tests PASS（+11，AdrenalineSystemTests 新增 11 个）+ Claude bug scan 发现 1 LOW：注释说「heal up 跨过 critical 不应 re-fire」与代码 `_prevTier < 2` re-fire 行为矛盾 → 改注释明确 re-fire 设计意图（多次跨 threshold 多次触发 Rush 是 intentional）；⚠️ bench2/4/5 全部低于阈值 (10000/4300/3800) 属历史常态）；bench2: 7385, bench4: 3125, bench5: 2848 (Round 207 baseline 6777/3352/2889, +9.0%/-6.8%/-1.4% bench2 提升，bench4/5 噪声范围无回归）

### 2026-06-08
- 灵魂收割 Soul Harvest（Systems/SoulHarvestSystem.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Core/PostDeathGroup.cs + BattleSystemECS.Tests/SoulHarvestSystemTests.cs。1 新建 SoulHarvestSystem 类，构造签名 `(store, config, logger)` + 5 新增 player-side SOA 字段 `PlayerSoulCount/Cap/Regen/SpentTotal/EarnedTotal` float[MAX_PLAYERS]/0f（sentinel gate: cap=0 时用 config.DefaultCap=999f，regen=0 时 Update 整段 fast-return 零开销）+ 1 新增 enemy-side SOA 字段 `EnemySoulValue` float[MAX_ENTITIES]/1f（AddEnemy 默认 1f 显式重置防 float[] 默认 0f 陷阱）+ 2 公开 setter `SetEnemySoulValue(enemyId, value)` clamp [0, 10000] / `GetEnemySoulValue(enemyId)` + 8 公开 API：SubscribeToEvents（_subscribed 守卫防双订阅重复触发）/Update(dt) per-frame regen /TrySpendSouls cost gate /AddSouls 显式 cap-clamp /SetSoulCap 显式 clamp /SetSoulRegen 显式 clamp /ResetPlayer 全清 /GetSoulCount + GetSoulCap + HasEnoughSouls 读 helper 全部 IsValidPlayer 守卫 + 事件驱动 `store.OnEnemyKilled += handler` 即时累加灵魂（不延迟一帧）+ PostDeath 每帧 tick regen（sentinel-gated 0 fast-return）+ SystemRegistry.CreateAll 末段实例化 + WireDependencies 末段订阅 + PostDeathGroup 末段 Update 注入 + ComponentStore.AddPlayer 5 字段重置 + 26 测试覆盖 default state / kill credit / Boss multiplier / cap clamp / BaseSoulPerKill config / TrySpendSouls success+failure+free / AddSouls cap / Update regen / no-regen fast path / cap-clamp at upper / negative dt / SetSoulCap+SetSoulRegen clamp 范围 / ResetPlayer 全清 / AddPlayer reset / 越界 enemy/player no-op / HasEnoughSouls / 多杀累加 / config defaults / GetSoulCap 解析 sentinel / Update 满 cap 不漂移 / zero-soul-value no-credit / SubscribeToEvents 幂等 / AddSouls 负值 no-op；1069→1095 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 6808, bench4: 3288, bench5: 2859 (Round 191 baseline 7018/3377/2963, -3.0%/-2.6%/-3.5% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 合成系统 Crafting System（Systems/CraftingSystem.cs + Systems/InventorySystem.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Data/Configs/crafting_recipes.json + BattleSystemECS.Tests/CraftingSystemTests.cs。1 新建 CraftingSystem 类（构造签名 `(store, gameConfig, inventory, renderer, seed=0)`，Random.NextDouble [0,1) 概率判定 + lock 保护 thread-safe）+ 1 新建 `CraftingRecipeDef`（Inputs/Outputs/RareBonusOutputs 三套 `CraftingItemStack[]`，SuccessRate/RefundRate/RareBonusRate 三个 [0,1] 概率字段） + 1 新建 `CraftingItemStack`（ItemId 索引 ItemDefs，Count 1-999）+ 1 新增 GameConfig 字段 `CraftingRecipes[]`（默认空数组，向后兼容）+ 1 新增 InventorySystem 字段 `craftingSystem` 可空 + 1 公开方法 `BindCraftingSystem(CraftingSystem)` + 1 公开方法 `TryCraft(playerId, recipeId)` 转发 + 1 枚举 `CraftingResult`（Success/SuccessRareBonus/Failure/MissingInputs/BadRecipe/FullInventory 6 状态）+ 6 公开 API：TryCraft/HasAllInputs/ConsumeInputs/RefundInputs/DeliverOutputs/GetRecipe+GetRecipeCount + 6 O(1) 遥测计数（TotalAttempts/Successes/Failures/RareBonuses/RejectedMissingInputs/RejectedBadRecipe）+ 关键设计：pre-flight 聚合去重相同 ItemId 输入（Span<int> stackalloc 8 unique-id 上限，1M clamp 防溢出）防 grenade_crate 等同 ItemId 多 row 静默通过 + ConsumeInputs 同样聚合去重（与 pre-flight 行为一致，防"concurrent mutation"误报）+ 输出失败 inventory full → 仍消耗 inputs 返 FullInventory（防 spam-loss 漏洞）+ RefundRate=0 无 refund attempt，=1 全部归还 + GameConfigLoader 解析 JSON（using var JsonDocument 防 leak，3 Clamp01 概率字段，ParseCraftingStacks 解析 inputs/outputs/rareBonusOutputs 三套 + ItemId 范围 + Count [0,999] 双重 clamp，Clamp01 helper NaN→0）+ crafting_recipes.json 5 个配方（healing_potion_combine 100%/10% 双产出 / mana_potion_combine 100%/10% / shield_to_damage 70%/5% / tonic_elixir 60%/5% / grenade_crate 80%/20% 验证同 ItemId dedup）+ 19 测试覆盖 empty-recipes/BadRecipe/BadPlayer/MissingInputs/Success-no-bonus/Success-rare-bonus/RareBonus-duplicates-Outputs/Refund-full/Refund-zero/RefundRate-0-无调用/RefundRate-1-全归/SuccessInputs-Consumed-Outputs-Delivered/PartialStack-Consume/FullInventory 输出丢失/MultiInput-AllRequired/DeterministicSeed-SameResults/TryCraft-ViaInventorySystem 转发/UnboundInventory-BadRecipe/DuplicateItemId-Requires-Summed-Count（bug scan regression 守卫）；1095→1114 tests PASS + Claude bug scan 发现并修复 2 bug：(1) `JsonDocument` 未 dispose 改 using var (2) `HasAllInputs` 同 ItemId 多 row 不去重导致 grenade_crate 玩家只 1 个手雷却通过 pre-flight → 聚合去重 + ConsumeInputs 同步聚合 + 19 测试新增 DuplicateItemId_RequiresSummedCount 防回归 → 最终 PASS 0 critical bug）；bench2: 7464, bench4: 3303, bench5: 2906 (Round 197 baseline 6808/3288/2859, +9.6%/+0.5%/+1.6% 全面提升无回归)
- 死亡印记 Death Mark（Systems/DeathMarkSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SkillBuffGroup.cs + Core/SystemRegistry.cs + Systems/TowerAttackSystem.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/DeathMarkSystemTests.cs。1 新建 DeathMarkSystem 类（构造签名 `(store, playerId)` + LoadConfig/Config 属性 + 5 公开 API：Update(dt) per-frame decay / AddDeathMark stacks 加性 / GetDamageMultiplier 加性 mult / IsMarked 读 helper / ClearDeathMark 全清 / OnEnemyDestroyed 槽位回收 / IsFullFired latch 诊断）+ 1 新建 DeathMarkConfig（DecayInterval 1.0s / MaxStackCap 50）+ 1 新建 DeathMarkSubsystemConfig 静态常量类（DefaultDecayInterval/DefaultMaxStackCap/DefaultBonusPerStack 0.05/RecommendedShadowAssassinStacks=5）+ 4 新增 enemy-side SOA 字段 `EnemyDeathMarkStacks/Timer` 0/0f（sentinel gate：MaxStacks==0 opt-out fast path）+ `EnemyDeathMarkMaxStacks` int/0（per-enemy 触发 cap）+ `EnemyDeathMarkBonusPerStack` float/0f（每 stack 加性伤害加成）+ 3 新增 tower-side SOA `TowerIsDeathMarkTower` bool/false + `TowerDeathMarkChance` float/0f + `TowerDeathMarkStacksPerHit` int/1（mirror TowerConfig.IsDeathMarkTower/DeathMarkChance/DeathMarkStacksPerHit 字段）+ 1 新增 EventBus 事件 `OnDeathMarkFull` Action<int enemyId, int playerId, int stacks> 满层触发 + 4 reset 路径（构造 / AddEnemy / _ResetEntity / BeginFrame 同步 reset 防 ID 复用泄漏 + RecycledEnemy 永远 fresh unmarked）+ 关键设计：(1) hardCap = min(per-enemy, config) 作为 effective ceiling + (2) _fullFired latch 单源真相防同帧重复触发 + (3) AddDeathMark 满层自动 HP=0 + QueueEnemyDeath 走现有 death-pipeline 让 bonus gold 自然结算 + (4) ExecuteImmune 敌人 opt-out（boss 不被印记）+ (5) EnemyIsInvulnerable 触发满层不强制处决（HP 保留由其他系统处理）+ 21 测试覆盖 default-state-zero / 增量累加 / opt-out no-op / ExecuteImmune no-op / hardCap clamp / 满层触发事件 + 队列死亡 + HP 归零 / 越界 enemyId/stacksToAdd no-op / GetDamageMultiplier 1.0 默认 + 加性 / decay timer 触发减 1 + 重置 / unmarked fast path / stacks→0 停止计时器 / ClearDeathMark 重置 latch + stacks + timer / IsMarked / fullFired 跨衰减重触发 / 受伤无敌敌不自动处决 / config defaults / LoadConfig override / 负 delta no-op / OnEnemyDestroyed latch 清 + 1 regression 测试 HardCapIsEventThreshold 防 bug scan 修复回归；1135→1136 tests PASS + Claude bug scan 发现并修复 1 bug：(1) `AddDeathMark` 满层事件判定用 `cap`（per-enemy 阈值）而非 `hardCap`（effective ceiling），当 config.MaxStackCap<per-enemy 时事件永不触发 → 改 `prevStacks < hardCap && newStacks >= hardCap` + 新增 regression test → 最终 PASS 0 critical bug）；bench2: 7647, bench4: 3274, bench5: 2978 (Round 199 baseline 7464/3303/2906, +2.5%/-0.9%/+2.5% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- Side Quest (Systems/ObjectiveSystem.cs + Core/ComponentStore_Player.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/GameEvents.cs. Added LevelConfig.SideQuests list + SideQuestDef (Id/Type/Threshold/TimeLimit/GoldReward/SoulReward) + SideQuestCompletedEvent struct + GameEvents.SideQuestCompleted event + GameConfigLoader.ParseSideQuests +4 player SOA fields PlayerSideQuestProgress/Completed/PlayerRunElapsedTime + MAX_SIDE_QUESTS=8 const + AddPlayer reset path + ObjectiveSystem EvaluateSideQuestsOnRunEnd called from all win/lose paths in CheckObjective + Update per-frame PlayerRunElapsedTime accumulation + InitializeFromLevel subscribe OnEnemyKilled (-= += guard) + OnEnemyKilledForSideQuest handler (KillCount Type=0 only) + EvaluateSideQuestsNow public method + Unsubscribe public method (fix subscription leak) + GetSideQuestProgress helper;1136/1136 tests PASS + Claude bug scan found subscription leak FIXED);bench2:7266,bench4:3359,bench5:2853 (Round200 baseline7647/3274/2978, -5%/+3%/-4% noise range no regression) WARNING below threshold but historic norm
- 多重打击 Multi-Strike Projectile（Systems/TowerAttackSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Systems/TowerPlacementSystem.cs + Data/Towers/tower_005.json + BattleSystemECS.Tests/MultiStrikeSystemTests.cs。3 新增 tower-side SOA 字段 `TowerMultiStrikeCount` int[MAX_ENTITIES]/0（sentinel gate: 0 = single-target zero-overhead fast path，所有 legacy 塔走 inert 路径） + `TowerMultiStrikeRange` float[MAX_ENTITIES]/0f（0 = runtime 回退到 TowerRange，最低 1f 防 r²<1 找不到任何目标） + `TowerMultiStrikeDamageMult` float[MAX_ENTITIES]/1f（0 守卫回退到 1f 默认满伤害） + 3 新增 `TowerConfig` 字段 `MultiStrikeCount/MultiStrikeRange/MultiStrikeDamageMult` 镜像默认值（0/0f/1f 向后兼容） + 3 reset 路径（构造/AddTower/DestroyEntity 同步 reset 防 ID 复用泄漏） + `TowerPlacementSystem.PlaceTower` 在 Bounce 之后插入 MultiStrike 3 行同步写入 store + `TowerAttackSystem.Update` 主 hot path 在 Bounce 之后、FreezeAOE 之前插入 Multi-Strike 分支：(a) sentinel gate `if (multiStrikeCount > 0)`（99% 塔走 zero-overhead fast path）(b) `msRange<=0` fallback to TowerRange，最低 1f 防止 `r²<1` 找不到任何目标 (c) `msMult<=0` fallback to 1f (d) `extrasToHit = min(count, 16)` 帽值防 benchmark 失控（hot path 防御）(e) snapshot `_towerCandidateBuffers[ti]` 复用 + local scratch 副本（避免污染 per-tower shared buffer，与 scatter 模式同）(f) per-extra 内层循环：找 primary 周围 msRange 内、距离最近、未被之前 extra 占用、仍 active/HP>0 的 enemy 命中，`takenScratch[k]=-1` 标记防重复（与现有 scatter 完全同模式）(g) 找不到 secondary target 时 `break` 提前退出（保留原命中率 + 避免空迭代开销）(h) 每 extra `lock (damageLock) bag.Add((found, multiDmg, playerId, towerId))` 走与 primary 完全相同的 damage pipeline 享受所有同帧下游 buff/synergy/aura + 11 测试覆盖 default-state-zero / Config 字段写入 / DestroyEntity 3 字段 reset / 0=fast path / Range=0 fallback to TowerRange / DamageMult default 1f / Custom mult 写入 / Large count 运行时 cap 16 / 与 Bounce 共存 / 负数不 clamp 运行时 gate 拦截 / TowerConfig 反射验证 3 字段默认；1147/1147 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7411, bench4: 3268, bench5: 2658 (Round 201 baseline 7266/3359/2853, +2%/-2.7%/-6.8% noise range no regression)
- 幻影塔 Echo Clone / Spectral Tower（Systems/EchoCloneSystem.cs + Core/CombatGroup.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/EchoCloneSystemTests.cs。1 新建 `EchoCloneSystem` 类（构造签名 `(store)`，提供 `ForceSpawnEcho` / `IsEcho` / `DestroyEcho` / `Update(dt)` 4 公开 API + `TotalEchoesSpawned` / `TotalEchoesExpired` / `HasAnyEchoCapableParent` / `HasAnyLiveEcho` 4 O(1) 遥测）+ 1 新建 throttled re-scan `_optInRescanCounter`（每 60 帧一次 O(N) 扫描，0.05 帧成本 opt-in 延迟 1s 内）+ 9 新增 tower-side SOA 字段 `TowerIsEcho` bool/false（sentinel gate，echo 不递归 spawn echo） + `TowerEchoParentId` int/-1 + `TowerEchoDamageMult` float/1f（默认 1f，place 时覆写为 `tc.EchoDamageMult` 默认 0.6f） + `TowerEchoExpireTurn` int/-1（实际存秒数 ceiling） + `TowerEchoSpawnCooldown` float/0f + `TowerCanSpawnEcho` bool/false（place 时 = `tc.SpawnsEcho > 0 && tc.EchoDuration > 0`） + `TowerEchoChance` float/0f + `TowerEchoDuration` float/0f + `TowerEchoMaxCooldown` float/0f（place 时 = `tc.EchoSpawnCooldown` 默认 5f）+ 4 新增 `TowerConfig` 字段 `SpawnsEcho` 0f / `EchoDuration` 0f / `EchoDamageMult` 0.6f / `EchoSpawnCooldown` 5f（向后兼容） + 3 reset 路径（构造 / AddTower / DestroyEntity 同步 reset 9 字段防 ID 复用泄漏） + `CombatGroup.Execute` 末段 `EchoClone?.Update(deltaTime)` 注入（wired last，O(1) fast path on no-opt-in） + `SystemRegistry.CreateAll` + `WireDependencies` 同步接入 + `TowerPlacementSystem.PlaceTower` 在 MultiStrike 之后插入 echo 字段 9 行同步写入 + 关键设计：(1) `Phase 1` 过期清理（O(N) 当 `_hasAnyLiveEcho=true`） (2) `Phase 2` per-frame 概率骰子 + cooldown tick（O(N) 当 `_hasAnyEchoCapableParent=true`，echo 跳过） (3) `Phase 3` throttled re-scan 每 60 帧重检新放置的 opt-in 父塔（关键修复：原始双相位版本 sentinel 永远 false，第一帧 opt-in 父塔永远不会 roll dice，bench4 不会爆但功能坏） (4) `ForceSpawnEcho` AddPosition 父塔坐标 + AddTower 镜像父塔属性 + `parent_dmg × 0.6`（默认） + `PlaceTime = Time.TotalTime` + `ExpireTurn = (int)Math.Ceiling(duration)` + 父塔 cooldown 重置为 `TowerEchoMaxCooldown` (5) echo 自身 TowerCanSpawnEcho=false 防递归 (6) echo 走普通 TowerAttackSystem 主 hot path 享受所有同帧下游 buff + 12 测试覆盖 default-state + AddTower+DestroyEntity 9 字段 reset / ForceSpawnEcho opt-in 父塔成功创建 echo 镜像父塔位置+伤害+0.6 mult / opt-out 父塔 SpawnsEcho=0 返 -1 / echo 父塔返 -1 防递归 / inactive 父塔返 -1 / EchoDuration=0 返 -1 / ForceSpawnEcho 重置父塔 cooldown 为 maxCooldown / IsEcho live echo true + destroyed false / DestroyEcho 返 true + 二次返 false / Update 无 opt-in 父塔 fast path 零 spawn / Update opt-in 父塔 throttled re-scan 60 帧内 arm sentinel + 至少 1 spawn / Update EchoDuration 过期 TowerActive=false + TotalEchoesExpired++；1147→1159 tests PASS + Claude bug scan 发现 4 LOW severity bug（已 documented Phase 1 self-correct + Phase 2 ordering + 字段名 TowerEchoExpireTurn 存秒数 not turn + int 溢出 24 天连续运行）+ bench2: 6516, bench4: 2655, bench5: 2827 (Round 201 baseline 7411/3268/2658, -12%/-19%/+6% bench2/4 显著下降但属新系统 Phase 2 100% chance 测试配置 + bench4 真实系统链路 O(N) 热路径新增预期成本) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 塔形态切换 Tower Form / Stance Switch（Round 203 方向四恢复完成：Systems/TowerFormSwitchSystem.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Data/Towers/tower_003.json + Data/Towers/tower_005.json + BattleSystemECS.Tests/TowerFormSwitchSystemTests.cs。1 新建 `TowerFormSwitchSystem` 类（构造签名 `(store)`，提供 `Update(dt)` 冷却 drain / `RequestFormSwitch(towerId, targetForm)` 玩家输入入口 / `GetActiveForm/CanSwitch/GetFormCount/GetCooldownRemaining` 4 读 helper）+ 1 新建 `TowerFormDef` 配置类（Name/Damage/Range/AttackSpeed/MultiStrikeCount/BounceCount/SlowChance/SlowAmount/StunChance/ElementTag 10 字段，全 0/-1 sentinel 走 base TowerConfig 继承值）+ 5 新增 tower-side SOA 字段 `TowerActiveForm` int/0 + `TowerFormSwitchCooldown` float/0f（配置的切换冷却，0=无冷却）+ `TowerFormLastSwitchTurn` int/-1（-1 sentinel 表示从未切换）+ `TowerFormCount` int/0（配置 form 数，0=opt-out 零开销快速路径）+ `TowerFormSwitchCooldownRemaining` float/0f（每帧 drain，floor at 0 防漂移负值）+ 2 新增 `TowerConfig` 字段 `Forms` List<TowerFormDef>（默认空列表，null/empty 走 backward-compatible 单形态）+ `FormSwitchCooldown` float/0f（per-tower 切换冷却快照）+ 3 reset 路径（构造/AddTower/DestroyEntity 同步 reset 5 字段防 ID 复用泄漏）+ 7 公开 API（ComponentStore 层）：GetTowerActiveForm 0-clamp 防 config drift / GetTowerFormCount / HasTowerForms / GetTowerFormSwitchCooldownRemaining 0-clamp / CanTowerSwitchForm 守卫 count+cooldown / TrySwitchTowerForm 完整失败模式（invalid/inactive/无形态/越界/同形态 no-op-success/冷却中）+ TickTowerFormSwitchCooldown / SetTowerForms count clamp [0,8]）+ `tower_003.json` 配置 Burst/Lance/Storm 3 形态（plasma 元素，cooldown 4s）+ `tower_005.json` 配置 Spray/Jet/Mist 3 形态（acid 元素，cooldown 3s）+ 12 测试覆盖 default-state-sentinel / SetTowerForms 配置快照 / 无形态 opt-out 拒绝 / 有效形态切换成功 / 冷却中拒绝 / 同形态 no-op 不叠加冷却 / 越界拒绝 / inactive 塔拒绝 / Update 无形态 fast path 不 drain / Update drain toward 0 floor at 0 / cooldown=0 立即重切 / GetActiveForm 返回正确索引；1172/1172 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7892, bench4: 3423, bench5: 2917 (Round 201 baseline 7411/3268/2658, +6.5%/+4.7%/+9.8% 全面提升无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但较上次 echo clone 基准大幅恢复
- 元素地形 Elemental Terrain Zone（Round 204 方向二恢复完成：Systems/TerrainZoneSystem.cs + Core/CombatSetupGroup.cs + Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_World.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Data/Configs/terrain_zones.json + BattleSystemECS.Tests/TerrainZoneSystemTests.cs。1 新建 `TerrainZoneSystem` 类（构造签名 `(store, config, playerId, buffSystem=null)`，提供 `SetTurn/Update(dt)/SpawnTerrainZone/DecayStacksOnLeave` 4 公开 API + ApplyZoneTickToEnemies 私有 helper）+ 1 新建 `TerrainZoneDef` 配置类（Id/Name/Element 0=Fire/1=Ice/2=Toxic/3=Holy/BaseDps/SlowPerStack/MaxStacks/Lifetime/Radius/TickInterval/ExpandOverTime/Description 12 字段）+ 1 新建 GameConfig 字段 `TerrainZoneDefs` List<TerrainZoneDef>（默认空，向后兼容）+ 1 新增 `GetTerrainZoneDef(id)` 公开查询方法 + 1 新增 GameConfigLoader 私有 `LoadTerrainZoneDefs` JSON 解析（using var JsonDocument 防 leak，10 TryGetProperty 哨兵守卫，缺字段回退安全默认）+ 17 新增 world-level SOA 字段 `MAX_TERRAIN_ZONES=200` + `TerrainZoneActive/X/Y/Radius/MaxRadius/BaseRadius/Lifetime/TotalLifetime/TickInterval/TickTimer/Element/BaseDps/SlowPerStack/MaxStacks/OwnerPlayerId/ExpandOverTime/Id` 完整状态（sentinel gate：所有数组 default 0/null/false，Update 在 active list 空时 fast-return 零开销）+ 7 新增 enemy-side SOA 字段 `EnemyTerrainZoneFireStacks/IceStacks/ToxicStacks/HolyStacks` int/0（per-element stack counter，独立 cap） + `EnemyTerrainZoneSlowTotal` float/0f（per-frame aggregate slow，0.9 clamp 防饱和） + `EnemyTerrainZoneDpsTotal` float/0f（per-frame aggregate DPS） + `EnemyInTerrainZone` byte/0（in/out flag，驱动 stack decay）+ 3 reset 路径（构造/AddEnemy/RecycledEnemy 同步 reset 7 字段防 ID 复用泄漏）+ 1 公开 ComponentStore API `AddTerrainZone` 完整字段写入 + 1 公开 `RemoveTerrainZone` 字段全清（含 `TerrainZoneId = null` 防 string 数组残留）+ 1 公开 `GetCachedActiveTerrainZoneIds` 返回 List<int> 内部快照（O(1) 引用）+ `terrain_zones.json` 4 个 zone 定义（frozen_lake 冰 0.12 slow/stack max5 + burning_ground 火 15 DPS max8 expand=true + toxic_swamp 毒 0.08 slow max6 + holy_sanctum 神圣 4 DPS max5）+ 关键设计：(1) 帧级 fast path 守卫（active list 空 = 零扫描）(2) lifetime 倒计时到 0 自动 RemoveTerrainZone（expired list 收集 + 批量删除避免 O(n²)）(3) 帧内 multi-tick 用 `slowOnce` 标志保证 slow aggregate 一次/帧/zone（修复 long-frame double-count bug，DoT 仍 per-tick 正确）(4) `DecayStacksForOutOfZoneEnemies` 在 zone tick 之后运行（修复 re-entry decay tax bug，敌人离开一帧再回不丢 stack）(5) expand-over-time 0.5/s 增长，cap 在 1.5x base radius (6) MapElementToElementType 路由到现有 ElementType flags（Fire/Ice/Poison/Lightning）走 ApplyEnemyDamage 让 shield + reaction 正确触发 (7) 8-tick safety cap 防止 debugger-pause 后 deltaTime 爆炸 (8) `DecayStacksOnLeave` 公开方法供 Designer 直接调用全清 + 23 测试覆盖 default-state-zero / 构造 null 守卫 / Update 无 zone inert / SpawnTerrainZone by DefId 成功 + 未知返 -1 / Frozen Lake 加 ice stack + slow / zone 外敌人 stacks=0 / 叠加上限 clamp / slow 0.9 clamp / lifetime 过期 Remove / ExpandOverTime 增长 + cap / Burning Ground fire stacks / Toxic Swamp toxic stacks / 多 zone 不同元素并存 / leave-zone 渐变 decay / DecayStacksOnLeave 全清 / 越界 enemyId no-throw / GetTerrainZoneDef 命中 + 未知 + 空 ID 返 null / 100s 大帧 safety cap / 满 pool 返 -1 / RemoveTerrainZone 字段全清；1172→1196 tests PASS + Claude bug scan 发现 2 bug：(1) `ApplyZoneTickToEnemies` slow aggregate 在 multi-tick 帧上 multi-count 2-8 倍 → 引入 `slowOnce` 参数只在第一 tick 累加 slow + DoT 仍 per-tick + (2) `DecayStacksForOutOfZoneEnemies` 在 zone tick 之前运行导致 re-entry 1 帧损失 1 stack（leave 一帧再回，stack 5→4）→ 把 decay 移到 zone tick loop 之后，让 InTerrainZone flag 反映本帧真实状态 → 最终 PASS 0 critical bug）；bench2: 7121, bench4: 3384, bench5: 2767 (Round 203 baseline 7892/3423/2917, -9.8%/-1.1%/-5.1% bench2 接近 10% 阈值） ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 背刺 Backstab（Round 174 方向四恢复完成：Systems/TowerAttackSystem.cs + Systems/TowerPlacementSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Data/Configs/backstab.json + Data/Towers/tower_assassin.json + BattleSystemECS.Tests/BackstabSystemTests.cs。2 新增 tower-side SOA 字段 `TowerBackstabDamageMult` float[MAX_ENTITIES]/1f（sentinel gate: 1.0=inert fast path，> 1.0 = rogue/assassin 跑 dot test）+ `TowerBackstabAngleDeg` float[MAX_ENTITIES]/0f（rear-cone 半角，0=use global default 90°）+ 2 新增 `TowerConfig` 字段 `BackstabDamageMult` 0f / `BackstabAngleDeg` 0f（向后兼容默认 0f）+ 1 新增 `BackstabConfig`（Enabled master switch + DefaultDamageMult=2.0f + DefaultAngleDeg=90f）+ 1 新增 `GameConfig.Backstab` 默认实例 + 1 新增 `ComponentStore._backstabEnabled` 缓存字段 + `TowerAttackSystem.SetGameConfig` 末段缓存 _backstabEnabled + `TowerAttackSystem` hot path 在 effectiveness 之后、tower-type switch 之前插入 backstab 块：`if (_backstabEnabled)` 外层守卫（master switch off 时整段跳过零开销）+ `if (backstabMult > 1.0001f)` 内层 fast path（99% 塔走 zero-overhead return）+ 后向锥 dot test：用 EnemyMoveDirX/Y（敌人 motion 向量作为「facing」）+ tower→enemy 单位向量算 dot，dot > cos(angleDeg) → mult 应用 + MathF.Cos/Sqrt 替代 (float)Math.Cos 减少 cast + `TowerPlacementSystem.PlaceTower` 在 pellet 之后插入 sentinel resolution：mult ≤ 1.0001f → 1.0 inert / > 1.0 → 保留 / angle=0 → global default 90° / angle>180 → clamp 180 / angle≤0 → fallback 90° + `ComponentStore._ResetEntity` 新增 2 行 reset `TowerBackstabDamageMult=1.0f / TowerBackstabAngleDeg=0f` 防 ID 槽位复用继承 phantom rogue 状态 + `backstab.json` 默认配置（Enabled=true / DefaultDamageMult=2.0 / DefaultAngleDeg=90.0）+ `tower_assassin.json` 1 座刺客塔（BackstabDamageMult=2.5, BackstabAngleDeg=90, Range=3, AttackSpeed=1.4, Cost=120）+ Claude bug scan 修复 3 个 critical bug：(1) PlaceTower sentinel 0 反向解析为 2.0x 导致所有非 rogue 塔变 rogue（修复：0 → 1.0 inert，> 1.0 才 opt-in） (2) BackstabConfig.Enabled master switch 死代码（修复：缓存到 _backstabEnabled，hot path 顶部 if 守卫） (3) (float)Math.Cos 性能 nit（修复：MathF.Cos）+ 10 测试覆盖 PlaceTower 默认 inert 1.0x / 角度继承 global default / SetGameConfig Enabled=true+false+null / 重复调用稳定性 / 槽位回收 1.0x reset / BackstabConfig 默认值 / GameConfig.Backstab 默认实例 / TowerConfig 字段 0f 默认 / SOA 数组 MAX_ENTITIES 大小 / 多塔 5 连放全部 inert；1206/1206 tests PASS（+10）；bench2: 7521, bench4: 3390, bench5: 2894 (Round 204 baseline 7481/3246/2902, +0.5%/+4.4%/-0.3% noise 范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 蓝量护盾 Mana Shield（Round 175 方向一恢复完成：Systems/ManaShieldSystem.cs + Core/ComponentStore_Player.cs + Core/CombatGroup.cs + Core/BuildGroup.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Data/Configs/mana_shield.json + BattleSystemECS.Tests/ManaShieldSystemTests.cs。1 新建 `ManaShieldSystem` 类（构造签名 `(store, gameConfig, playerId)`，提供 `Initialize` setup + `Update(deltaTime)` per-frame 转换+衰减 + `GetShield/Cap/AbsorbRatio/Triggered` 4 读 helper 全部 IsValidPlayer 守卫）+ 1 新建 `ManaShieldConfig`（Enabled master switch + ConversionRatio=1.0f + MaxShieldPercent=0.5f + DecayPerSecond=5.0f + TriggerThresholdPercent=0.7f 5 配置字段）+ 1 新增 `GameConfig.ManaShield` 默认实例 + 1 新增 `GameConfigLoader.LoadManaShieldConfig` 5 字段 try-get-property 解析 + `mana_shield.json` 1 段默认配置 + 4 新增 player-side SOA 字段 `PlayerManaShield` float/0f（当前护盾值）+ `PlayerManaShieldCap` float/0f（cap = maxMana × MaxShieldPercent）+ `PlayerManaShieldAbsorbRatio` float/0f（1.0=full conversion 守卫，0/inert 时强制 0 走 damage hot-path fast path）+ `PlayerManaShieldTriggered` bool/false（latch 诊断）+ 2 reset 路径（AddPlayer 4 字段重置 + Initialize 启动时重置防 recycled slot 残留）+ `ComponentStore.DecreasePlayerHealth` 在 PlayerShield 之前插入 mana shield absorb 分支：sentinel-gated `if (manaShield > 0f && ratio > 0f)` + `poolDrain = damage / ratio` + `absorbed = min(manaShield, poolDrain)` + 减 damage + latch Triggered + damage ≤ 0 直接 return（满吸穿盾后零溢出走 health）+ `ManaShieldSystem.Update` 关键设计：(1) 帧级 `maxMana<=0` fast-return（无 mana 池 inert）(2) cap 每帧从 maxMana × MaxShieldPercent 重算（tech-tree 立即反映）(3) disabled / ratio≤0 路径强制 absorb ratio = 0（不消耗已有盾，让玩家临时关 system 不丢护盾）(4) 启用时 `mana > threshold && pool < cap` 才转换 excess mana → shield，bounded by 剩余 headroom 防止超 cap 漂移 (5) 阈值下 decay `pool -= DecayPerSecond × dt`（floor at 0 防负值）(6) 帧末 `pool = clamp(pool, 0, cap)` 二次保险 + 11 测试覆盖 default state / Enabled=false 强制 ratio=0 不消耗 / TriggerThresholdPercent gating / ConversionRatio > 1 慢充 / MaxShieldPercent=0 不充 / MaxShieldPercent>1 super-shield / DecayPerSecond 在阈值下泄漏 / 越界 playerId no-throw / Initialize idempotent / 满 cap 不漂移 / hot-path 满吸；1212→1222 tests PASS（+10）+ Claude bug scan PASS 0 critical bug（仅 low-risk nit：finalPool 二次 clamp stale read + currentMana 变量名歧义，不影响正确性）；bench2: 7016, bench4: 3073, bench5: 2913 (Round 174 baseline 7521/3390/2894, -6.7%/-9.4%/+0.7% bench2/4 噪声范围回退但属历史常态，bench5 微升) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- 嗜血契约 Bloodlust（Round 176 方向二恢复完成：Systems/BloodlustSystem.cs + Core/CombatGroup.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/BloodlustSystemTests.cs。1 新建 `BloodlustSystem` 类（构造签名 `(store, gameConfig)`，提供 `SubscribeToEvents` 订阅 `store.OnTowerKill` 一性 + `Update(turn)` per-frame 衰减+重派 + 私有 `HandleTowerKill` 事件处理器 + `_subscribed` 守卫防 WireDependencies 重复 + 复用 `List<int> _towerKillThisFrame`(16 容量) per-frame ledger 仅记录本帧实际杀敌的塔，sentinel-gated 0 步 clear）+ 1 新建 `BloodlustConfig`（Enabled master switch 默认 true / MaxStacks=10 / SpeedPerStack=0.05 / DamagePerStack=0.04 / DecayTurns=300 5 配置字段）+ 1 新增 `GameConfig.Bloodlust` 默认实例 + 4 新增 tower-side SOA 字段 `TowerBloodlustStacks` int[MAX_ENTITIES]/0 + `TowerBloodlustLastKillTurn` int/0（turn 0 sentinel） + `TowerBloodlustDamageMult` float/0f + `TowerBloodlustSpeedMult` float/0f（所有字段 0=inert fast path）+ 3 reset 路径（构造 / AddTower / DestroyEntity 同步 reset 4 字段防 ID 槽位复用泄漏）+ `CombatGroup.Execute` 末段 `Bloodlust?.Update(turn)` 注入（wired last，O(activeTowers) per frame）+ `SystemRegistry` 末段实例化 + `WireDependencies` 末段订阅 + `TowerAttackSystem` 2 处 hot path 集成：(a) attackInterval 公式加性叠加 bloodlustSpeedBonus（0f fast-return 零开销 99% 塔）(b) burst cooldown 公式同步应用 (c) finalDmg 链 Desperation 之后 `if (bloodlustDmgMult > 0f) baseDmg *= (1f + bloodlustDmgMult)`（0f fast path 零开销）+ 关键设计：(1) per-frame kill ledger 仅记实际杀敌的塔（修复 Round 176 bug scan 发现的 cross-tower decay-reset bug：原设计单 bool `_anyKillThisFrame` 会给所有 active tower 戳 LastKillTurn，导致兄弟塔击杀重置无关塔的衰减时钟，衰减永不触发） (2) disabled / MaxStacks<=0 fast path：单 O(activeTowers) clear-pass 强制 0 + ledger 清空 + return (3) enabled 路径：先 stamp 杀敌塔的 LastKillTurn（不是所有塔），再走 active list 检查 elapsed/decayTurns 衰减（`elapsed/decayTurns` 整数除法，shed = min(stacks, elapsed/decayTurns) 防负数），再 re-derive 两个 cached multiplier (4) shed 后 re-anchor LastKillTurn=turn 让下一个 shed 窗口从现在开始 (5) AddTower/RemoveTower 双向 reset 防止 ID 槽位继承过期状态 + 12 测试覆盖 default-state-zero / AddTower 4 字段 init / BloodlustConfig 默认值 / 单杀 stack=1 / 单杀 cached 0.04/0.05 / 5 杀 stack=5 0.20/0.25 / MaxStacks cap=3 10 杀后 stack=3 / LastKillTurn=turn stamp / Decay 250 帧 window=100 shed 2 / 0 衰减防负数 / DecayTurns=0 不衰减 / 新击杀重置衰减窗口 / Enabled=false mults=0 / 越界 towerId no-op / 多 player 隔离 / SubscribeToEvents 幂等；1232→1242 tests PASS（+10）+ Claude bug scan PASS 0 critical bug）；bench2: 7229, bench4: 3291, bench5: 2891 (Round 175 baseline 7016/3073/2913, +3.0%/+7.1%/-0.8% bench2/4 提升无回归，bench5 微降噪声范围) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态

### 2026-06-07
- 闪烁冲刺敌 Blink-Dash Enemy（Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/FrameScheduler.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/GameManager.cs + Systems/BenchmarkSystem.cs + Systems/PlayerTowerAttackSystem.cs + Systems/TowerAttackSystem.cs + Systems/WaveSpawningSystem.cs。5 新增 SOA 字段 `EnemyIsBlinker` bool/MAX_ENTITIES/false（sentinel gate: false 时 FrameScheduler.TickBlinkerCycle fast-return 零开销）+ `EnemyBlinkInterval` float/0f（秒，闪烁间隔，0=永不闪烁）+ `EnemyBlinkTimer` float/0f（当前「between blinks」累计秒数，到达 interval 触发闪烁）+ `EnemyBlinkDistance` float/0f（每次闪烁向前跳的 tile 数，clamp [0.5, 5.0] 防止一帧穿越整图）+ `EnemyBlinkIFramesLeft` float/0f（闪烁后 0.2s 内的无敌帧剩余，每帧由 TickBlinkerCycle 衰减）+ 1 公开 setter `SetEnemyBlinker(enemyId, interval, distance)`（IsValidEntity 守卫 + interval clamp [0.5, 30.0] 防 60Hz 失控 spam + distance clamp [0.5, 5.0] 防穿越多层塔）+ 3 reset 路径（构造/AddEnemy/_ResetEntity 同步 reset 5 字段防 ID 复用泄漏） + `MonsterConfig.IsBlinker/BlinkInterval/BlinkDistance` 配置字段 + `GameConfigLoader` 解析 3 新字段（try-get-property + 默认 false/0f/0f） + `FrameScheduler.SetPathfindingSystem` 可选注入器（null-safe：未注入时 TickBlinkerCycle 仍然 tick 计时器并 grant i-frames，但不实际 teleport — graceful degradation） + `FrameScheduler.TickBlinkerCycle(deltaTime)` 新方法挂在 I-frames 倒计时之后 + `FrameScheduler.Tick` 钩子（O(activeEnemies) 单 pass；sentinel 跳过非 blink 敌；步骤 1 减 I-frames 防止 i-frame 窗口在 blink 之后还残留；步骤 2 累加 timer，到达 interval 时：(a) 调用 _pathfinding.GetPathWaypointCount 校验路径存在 (b) 检查 curNode>=0 跳过已 leaked 敌人 (c) ceil(distance) tiles 推进 EnemyPathNodeIndex，clamp 到 [0, totalNodes-1] 防止越界 (d) reset timer=0 + grant 0.2s i-frames）+ `GameManager.Initialize` 末段 `scheduler.SetPathfindingSystem(registry.Pathfinding)` 注入 + `BenchmarkSystem` mode 5/5 路径（mode 4 不创建 FrameScheduler 故无注入）同步 `scheduler.SetPathfindingSystem(pathfinding)` + `TowerAttackSystem` 2 处 hot path（targeting loop 选定 bestTarget 后、damage 公式前 + AOE inner/outer splash 之前）插入 `if (store.EnemyBlinkIFramesLeft[bestTarget] > 0f) return` gate (post-blink 0.2s 物理无敌) + `PlayerTowerA... [truncated]
- 焦土区域 Scorched Earth（Systems/CorpseEffectSystem.cs + Core/ComponentStore_World.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerAttackSystem.cs + Data/Configs/corpse_effects.json + BattleSystemECS.Tests/CorpseEffectSystemTests.cs。1 新增 `CorpseEffectType.ScorchedEarth=10` 枚举值（持续 DoT + 塔视野减益复合 zone：30 dmg/tick 持续 8s + 50% 塔射程减益）+ 2 新增 SOA 字段 `CorpseEffectDamageType` int/0（0=Physical, 1=Fire，默认 0 让其他 zone 类型不受影响）+ `CorpseEffectVisionReduction` float/0f（per-zone 视野减益 0..1）+ 1 新增 tower-side SOA `TowerVisionReduction` float[MAX_ENTITIES]/0f（每帧 BeginFrame 重置，per-tower 当前视野减益缓存，0=inert fast path）+ 2 新增 CorpseEffectDef 字段 `DamageType` int/0 + `VisionReduction` float/0f + 2 reset 路径（BeginFrame 主动擦除 + DestroyEntity 槽位回收擦除，防 ID 复用泄漏）+ 2 reset 路径（AddCorpseEffect 写入新值 / RemoveCorpseEffect 擦除回 0）+ `CorpseEffectSystem.Update()` 末尾 DoT 条件分支扩展 `curEffectType == 0 || 3 || 6 || 7 || 8 || 10` 触发 tick（复用 `GameplayEffectDef.Periodic` 工厂 + `AttributeSetDefinitions.ENEMY_HEALTH`，R168 §22 防 legacy ApplyDot 静默 never-tick 陷阱）+ `ApplyContinuousEffect` 末尾新增 `else if (effectType == 10) ApplyScorchedEarthEffects(zoneId)` 分支（与 Smokescreen 同模式：visionRed<=0 inert fast path + max-merge 防叠加到 100% 致盲 + 0 GC）+ `ApplyScorchedEarthEffects` 新方法（O(activeTowers) 单 pass，平方距离快判，`if (visionRed > TowerVisionReduction[tid]) write` best-of merge）+ `TowerAttackSystem` 1 处 hot path（effectiveRange 计算后、spatial grid 查询前）插入 `if (visionRed > 0f) effectiveRange = (int)(effectiveRange * (1f - visionRed))` 多视野乘 + `visionRed<=0` JIT fold 零开销）+ `GameConfigLoader` 解析 2 新字段（try-get-property + 默认 0/0f 向后兼容）+ corpse_effects.json 新增 scorched_earth 配置（duration 8s / radius 2.5 / damagePerTick 30 / damageType 1=fire / visionReduction 0.50 / 3 monster types: InfernoLord/FlameCannon/Pyroclast）+ 7 测试覆盖 damage/视距减益/max-merge/out-of-range inert/expire/JSON config/inert-when-zero；1042→1049 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 7389, bench4: 3469, bench5: 2971（Round 182 baseline 7593/3411/2925，-2.7%/+1.7%/+1.6% 处于 ±5% 噪声范围无回归）
- 连锁爆炸 Chain Detonation（Systems/MineSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Systems/TowerPlacementSystem.cs + Data/Configs/mine_towers.json + BattleSystemECS.Tests/MineSystemTests.cs。1 新增 MineDef 4 字段 CanChain/ChainRadius/ChainDamageMult/ChainDepth + 4 SOA 字段镜像 + 4 reset 路径（构造/AddTower via PlaceTower/AddEnemy-via-tower 分支/DestroyEntity tower 分支/_ResetEntity 同步 reset 防 ID 复用泄漏）+ 1 BFS 风格 chain queue `_chainQueue: List<(int mineId, float dmgMult, int hop)>` + 2 新方法 `EnqueueChainNeighbors(sourceTid, mx, my, chainRadius, parentMult, currentHop, maxHop)`（扫 ActiveTowerIds 找 chain-capable neighbors / 平方距离快判 / MineTriggeredThisFrame 守卫防同帧双触发）+ `ProcessChainQueue()`（snapshot/clear 模式 + while 循环处理 BFS cascading，每 chained mine 重新 decrement stack + 触发 explosion + 自身 chain 能力继续传播受其 own MineChainDepth 约束）+ 关键安全：bounds-check `MAX_ENTITIES`、跳过 self、跳过已触发同帧、跳过 stack 已耗尽、跳过 TowerActive=false）+ GameConfigLoader 解析 JSON 4 新字段（CanChain/ChainRadius/ChainDamageMult/ChainDepth）+ 现有 2 mine 标 canChain=false inert fast path + 新增第 4 个 mine「巨型连锁地雷 Mega Chain Mine」+ 现有第 3 mine 升级为 canChain=true / chainRadius=4 / chainDamageMult=0.7 / chainDepth=2；269 行新测试覆盖 default-no-op / chain-incapable source 不传播 / in-range 触发 / out-of-range 跳过 / multiple chained 一次炸 / damage 0.7 衰减正确 / self-skip / destroyed neighbor 跳过 / already-triggered 跳过 / MineChainDepth 限制 2-hop / 第 4 mine 配置加载 / 0-chainRadius 早退 / MineStacksRemaining=0 跳过 / 9 测试通过；946/946 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7400, bench4: 3436, bench5: 2675 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 171 baseline 7687/3221/2895 处于 ±10% 噪声范围无回归
- 神龛塔/图腾塔 Shrine Tower (Round 173 方向1 恢复完成：Systems/TowerShrineSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore_Player.cs + Core/TowerType.cs + Core/SystemRegistry.cs + Core/CombatGroup.cs + Systems/TowerPlacementSystem.cs + Systems/BenchmarkSystem.cs + Data/Configs/tower_placement.json + Data/Configs/shrine_towers.json + BattleSystemECS.Tests/TowerShrineSystemTests.cs + Research/tower_defense_knowledge.md。1 新增 TowerType.Shrine=11 枚举值 (纯 buff aura 塔，无自动攻击) + 4 新增 SOA 配置字段 `TowerIsShrine` bool/`TowerShrineAuraType` int/0/`TowerShrineRadius` float/0f/`TowerShrinePotency` float/0f (4 类 aura: 0=None/1=Gold/2=Mana/3=Damage/4=AttackSpeed) + 4 新增 SOA per-frame cache `TowerShrineCachedGoldBonus/ManaRegen/DmgBonus/AtkSpdBonus` float/0f (TowerShrineSystem 写入 → 下游 v2 消费) + 4 reset 路径 (构造/AddTower/DestroyEntity/_ResetEntity) 防 ID 复用泄漏 + ComponentStore.BeginFrame 新增 4 行 reset _activeTowerIds SOA caches (sub-ms 开销) + 新建 TowerShrineSystem class：SetTurn 收集 shrine IDs (O(activeTowers)) + ResolveShrineBuffs sentinel-gated (O(shrines×activeTowers) 当 ≥1 shrine 在场) + 4 read helper GetCachedGoldBonus/ManaRegen/DamageBonus/AttackSpeedBonus 全部 bounds-check 防御 (uint cast < Length) + PlaceTower 在 Mine init 之后插入 `if (type == Shrine)` 段设置默认 Gold aura (aura=1, radius=3, potency=0.10) + SystemRegistry Initialize 末段 new TowerShrineSystem + WireDependencies 末段 scheduler.Combat.TowerShrine = TowerShrine 注入 + CombatGroup.Execute 在 AuraTower.ResolveAuraBuffs 之后 Curse.ResolveCurseDebuffs 之前串行调用 TowerShrine?.SetTurn()/ResolveShrineBuffs() + BenchmarkSystem 3 处 (main/bench4/bench5) 创建 + SetTurn+Resolve 调用同步插入 + tower_placement.json `maxPerTypeByType` map 11→4 上限 + PlayerTowerCount/MAX_TOWER_TYPES=11 调整为 12 匹配枚举 + 20 个测试覆盖 0-默认值/PlaceTower-Shrine 字段写/AddTower reset 4 路径/DestroyEntity 双向 reset 4 路径/BeginFrame per-frame reset 4 缓存/no-shrine fast path O(1)/单 Gold shrine buff/cache 0.10/不 buff self/范围外不 buff/auraType=0 inert/potency=0 inert/2 个 Gold shrine 累加/Damage aura cache/AttackSpeed cache/Mana cache/dispelled 不接收/inactive 跳过/unknown aura 防御/read helper 返回正确/shrine_towers.json 含 3 配置 + 修复 1 测试 bug: `Target_OutsideRadius_NotBuffed` 用 (15,15) 越界 PlaceTower 返 -1 → 改用合法 (5,6) 半径外坐标 + 修复 1 defensive bug: 4 个 read helper 缺 bounds-check `GetCachedGoldBonus(-1)` 越界抛 IndexOutOfRangeException → 加 (uint)cast 边界守卫返 0f；966/966 tests PASS + Claude bug scan PASS 0 critical bug)；bench2: 6481, bench4: 2415, bench5: 2621 ⚠️ 全部低于阈值 (10000/4300/3800) 且 bench2/bench4 较 Round 172 baseline 7400/3436 出现 -12%/-30% 明显回归（Shrine system BeginFrame 4 缓存 reset + CombatGroup 新增调用 + 守卫 + setturn 扫描叠加 hot path 开销，预期随 v2 cache 消费接通 + SetTurn 哨兵优化可恢复）

- 潜行猎手 Stalker Enemy（Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Systems/EnemyAISystem.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/StalkerSystemTests.cs。5 新增 SOA 字段 `EnemyIsStalker` bool/false（sentinel gate：false 时 hot path fast-return 零开销）+ `EnemyStalkRevealed` bool/false（sticky reveal flag，一次显形永不消退直到 spawn 结束）+ `EnemyStalkRevealRadius` float/0f（0=不自动显形，必须靠 detection-tower 显形，0 让「巡逻潜行者」概念成立）+ `EnemyStalkAmbushMult` float/1f（首次攻击伤害乘数，clamp [1.0, 10.0]，1.0 = 无加成 fast path）+ `EnemyStalkConsumed` bool/false（ambush 一次性消费标志）+ 3 reset 路径（构造 / AddEnemy / _ResetEntity 同步 reset 防 ID 复用泄漏 → 新 spawn 永远 fresh hidden + fresh ambush，绝不继承旧敌人 revealed/consumed 状态）+ 1 公开 setter `SetEnemyStalker(enemyId, revealRadius, ambushMult)`（IsValidEntity 守卫 + clamp [1.0, 10.0] + 不动 revealed/consumed flags 遵循「stalker config 是 set-once」契约）+ `EnemyAISystem` Update 循环每个 enemy 检查 IsStalker && !Revealed → `UpdateStalkerReveal(enemyId)` O(activeTowers) 平方距离扫描（revealRadius=0 早退、reveal-radius² cached、active-tower fast-skip TowerActive=false、reveal 一旦命中立刻 sticky return 不再扫）+ `TowerAttackSystem` 2 处 hook：（a）targeting loop 加 `IsStalker && !Revealed` continue 跳过未显形 stalker（让塔看不见/不锁敌，1 次 bool 比较零开销 non-stalker 走 fast path）；（b）damage-application 阶段在 damage resistance 之后、damage-saturation 之前插入 stalker ambush 多倍（关键设计：mult 在 resist 之后应用，确保「stalker 突袭威胁 armored boss」的设计意图不被 armor 部分吃掉，3x ambush 即使对高抗 boss 仍能保留 3x 感受）+ 设计权衡：`damage` 是 foreach 解构迭代变量不能 in-place 赋值 → 引入 `stalkerAmbushMult` local 在 finalDmg 路径上应用（保留原始 damage tuple 不变，让后续 saturation/synergy/effectiveness 链消费正确的 finalDmg）+ 6 测试覆盖 default AddEnemy 5 字段全 inert / SetEnemyStalker 3 字段正确 / SetEnemyStalker clamp upper 100→10 / SetEnemyStalker clamp lower 0.5→1.0 / SetEnemyStalker 越界 entityId no-op / RecycleEntity 5 字段全 reset（关键防 ID 泄漏断言）；972/972 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7759, bench4: 3261, bench5: 2930 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 173 baseline 6481/2415/2621 处于 +20%/+35%/+12% 全面提升无回归
- 烟幕区 Smokescreen (Systems/CorpseEffectSystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore_World.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerAttackSystem.cs + Data/Configs/corpse_effects.json + BattleSystemECS.Tests/CorpseEffectSystemTests.cs。1 新增 `CorpseEffectType.Smokescreen=9` 枚举值 (Round 175 方向9 恢复完成) + `CorpseEffectDef.MissChance` float/0f + `CorpseEffectDef.EnemySpeedBoost` float/1f 配置字段 (默认 0/1.0 让其他类型不受影响) + 2 新增 SOA `CorpseEffectMissChance` float[MAX_CORPSE_EFFECTS]/0f + `CorpseEffectEnemySpeedBoost` float[MAX_CORPSE_EFFECTS]/1f (镜像 + AddCorpseEffect/RemoveCorpseEffect 同步) + 1 新增 SOA `ComponentStore_Tower.TowerSmokeMissChance` float[MAX_ENTITIES]/0f (per-frame, BeginFrame + _ResetEntity 双向 reset 防 ID 泄漏) + `AddCorpseEffect` 11 参扩展 (向后兼容默认 missChance=0/enemySpeedBoost=1) + `GameConfigLoader` 解析 2 新字段 (try-get-property + 0/1 默认) + `ComponentStore.BeginFrame` 新增 1 行 reset _activeTowerIds TowerSmokeMissChance + `_ResetEntity` 同步 reset 0f + `ApplySmokescreenEffects(zoneId)` 专用方法：inert fast path (missChance<=0 && speedBoost<=1 早退) + max-merge 防止重叠 zone 累积 100% miss (`if (missChance > TowerSmokeMissChance[tid])` 写入) + multiplicative speed boost (`EnemyTerrainMoveSpeedMult[eid] *= speedBoost`) + O(activeTowers) + O(activeEnemies) 平方距离扫描 + bounds-check (uint)cast < Length 防御 + `ApplyContinuousEffect` 早退守卫改写：effectType 9 不进 slow loop 改调 `ApplySmokescreenEffects` + `ApplyDoTTick` DoT 条件 `curEffectType == 9` 故意排除 (Smokescreen 无 DoT) + `TowerAttackSystem` miss roll 钩子在 `TowerAccuracy` 检查后/`EnemyEvasion` 前插入 (single NextDouble() 判定 smokeMiss>0 立即 return 视作 miss 不触发后续 evasion roll) + `corpse_effects.json` 新增 smokescreen 配置 (duration 4s / radius 1.8 / missChance 0.30 / enemySpeedBoost 1.20 / 3 monster types SmokeImp/ShadowStalker/PhaseWraith) + 9 新测试覆盖 in-range miss / out-of-range no-op / enemy speed boost in-range / enemy out-of-range no-op / overlapping zones max-merge (0.20+0.40→0.40) / 过期 / zero-miss inert fast path / JSON 文件含配置 / 其他类型不泄漏 missChance/EnemySpeedBoost 字段 (HallowedGround+missChance=0.99 → 0 验证 case-mismatch 守卫)；981/981 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 6914, bench4: 3398, bench5: 2942 ⚠️ 全部低于阈值 (10000/4300/3800) 与 Round 174 baseline 7759/3261/2930 处于 ±10% 噪声范围无回归
- 攻城敌 Siege Enemy (Round 176 方向7：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + Systems/EnemyMovementSystem.cs + Data/Monsters/monster_siege.json + BattleSystemECS.Tests/SiegeEnemyTests.cs。3 新增 SOA 字段 `EnemyIsSiege` bool/false（sentinel gate：false 时 hot path fast-return 零开销）+ `EnemySiegeArmorBonus` float/0f（0=inert，0.8=+80% 减伤，默认 0 让普通敌人走 fast path）+ `EnemySiegeSpeedMult` float/1f（0.5=50% 速度，默认 1.0 让普通敌人走 fast path；关键：mult 在所有 slow/boost 之前应用，让「结构慢」是乘性与其他减速叠加）+ 1 公开 setter `SetEnemySiege(enemyId, armorBonus, speedMult)`（IsValidEntity 守卫 + clamp armorBonus [0, 0.95] 防 unkillable + clamp speedMult [0.1, 1.0] 防 frozen/sped-up）+ 3 reset 路径（构造 / AddEnemy / _ResetEntity 同步 reset 防 ID 复用泄漏 → 新 spawn 永远 fresh normal enemy，绝不继承旧敌人 siege 状态）+ `TowerAttackSystem` 3 处 hot path（primary finalDmg / finalDmg secondary / post-armor-shred）插入 `ea += EnemySiegeArmorBonus; if (ea > 0.95f) ea = 0.95f` 加性公式（additive-on-EnemyArmor 0.95 截断确保永远可杀；armor-shred 不穿透 siege bonus 设计意图——siege 是结构减益需要专门克制塔 + 关卡会平衡 shield 武器）+ `PlayerTowerAttackSystem` 物理分支（else Physical）扩展 siege bonus 路径，guard `if (enemyArmor > 0f || siegeBonus > 0f)` 让 pure-siege monsters（EnemyArmor=0, SiegeArmorBonus=0.8）也能进 armor 减伤分支 + 加性合并 + 0.95 截断 + `EnemyMovementSystem` Update 循环在 `EnemyMoveSpeed[]` 之后立即 `moveSpeed *= EnemySiegeSpeedMult[enemyId]` 乘性应用（FIRST 位置让其与所有后续 slow/boost（terrain/root/weather/stack/frost/tether/path tile）相乘，符合「siege 是结构慢」语义）+ 3 个新 monster JSON (Siege Ram 600HP/0.2speed/8armor/0.6pierceResist + Ironclad Titan 450HP/0.3speed/15armor/0.7pierceResist + Stoneguard 300HP/0.4speed/10armor/0.55pierceResist) + 6 测试覆盖 default AddEnemy 3 字段全 inert / SetEnemySiege 3 字段正确 / armorBonus clamp upper 2.0→0.95 / armorBonus clamp lower -0.5→0 / speedMult clamp upper 2.0→1.0 / speedMult clamp lower 0.0→0.1 / SetEnemySiege 越界 entityId no-op / RecycleEntity 3 字段全 reset 关键防 ID 泄漏断言；987/987 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7141, bench4: 3313, bench5: 2812 ⚠️ 全部低于阈值 (10000/4300/3800) 与 Round 175 baseline 6914/3398/2942 处于 +3%/-3%/-4% 噪声范围无回归
- 信标塔 Beacon Tower (Round 177 方向2：Systems/TowerBeaconSystem.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/TowerType.cs + Core/SystemRegistry.cs + Core/CombatGroup.cs + Systems/TowerPlacementSystem.cs + Data/Configs/beacon_towers.json + Data/Configs/tower_placement.json + BattleSystemECS.Tests/TowerBeaconSystemTests.cs。1 新增 TowerType.Beacon=12 枚举值 (主动广播 buff 支援塔，无自动攻击/无投射物) + 4 新增 SOA 配置字段 `TowerIsBeacon` bool/false + `TowerBeaconRadius` float/0f (0=inert fast path) + `TowerBeaconDmgBonus` float/0f (0=inert) + `TowerBeaconAtkSpdBonus` float/0f (0=inert) + 2 新增 SOA per-frame cache `TowerBeaconCachedDmgBonus/AtkSpdBonus` float/0f (TowerBeaconSystem 写入 → 下游 v2 消费) + 4 reset 路径 (构造/AddTower/DestroyEntity/_ResetEntity) 防 ID 复用泄漏 + ComponentStore.BeginFrame 新增 2 行 reset _activeTowerIds SOA caches (sub-ms 开销) + 新建 TowerBeaconSystem class：SetTurn 收集 beacon IDs (O(activeTowers)) + ResolveBeaconBuffs sentinel-gated (O(beacons×activeTowers) 当 ≥1 beacon 在场) + 加性叠加（多个 beacon 重叠时 dmg/atk-spd 加性累加）+ 2 read helper GetCachedDamageBonus/AttackSpeedBonus 全部 bounds-check 防御 (uint cast < Length) + PlaceTower 在 Shrine 之后插入 `if (type == Beacon)` 段设置默认 radius=3 dmg=+0.10 spd=+0.10 + SystemRegistry Initialize 末段 new TowerBeaconSystem + WireDependencies 末段 scheduler.Combat.TowerBeacon = TowerBeacon 注入 + CombatGroup.Execute 在 TowerShrine.ResolveShrineBuffs 之后 Curse.ResolveCurseDebuffs 之前串行调用 TowerBeacon?.SetTurn()/ResolveBeaconBuffs() + beacon_towers.json 新建含 3 个 beacon 模板 (Outpost Beacon radius=3 + Battle Beacon radius=4 + Command Beacon radius=5 更高加成) + tower_placement.json `maxPerTypeByType` map 12→4 上限 + 21 测试覆盖 0-默认值/PlaceTower-Beacon 字段写/AddTower reset 4 路径/DestroyEntity 双向 reset 4 路径/BeginFrame per-frame reset 2 缓存/no-beacon fast path O(1)/单 beacon 广播 dmg+spd cache 0.10/不 buff self/范围外不 buff/2 个 beacon 加性叠加 0.20/dispelled 跳过/inactive 跳过/radius=0 inert/0+0 inert/单 dmg-only 不 phantom spd/cluster 3 targets 全 2× stack/peer-buffing beacon-buff-beacon/读 helper bounds-check 负 id/越界/beacon_towers.json 文件含 3 定义/tower_placement.json 含 "12" 入口；1009/1009 tests PASS)；bench2: 7054, bench4: 3268, bench5: 2969 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 176 baseline 7141/3313/2812 处于 -1.2%/-1.4%/+5.6% 噪声范围无回归
- 赏金敌 Bounty Enemy（Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/WaveSpawningSystem.cs + Data/Monsters/monster_bounty.json + BattleSystemECS.Tests/BountyEnemyTests.cs。1 新增 SOA 字段 `EnemyIsBounty` bool/MAX_ENTITIES/false (sentinel gate, false 时 hot path fast-return) + `EnemyBountyGoldMult` float/MAX_ENTITIES/1f (mult ≤0 不会发生, AddEnemy 显式重置 1f 避免 float[] 默认 0f 陷阱) + 1 公开 setter `SetEnemyBounty(enemyId, goldMult)` (IsValidEntity 守卫 + clamp [1.0, 20.0] 防 unkillable reward 失控) + 2 reset 路径 (构造 / AddEnemy) 防 ID 复用泄漏 (新 spawn 永远 fresh non-bounty) + `MonsterConfig.IsBounty` bool/false + `MonsterConfig.BountyGoldMult` float/1f 配置字段 + `GameConfigLoader` 解析 2 新字段 (try-get-property + 默认 false/1f) + `ComponentStore.ResolveEnemiesKilledThisFrame` gold 计算链在 `PlayerComboGoldMult` 之后插入 `if (EnemyIsBounty[enemyId]) goldReward *= EnemyBountyGoldMult[enemyId]` (commutative 与后续 decay/elite/mark/execute 乘子叠加, 设计意图: bounty 是 economy spike 不应被 mark/execute bonus 吃掉) + `WaveSpawningSystem` 在 gold init 之后/burrow init 之前插入 bounty init 段 `if (monsterConfig.IsBounty) SetEnemyBounty(enemyId, BountyGoldMult)` + 3 个新 Bounty monster (Gold Hoarder 150HP/15gold/×5 + Treasure Thief 120HP/12gold/×7 + Coin Golem 220HP/25gold/×4, 全部 Type=Bounty 非 Boss) + 7 测试覆盖 default AddEnemy 2 字段全 inert / SetEnemyBounty 2 字段正确写入 / goldMult clamp upper 50→20 / goldMult clamp lower -5→1 / RecycleEntity 2 字段全 reset 关键防 ID 泄漏 / Killing Bounty 5× mult 实际放 50 gold (10 base × 1× killMult × 1× income × 5× bounty) / Killing non-Bounty 10 gold 不受 mult 影响 + 修复 1 测试 bug: `ResolveEnemiesKilledThisFrame` 只处理 death queue, `SetEnemyHealth(0)` 不会触发, 加 `QueueEnemyDeath` 才能让 killing test 实际结算 → 与 CombatResolution test pattern 一致 + 1016/1016 tests PASS + Claude bug scan PASS 0 critical bug)；bench2: 6970, bench4: 3236, bench5: 2881 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 178 baseline 7453/3332/2742 处于 -6.5%/-2.9%/+5.1% 噪声范围无回归
- 堡垒护甲 Tower Fortress Aura (Round 180 方向5 恢复完成：Systems/TowerFortressSystem.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Core/CombatGroup.cs + Core/CombatSetupGroup.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/TowerFortressSystemTests.cs。1 新增 3 SOA 字段 `TowerFortressNeighborCount` int[MAX_ENTITIES]/0 (O(N²) 邻居计数) + `TowerFortressCachedDmgBonus` float[MAX_ENTITIES]/0f (per-tower cached dmg mult, 0=inert fast path) + `TowerFortressCachedAtkSpdBonus` float[MAX_ENTITIES]/0f (per-tower cached atk-spd mult) + 1 静态 config `FortressConfig` (radius=2.0/T1=2 neighbors dmg=+0.15/T2=4 neighbors dmg=+0.25 spd=+0.10，方便 designers 改静态常量即可) + 1 公开 setter + 3 read helper (含 `IsValidEntity` 守卫 + clamp [0,32]/[0,1] 防溢出) + AddTower reset 3 路径 (Claude bug scan fix #1: 防 ID 槽位复用继承过期缓存) + 新建 TowerFortressSystem class：SetTurn 二次 pass（先清零缓存 → 后 O(N²) Chebyshev 距离扫描同类型邻居 → 邻居数映射到 T1/T2 tier → 写缓存字段；dispelled tower 双向 skip 防 cluster 污染 + 同步对称消除内外循环偏差）+ 6 accessors (FortressCount/DmgBonus/AtkSpdBonus 双向) + SystemRegistry 末段 `new TowerFortressSystem(store, logger)` + WireDependencies 末段 `scheduler.Combat.TowerFortress = fortress` + CombatGroup.Execute 在 Curse.ResolveCurseDebuffs 之后顺序调用 `TowerFortress?.SetTurn()` + CombatSetupGroup new 构造函数 (与 CombatGroup 同步注入) + TowerAttackSystem 2 处 hot path：(a) effectiveDmg 公式前 `fortressDmg = store.GetTowerFortressDmgBonus(towerId); if (fortressDmg > 0f) dmg *= (1f + fortressDmg)`（cache 0 fast-return 零开销 99% 塔）；(b) attackInterval 公式同步应用 atkSpd bonus（cache 0 fast-return 零开销）+ BenchmarkSystem 2 处 (bench4/bench5) 实例化 + SetTurn 调用同步插入 + 19 新测试覆盖 0-默认值/AddTower 3 字段 reset/单塔 0 bonus/2 邻居 T1/4 邻居 T2/异类型不 cluster/2.1 距离内 cluster/3 距离外不 cluster/3.0 边界含 (radius=2.0 chebyshev≤2 含边界 3.0)/2 cluster 互不影响/dispelled 跳过/source 自身 dispelled 不 cluster/目标 dispelled 不 cluster (bug scan fix #1)/bounty 同 type 仍然 cluster (这是 aura 不是 enemy 触发)/测试名 SetTurn_ThreeSameType_TriggersTier1 实际 4 座 2 邻居 (T1 阈值 = 邻居数而非塔数)/active=false tower 不 cluster/clamp upper 32/6 行内插 + 修复 1 Claude bug scan 发现 bug: 网格坐标虽然整数但 `(int)Math.Ceiling(radius)` 截断会过度扩大 neighbor 范围（理论风险）→ 改用 float 直接比较 `cheby > radius` 防御 + 1035/1035 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7583, bench4: 3294, bench5: 2951 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 179 baseline 6970/3236/2881 处于 +8.8%/+1.8%/+2.4% 全面提升无回归
- 后排传送敌 Phase-Through Enemy (Round 181 方向9：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/FrameScheduler.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/WaveSpawningSystem.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + Data/Monsters/monster_phaser.json + BattleSystemECS.Tests/PhaserEnemyTests.cs。6 新增 SOA 字段 `EnemyIsPhaser` bool/MAX_ENTITIES/false (sentinel gate: false 时 hot path fast-return 零开销) + `EnemyPhaserInterval` float/0f (秒，相位循环间隔，0=永不自动相位) + `EnemyPhaserDurationLeft` float/0f (当前相位剩余秒数，0=不在相位中) + `EnemyPhaserPhaseActive` bool/false (是否当前对物理伤害免疫) + `EnemyPhaserCycleTimer` float/0f (在「易伤」间隔中的累计秒数) + `EnemyPhaserPhaseDuration` float/0f (每次相位窗口持续的秒数，默认 1.5s) + 1 公开 setter `SetEnemyPhaser(enemyId, interval, phaseDuration)` (IsValidEntity 守卫 + interval clamp [0.1, 30.0] 防过快相位 + phaseDuration clamp [0.1, 10.0] 防软免疫敌) + 3 reset 路径 (构造/AddEnemy/DestroyEntity 同步 reset 6 字段防 ID 复用泄漏) + `MonsterConfig.IsPhaser` bool/false + `MonsterConfig.PhaserInterval` float/4f + `MonsterConfig.PhaserPhaseDuration` float/1.5f 配置字段 + `GameConfigLoader` 解析 3 新字段 (try-get-property + 默认 false/4f/1.5f) + `FrameScheduler.TickPhaserCycle(deltaTime)` 新方法挂在 I-frames 倒计时之后：O(activeEnemies) 单 pass，sentinel-gated 跳过非相位敌，在相位中 → 减 DurationLeft，归零时清相位标志重置 CycleTimer；不在相位中 → 累加 CycleTimer，到达 interval 时设置 PhaseActive=true + DurationLeft=PhaseDuration + 0 分配；`FrameScheduler.Tick` 钩子 + `WaveSpawningSystem` 在 gold init 之后/burrow init 之前插入 phaser init 段 `if (monsterConfig.IsPhaser) SetEnemyPhaser(enemyId, PhaserInterval, PhaserPhaseDuration)` + `TowerAttackSystem` 物理分支 (else Physical) 头部插入 phaser gate `if (EnemyPhaserPhaseActive[bestTarget]) return` (Parallel.For 内 return 等价于 continue，跳过该 tower 攻击迭代，让相位敌免疫物理伤害) + 物理分支的 2 个 conversion path (origPortion/convPortion 物理部分) 同样加 phaser gate (Magic/True damage 完全不受影响) + `PlayerTowerAttackSystem.ApplyResistancesAndEnqueue` 物理分支同步加 `if (EnemyPhaserPhaseActive[enemyId]) return` gate + 1 新 monster JSON `monster_phaser.json` (Wraith 120HP/0.5armor/0.5pierceResist/0.6speed/IsPhaser=true PhaserInterval=4.0 PhaserPhaseDuration=1.5) + 7 测试覆盖 default AddEnemy 6 字段全 inert / SetEnemyPhaser 6 字段正确配置 (含区间内值) / interval clamp upper 60→30 / interval clamp lower 0.01→0.1 / phaseDuration clamp upper 30→10 / phaseDuration clamp lower 0.01→0.1 / SetEnemyPhaser 越界 entityId no-op / RecycleEntity 6 字段全 reset 关键防 ID 泄漏 / PhaseActiveFlag 可读写 (单源真相，被 FrameScheduler.TickPhaserCycle 唯一更新) + 1042/1042 tests PASS + Claude bug scan PASS 0 critical bug)；bench2: 7464, bench4: 3314, bench5: 3027 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 180 baseline 7583/3294/2951 处于 -1.6%/+0.6%/+2.6% 噪声范围无回归
- Volley 多弹塔 Shotgun/Scatter (Round 184 方向7 恢复完成：Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerPlacementSystem.cs + Systems/TowerAttackSystem.cs + game_config.json。3 新增 `TowerConfig.ProjectileCount` int/1 + `PelletDamageMult` float/1f + `PelletConeRadius` float/0f（默认值 = inert single-shot 路径，所有 legacy 塔走 zero-overhead fast path）+ 3 新增 tower-side SOA `TowerProjectileCount` int[MAX_ENTITIES]/0 + `TowerPelletDamageMult` float/MAX_ENTITIES]/0f + `TowerPelletConeRadius` float[MAX_ENTITIES]/0f 镜像 + ComponentStore.AddTower 初始化路径 explicit 写入 1/1f/0f（即使 TowerConfig 默认值已对，defense in depth 防止 ctor field 顺序引发的 0/0f 陷阱）+ ComponentStore.DestroyEntity reset 路径 ProjectileCount=1（与 AddTower 对齐的 cosmetic 一致性，Claude bug scan LOW 警告）+ ComponentStore recycle path PelletDamageMult=1f / PelletConeRadius=0f + `GameConfigLoader` 解析 3 字段 + **fallback 防护**（`ExtractInt`/`ExtractFloat` 在 key 缺失时返 0，会覆盖 C# 默认 1/1f 让 legacy 塔静默失效；新增 `>0` 判断 fallback 到 1/1f，未来若有人放松 `projCount > 1` 守卫改为 `>= 1` 不会让所有 pre-184 塔静默破）+ `TowerPlacementSystem` PlaceTower 在 Lifesteal 之后 Taunt 之前插入 scatter 字段 3 行同步写入 store + `TowerAttackSystem.Update` 主 hot path 在 crit 段之后、chain lightning 段之前插入 scatter 分支：(a) `if (projCount > 1)` sentinel gate (b) pelletMult `<=0` fallback to 1f，coneRadius `<=0` fallback to TowerRange，最低 1f 防止 `r²<1` 找不到任何目标 (c) `pelletsToFire = min(projCount-1, 16)` 帽值防 benchmark 失控 (d) snapshot `_towerCandidateBuffers[ti]` 复用 + local scratch 副本（避免污染 per-tower shared buffer）(e) per-pellet 内层循环：找 primary 周围 coneRadius 内、距离最近、未被其他 pellet 占用、仍 active/HP>0 的 enemy 命中，`takenScratch[k]=-1` 标记防重复 (f) 找不到 secondary target 时回退到 bestTarget（保持原命中率）(g) 每 pellet `lock (damageLock) bag.Add((pelletTarget, baseDmg*pelletMult, ...))` 走与 primary 完全相同的 damage pipeline 享受所有同帧下游 buff/synergy/aura + game_config.json 新增 3 个散弹塔模板：Scatter Gun #151 (5 pellets × 0.4 mult, range 3) / Scorch Cannon #152 (3 pellets × 0.55 mult, range 4) / Storm Salvo #153 (6 pellets × 0.45 mult, range 5) + Range mult 塔受 weather rangeMult 缩放，0 scatter branch 时该塔退回 single-shot 行为；bench2: 7389, bench4: 3469, bench5: 2971 (Round 183 baseline 7593/3411/2925, -2.7%/+1.7%/+1.6% 噪声无回归)
- 沙暴天气 Sandstorm Weather (Round 185 方向1 恢复完成：Systems/WeatherSystem.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Data/Configs/weather.json + BattleSystemECS.Tests/SandstormWeatherTests.cs。1 新增 `WeatherConfig.Sandstorm=4` 枚举值 + 1 新增配置字段 `WeatherTypeConfig.EnemyDotPct` float/0f (默认 0 让其他天气类型 inert fast path) + 1 新增 cache 字段 `WeatherSystem._cachedEnemyDotPct` float/0f (sentinel-gated 0 short-circuit ApplyWeatherDot 整个循环) + 5 路径同步 reset (构造/ForceWeather 对应 4 分支) + `WeatherSystem.Update` 主循环在 `UpdateCachedMultipliers` 之后插入 `ApplyWeatherDot(playerId, deltaTime)` 调用 (Round 185 sentinel-gate 模式) + `ApplyWeatherDot` 新方法 (sentinel-gated `if (_cachedEnemyDotPct <= 0f) return` + intensity 缩放 + O(activeEnemies) 扫描 + HealthFloor gate + bounds-check 防御) + `WeatherSystem.GetEnemyDotPct(playerId)` 新公开方法 (sentinel-gated + intensity 缩放) + `GameConfigLoader` 解析 `enemyDotPct` 字段 (try-get-property + 默认 0 向后兼容) + `weather.json` 新增 Sandstorm 配置 (12s duration / 0.5-1.0 intensity / 0.95 enemySpeedMult / 0.8 towerRangeMult / 0.005 enemyDotPct = 0.5%/s DoT) + 7 测试覆盖 enum 4 / default EnemyDotPct=0 / RangeMult reduced / 实际 DoT 应用 / Clear 无 DoT / GetEnemyDotPct zero-for-non-sandstorm / GetEnemyDotPct intensity 缩放；1049→1056 tests PASS + Claude bug scan 发现并修复 2 个 bug: (1) ApplyWeatherDot 热路径里残留 `System.Console.WriteLine` 调试代码 (Round 184 时引入) 移除后 7793/3356/3032 → 7793 恢复 (2) `intensity` 局部变量作用域 bug 修复 (scaledDotPct 提前计算避免重复乘) → 最终 PASS 0 critical bug；bench2: 7245, bench4: 3303, bench5: 2922 (Round 184 baseline 7389/3469/2971, -2.0%/-4.8%/-1.7% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态
- Sapper 攻城工程敌 (Round 186 方向2 恢复完成：Systems/SapperSystem.cs + Core/AIGroup.cs + Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerAttackSystem.cs + Systems/TowerPlacementSystem.cs + Systems/WaveSpawningSystem.cs + Data/Monsters/sapper.json。9 新增 SOA 字段 `EnemyIsSapper` bool/false (sentinel gate: false 时 hot path fast-return 零开销) + `EnemySapperTargetTowerId` int/-1 (cached target, -1=未选定) + `EnemySapperAttackTimer` float/0f (累计秒数) + `EnemySapperDamage` float/0f (单次挥击伤害) + `EnemySapperAttackInterval` float/0f (挥击间隔) + `EnemySapperAtkSpdSlow` float/0f (累计减速 0..maxStacks*perStack) + `EnemySapperAtkSpdSlowPerStack` float/0f (单次 stack 减速) + `EnemySapperMaxSlowStacks` int/0 (stack 上限) + `EnemySapperRange` float/0f (搜索半径) + 3 新增 tower-side SOA `TowerCurrentHp` float/0f (0=已被摧毁) + `TowerMaxHp` float/0f (0=legacy indestructible 路径) + `TowerSapperSlowMult` float/0f (per-tower 累计减速倍数，BeginFrame 重置，SapperSystem 重新派生) + 1 公开 setter `SetEnemySapper` (IsValidEntity 守卫 + 全 5 数值字段 clamp 防 malformed JSON 失控) + 3 reset 路径 (构造/AddEnemy/_ResetEntity 同步 reset 防 ID 复用泄漏) + 1 新建 SapperSystem class (Update 2-phase：phase1 read-only 决定 attack events，phase2 serial 应用 damage + slow stacks；RecomputeTowerSlows 末尾 roll-up 派生 TowerSapperSlowMult；FindNearestTower manhattan 距离扫描排除 TowerMaxHp<=0 不可破坏塔) + AIGroup.Execute 末段 `Sapper?.Update + RecomputeTowerSlows` 注入 (lazy-init 使用 ConsoleLogger 兜底) + `MonsterConfig.IsSapper + 5 Sapper*` 配置字段 + `TowerConfig.MaxHp` 配置字段 (0=indestructible legacy) + `GameConfigLoader` 解析 6 新字段 + `TowerPlacementSystem.PlaceTower` MaxHp>0 时设置 TowerMaxHp/CurrentHp 双值 + `TowerAttackSystem` hot path 在 attack interval 公式前：(a) `if (MaxHp>0 && CurrentHp<=0) return` 摧毁塔跳过攻击 (b) `sapperAtkSpdMult = max(0, 1-TowerSapperSlowMult)` 乘性减速应用 + `WaveSpawningSystem.SpawnEnemy` IsSapper 分支初始化 + sapper.json 3 个 Sapper 怪物 (Saboteur 200HP/0.4spd/5dmg/0.10perStack×3/3.0range / Demolisher 280HP/0.35spd/8dmg/0.12perStack×3/3.0range / Siege Engineer 360HP/0.3spd/12dmg/0.15perStack×3/3.5range) + build 0 错误 13 警告 + 1056/1056 tests PASS；bench2: 7421, bench4: 3422, bench5: 2899 (Round 185 baseline 7245/3303/2922, +2.4%/+3.6%/-0.8% 噪声范围无回归) ⚠️ 全部低于阈值 (10000/4300/3800) 但属历史常态

- 集结号 Rally Buff（Round 187 方向4 恢复完成：Systems/RallySystem.cs + Core/ComponentStore.cs + Core/ComponentStore_Player.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SkillBuffGroup.cs + Core/SystemRegistry.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/RallySystemTests.cs。1 新增 GameConfig.RallyConfig 静态常量类（RallyRadius=5/RallyAtkSpdBonus=0.30/RallyDuration=3s/RallyCooldown=5s）+ 3 新增 SOA PlayerRallyActive/PlayerRallyDurationLeft/PlayerRallyCooldown + 1 新增 tower-side SOA TowerRallyAtkSpdBonus（BeginFrame 每帧重置 0）+ 1 新建 RallySystem class：订阅 GameEvents.PlayerDamaged 事件 → OnPlayerDamagedHandler（cooldown gate + ResolvePlayerIdFromEvent health-matching 启发式）→ stamp duration+cooldown → ApplyRallyBonusesForPlayer O(activeTowers) 扫描 → Update 每帧 tick duration/cooldown + re-derive bonuses + rally 过期清除 + SystemRegistry 构造注入 eventBus + SkillBuffGroup.Execute 末段 Rally?.Update(deltaTime) 接入 + TowerAttackSystem hot path attackInterval 公式加性叠加 rallyAtkSpdBonus（0=inert zero-overhead fast path）+ 10 测试覆盖 config 默认值/SOA 零值/BeginFrame reset/无 rally no-op/PlayerDamaged 触发/cool-down gate 阻断/Update 递减到过期/cooldown 递减/dispel 塔排除/过期后 BeginFrame 清零 + 修复 1 测试 bug: Env() 错误使用 CreateEntity() 获取玩家 id 改为直接 PlayerId=0 固定槽位（玩家数据是 MAX_PLAYERS 固定数组非 entity ID）；1066/1066 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 7559, bench4: 3205, bench5: 2960（Round 186 baseline 7421/3422/2899，+1.9%/-6.3%/+2.1% 噪声范围无回归）⚠️ 全部低于阈值但属历史常态

### 2026-06-06
- 链式治疗 Chain Heal（Core/GAS/GameplayAbility.cs AreaShapeType.ChainHeal=17 + Systems/SkillSystem.cs + Data/Configs/skills.json + BattleSystemECS.Tests/ChainHealTests.cs；mirror of ChainLightning 但目标为受伤友军 + optional 护盾附加；primary target = max HP deficit，ties by nearest；max 4 hops；50% decay/hop；out-of-range/dead/full-HP skip；no-overheal clamp；caster excluded；pool = MAX_PLAYERS=10 串行循环 zero allocation；+1 private bool[] _chainHealHitBuffer 复用 +1 16 测试覆盖 AreaShapeType 常量 / FromString 解析 / single / multi / decay / max-4 / range / no-overheal / dead skip / full-HP skip / shield bonus / cast dispatch / FromString unknown fallback + 修复 1 Claude bug scan 发现 bug: 6/6/6 hop 完全相同 deficit 的 ties broken by nearest-pick-first 路径在 hop 1+ 因 4 hop 后 bestDistSq 重置而严格 < 拒绝重复 0 dist，保留 ties-by-nearest 一致语义；696/696 tests PASS）；bench2: 8115, bench4: 3466, bench5: 3212
- 每塔类型放置上限 Per-Type Placement Cap（Round 139 方向2 恢复完成：Core/ComponentStore_Player.cs + Core/ComponentStore.cs + Systems/TowerPlacementSystem.cs + Systems/MineSystem.cs + Data/Configs/tower_placement.json + BattleSystemECS.Tests/TowerPlacementSystemTests.cs + BattleSystemECS.Tests/BattleSystemECS.Tests.csproj。1 新增 SOA 字段 `PlayerTowersOfType` int[MAX_PLAYERS * MAX_TOWER_TYPES=110]（playerId × typeId 索引，default-initialized 0） + 1 配对 `PlayerTowersOfTypeCap` int[110]（配置驱动上限，0=无上限）+ 1 const `MAX_TOWER_TYPES=11` 必须与 TowerType 枚举数量一致 + `LoadPerTypeCaps()` 从 tower_placement.json `maxPerTypeByType` map 读 (0..10 → cap) 填充所有 player 槽（单玩家但 future-proofing）+ `PlaceTower` 放置前 per-type cap 守卫（perTypeCap > 0 && perTypeCount >= perTypeCap → return -1 + 提示「Mix tower types」）+ `PlaceTower` 成功路径 `PlayerTowersOfType[capBase]++`（与全局 `PlayerTowerCount[playerId]++` 同步）+ `SellTower` 注释明确不再 decrement 计数器（避免双 decrement），`DestroyEntity` phase 1 snapshot `rtType` 读取 pre-reset 旧 type → phase 4 末尾 `if (rtType>=0 && rtType<MAX_TOWER_TYPES) { PlayerTowersOfType[capBase]--; PlayerTowerCount[ownerIdx]--; }` 单一权威回收点 + `MineSystem` 删除冗余 `PlayerTowerCount--` 防止负值 + tests.csproj 新增 `Data/Configs/tower_placement.json` CopyToOutputDirectory 让测试 bin 能 LoadPerTypeCaps + 7 测试覆盖 `LoadFromConfig` Basic=8/Sniper=4/EMP=3/Mine=6 / `BlocksExceedingTypeCount` 4th EMP rejected / `DoesNotAffectOtherTypes` 5th Sniper 拒但 Basic 通过 / `SellFreesTheSlot` counter 3→2→3 / `DestroyEntityDecrementsCounter` Mine 1→0 / `ZeroCapMeansUnlimited` 10 EMP 全过 / `PlayerTowerCountMatchesTypeSum` 全局计数=Σ per-type + 修复 1 doc bug：卖塔注释补上「destroy 也 decrement 故这里不 decrement」防未来维护者重复 decrement；814/814 tests PASS + Claude bug scan 0 critical bug + 1 已知 limitation：DestroyEntity 硬编码 ownerIdx=0（单玩家 TODO 多玩家 owner lookup）；bench2: 7436, bench4: 3566, bench5: 3142 ⚠️ bench2 -14.3% / bench5 +3.7% / bench4 +4.1% — bench2 显著回归需后续轮次观察
- 处决免疫 / 血量地板 Execute Immunity / Min-Health Floor（Round 132 方向8：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Systems/MarkSystem.cs + Systems/PlayerTowerAttackSystem.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/ExecuteImmunityTests.cs。2 新增 SOA 字段 `EnemyMinHealthFloor` float/0f（HP fraction 0-1 of MaxHealth，默认 0=无地板 opt-out）+ `EnemyExecuteImmune` bool/false（hard opt-out 标志 for execute-style 效果）+ 2 边界安全访问器 `ClampDamageToHealthFloor(enemyId, damage)`（floor=0 fast path 零开销；maxHp=0 早退；allowed≤0 → 0；damage>allowed → clamp）+ `ApplyMinHealthFloorInPlace(enemyId)`（in-place 后置 clamp 给绕过 ApplyEnemyDamage 的 hot path 用：explosion/transfer/vanguard/lifelink/tether/chain/splash）+ 4 reset 路径（构造 / AddEnemy / DestroyEntity / _ResetEntity 同步 reset 为 0f/false 防 ID 复用泄漏）+ `ApplyEnemyDamage` 在 path terrain mult 之后、shield 分流之前插入 floor clamp 段（floor=0 fast path 1 比较零开销）+ `MarkSystem.AddMark` 头部 `if (EnemyExecuteImmune) return 0` 守卫防止 immune 敌人被 mark + threshold fired + execute bonus 结算 + `TowerAttackSystem` 8 处 hot path bypass（vanguard transfer / lifeLink partner / tether partner / chain hop / splash inner/outer/no-falloff / TowerAttack primary finalDmg / Explosion）插入 `ApplyMinHealthFloorInPlace` 调用 + `PlayerTowerAttackSystem` lifeLink partner 1 处同步 + 16 测试覆盖 ClampDamage no-floor/zero-dmg/overkill-clamp/at-floor/below-floor/active-damage/Mult 0.1/0.5/AddEnemy reset/DestroyEntity reset/Recycle ID 无泄漏/ApplyInPlace 零 floor no-op/active-clamp/串行 ApplyEnemyDamage 集成；714/714 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8481, bench4: 3586, bench5: 3106 ⚠️
- 群体复活 Mass Resurrect（Round 133 方向5：Core/GAS/GameplayAbility.cs + Systems/NecromancerSystem.cs + Systems/SkillSystem.cs + Core/SystemRegistry.cs + Data/Configs/skills.json + BattleSystemECS.Tests/MassResurrectTests.cs。1 新增 `AreaShapeType.MassResurrect=18` 常量 + `FromString("massresurrect")` 解析 + 1 新增公开 `NecromancerSystem.MassResurrect(playerId, centerX, centerY, radius, hpFraction)` 玩家驱动的 AOE 复活：扫 CorpseQueue（≤256 槽循环零分配）→ 跳过已 reanimated/已被 necromancer claim/超出半径/超龄的 corpse → 对每个有效 corpse `CorpseReanimated=true + CorpseOwnerId=playerId` 防双复活 + 委派 `SpawnReanimatedMinion`（从原 TryResurrectCorpse 内部抽出共享 helper：查 MonsterConfig → AddEnemy 30% HP 一次性减半速度/伤害 → mark EnemyIsReanimated + EnemyOwnerId + SetSummonCircle 在玩家位置作召唤阵锚点） + 1 新增 `SkillSystem.InjectNecromancerSystem(necromancerSystem)` 可选依赖（null-safe；未注入时 case 18 log 警告返 0） + `ExecuteAbility` case 18 委派到 `NecromancerSystem.MassResurrect(playerId, playerX, playerY, massRadius=def.AreaRadius||3, hpFraction=def.HealPercent||0.3)` + 防御性 `necromancerSystem.SetTurn(_currentTurn, _currentTurn)` 同步 sim time 给 age 门控用 + `SystemRegistry.WireDependencies` 末尾 `Skill?.InjectNecromancerSystem(Necromancer)` 接线 + `skills.json` 新增 Mass Resurrect 技能（Radius=4 / HealPercent=0.3 / Cooldown=60s / Hotkey=R / AutoCast=false）+ 16 测试覆盖 `AreaShapeType.MassResurrect=18` 常量 / FromString 解析 / single corpse in radius / multiple in radius / out of radius ignore / claimed-by-necromancer ignore / already reanimated ignore / age-too-old ignore / zero-radius returns 0 / negative-hpFraction uses default 0.3 / twice-in-a-row second is no-op / MassResurrect on empty queue / FromString unknown fallback / case 18 dispatch / InjectNecromancerSystem null-safe / MassResurrect radius scaling + 修复 1 Claude bug scan 发现 bug：Test 15 `Assert.Equal(30f, store.EnemyHealth[minionId])` 因 IEEE 754 float 乘法累计（100 * 1.0 * 0.3）产生 30.0000019 偏差 → 改用 `Assert.Equal(30f, actual, 3)` tolerance 匹配项目惯例 + 732/732 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8193, bench4: 3... [truncated]
- Boss HP 自然再生 Boss Regen（Round 134 方向3 恢复完成：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/EnemyAISystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/BossRegenTests.cs。1 新增 SOA 字段 `EnemyHealthRegenPerSec`（float/0f，default zero regen fast path opt-in） + 1 `EnemyHealthRegenMult`（float，default-initialized to 1.0 via shared readonly template + per-instance Clone()，process-lifetime 100k fill loop runs only once 防止每次 new ComponentStore() 跑 100k 填循环） + `MonsterConfig.HealthRegenPerSec` + `MonsterConfig.PhaseRegenMult: List<float>` 配置字段 + `GameConfigLoader.ParseFloatArray` helper 处理 JSON 数组（含科学记数法 sign guard） + `EnemyAISystem.TickBossRegen()` 帧末 O(active enemies) 单 pass：`baseRegen <= 0f` 守卫 → 死活守卫 `currentHp <= 0f || currentHp >= maxHp` 跳过 → mult fallback（config lookup 失败时用 cached per-enemy 值）→ live-refresh phase mult indexed by `EnemyBossPhase` → `heal = baseRegen * mult * dt` → clamp to maxHp → `BossRegenDrainCount` 诊断计数 + `WaveSpawningSystem.SpawnEnemy` 在 boss 路径中读 `monsterConfig.HealthRegenPerSec`/`PhaseRegenMult` 写入 SOA + 恢复完成：修复 2 测试 bug（`EnemyHealthRegenMult` 默认 0 改为 1.0 via static readonly template Clone；`TickBossRegen_MultipleEnemies_OnlyRegenOnesTouched` 漏设 noRegen HP=50 改为显式 seed）+ BossRegenDrainCount stale-on-dt<=0 诊断噪声小修 + 747/747 tests PASS, bench2 8377, bench4 3510, bench5 3179 ⚠️ 全部低于阈值 (10000/4300/3800)）；bench2: 8377, bench4: 3510, bench5: 3179 ⚠️
- 神圣伤害 Holy Damage（Round 135 方向1：Core/DamageType.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Systems/PlayerTowerAttackSystem.cs + Systems/TowerAttackSystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/HolyDamageTests.cs + Data/Monsters/monster_demon_pitfiend.json + Data/Monsters/monster_undead_bat.json + Data/Monsters/monster_undead_lich.json + Data/Monsters/monster_undead_skeleton.json + Data/Towers/tower_holy.json。新增 `DamageType.Holy=64` 第 7 元素（位 6，与现有 1/2/4/8/16/32 位掩码无冲突）；`EnemyHolyResist: float[]` SOA 字段（float/0f，default 0=no-resist fast path 兼容旧怪物）；`MonsterConfig.HolyResist` 配置字段；SetElementalResist 扩展支持 Holy；AddEnemy 新增 holyResist=0f 可选参数（向后兼容）；DestroyEntity 在 EnemyHolyResist 之后 reset 0 防 ID 复用泄漏；WaveSpawningSystem 4 处 spawn site 调用 SetElementalResist 桥接 holyResist；TowerAttackSystem finalDmg 链在 Lightning 之后 Physical 之前插入 Holy 分支（resist 1% floor + _damageTakenMult）；PlayerTowerAttackSystem 同步插入 Holy 分支；4 个不死系怪物配置（undead_bat/lich/skeleton/demon_pitfiend）+ 1 个 Holy 塔配置 + 765/765 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 7388, bench4: 3532, bench5: 3185 ⚠️
- AOE 群体控制 AOE CC Group Control（Round 136 方向2 恢复完成：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/GAS/GameplayAbility.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/EnemyMovementSystem.cs + Systems/SkillSystem.cs + Data/Configs/skills.json + BattleSystemECS.Tests/AoeCcTests.cs。1 新增 SOA 字段 `EnemyRootDurationLeft` float[MAX_ENTITIES]/0f（duration 倒计时，0=no-root fast path）+ 1 边界安全访问器 `IsEnemyRooted(enemyId)`（validity 守卫 + duration>0 判定）+ 1 `ApplyEnemyRoot(enemyId, duration)`（unstoppable 早退 + refresh-or-set 取较大值语义对齐 stun/disarm）+ ComponentStore 构造 AddEnemy/DestroyEntity 双向 reset 防 ID 复用泄漏 + 3 新增 `AreaShapeType.AoeStun=19/AoeRoot=20/AoeKnockback=21` 常量 + `FromString` 解析 aoestun/aoeroot/aoeknockback + `GameplayAbilityDef` 3 字段 `AoeStunDuration/AoeRootDuration/AoeKnockbackForce`（0=no-effect 零开销 fast path）+ 构造时默认 0 初始化 + `SkillConfig` 3 字段同步 + `GameConfigLoader.ExtractFloat` 桥接 JSON → SkillSystem def 构造处复制 + `ExecuteAbility` case 19/20/21 委派 + 3 新增公开方法 `CastAoeStun/CastAoeRoot/CastAoeKnockback(centerX, centerY, radius, duration/force, name)`：并行 collect 半径内敌人 → 串行 apply CC effect（_activeEnemyList null guard + 死亡 enemyId 跳过 + 半径 ≤0 / duration ≤0 / force ≤0 早退 + playerId 自排除 + per-enemy CC immunity/stun-resistance/refresh 全部下放 store.Apply* 层处理，保证一致语义）；`EnemyMovementSystem` root decrement 置于 stun 早退之前（修复 1 Claude bug scan 发现 bug: stunned+rooted 敌人原本 root 倒计时永不衰减，root 实际持续 = 配置值 + stun 时长） + moveSpeed *= 0 final-gate（rooted 敌人位置更新 = noop 但 AI/ability 系统不受影响，distinct from stun） + skills.json 新增 3 技能（War Stomp aoestun radius=200 duration=2 CD=30s Hotkey=Y / Earthroot aoeroot radius=250 duration=3 CD=25s Hotkey=U / Shockwave aoeknockback radius=300 force=80 CD=20s Hotkey=I）+ 27 测试覆盖 AreaShapeType 常量+FromString 6 例 / CastAoeStun 命中/0-dur/0-radius/unstoppable skip/dead skip / CastAoeRoot 命中/默认 0/refresh longer-wins/shorter-then-longer/0-duration/OOR/unstoppable skip / CastAoeKnockback 命中/0-force/OOR/可累加/0-radius / 正交性 4 例 root≠stun / stun≠root / 串行叠加 / 互不干扰 + Claude bug scan 发现 1 bug 已修复；792/792 tests PASS, bench2: 8651, bench4: 3464, bench5: 3114 ⚠️）
- 主题 Boss 召唤 Themed Boss Summon（Round 137 方向6 恢复完成：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Systems/EnemyAISystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/ThemedSummonTests.cs。1 新增 SOA 字段 `EnemyThemedHpMult` float[MAX_ENTITIES]/1f（per-minion 主题 HP 加成，1.0=no-bonus fast path 兼容旧 minion）+ ComponentStore 构造静态 readonly 模板 1.0 预填 + 4 reset 路径（构造/AddEnemy/DestroyEntity/_ResetEntity 同步 reset 为 1.0f 防 ID 复用泄漏）+ `MonsterConfig.ElementAffinity` 字符串字段已存在沿用 + `WaveSpawningSystem.SpawnMinionNearPosition` 4 参 legacy overload 保留 100% 行为不变，**新增 6 参 overload** `(int typeId, int sourceEnemyId, float x, float y, int bossElementAffinity, int bossPhase = 0)`：themedHpMult 计算规则 = bossElementAffinity∈{Fire=1/Ice=2/Lightning=3/Poison=4} ∩ monsterConfig.ElementAffinity.ToString() 等值匹配（大小写不敏感 OrdinalIgnoreCase）→ +10% HP bonus（themedHpMult=1.10）；None/空/不匹配 → themedHpMult=1.0（zero penalty）→ 缩放公式 `scaledHealth = baseHP × waveHealthMult × themedHpMult`（与 wave damage mult 分离，boss 召唤不享受 wave damage 缩放，符合"召唤随从 vs 召唤 boss"语义差异）+ `scaledMaxHealth = baseMaxHP × waveHealthMult × themedHpMult` 同步 + `scaledDamage = baseDamage × waveDamageMult` 不变（themed bonus 仅影响 HP 不影响伤害，design 决策：召唤随从主要用作肉盾+少量输出） + `bossPhase` 形参预留未来 phase-based 缩放扩展（当前未使用但保留签名兼容） + AddEnemy 末段 `store.EnemyThemedHpMult[minionId] = themedHpMult` 写入 + legacy 4 参 overload 委派 6 参 overload 传 `bossElementAffinity=0`（None）保持向后兼容 + 6 测试覆盖 legacy 4 参 NoBonus baseline / MatchingElement_Fire→+10% HP（100→110） / MismatchedElement Ice+Fire affinity=无 bonus / EmptyAffinity Monster（无主题 boss）=无 bonus / CaseInsensitive "fire" vs Fire 枚举匹配（50→55） / AllFourElementTypes 全 4 元素 themed bonus 应用（200→220）+ 修复 5 测试 bug：`new GameConfig()` 调 `InitializeDefaultConfig()` 预填 5 个默认 Monster（Normal Slime Health=20/Fast/Strong/Ranged/Drainer 在 index 0-4）→ typeId=0 实际指向 Normal Slime 而非测试添加的 FireImp → 改测试用 `int testTypeId = config.MonsterTypes.Count - 1` 索引最新添加的测试 monster + debug log 路径走 `MockRenderer.Logs` capture 而非 stdout（xunit 默认不显示 stdout）+ 807/807 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 8857, bench4: 3578, bench5: 3053 ⚠️ 全部低于阈值 (10000/4300/3800)）
- 塔主动技能 Per-Tower Active Skill（Round 138 方向7 恢复完成：Systems/TowerActiveSkillSystem.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Core/CombatGroup.cs + Systems/TowerPlacementSystem.cs。1 新建 `TowerActiveSkillSystem` 公共 API：每帧 `Update(dt)` 走 ActiveTowerIds 倒计时 cooldown（O(activeTowers) 单 pass；inert 塔 ActiveSkillId==-1 单 if 跳过零开销）+ `TriggerTowerActive(towerId)` 公开入口供 HUD 按键调用（IsValidEntity 守卫 → TowerActive 守卫 → skillId≥0 守卫 → cooldown<=0 守卫 → SetTowerActiveOnCooldown 翻转 + log 观测）+ 3 个 read-only helper `IsTowerActiveReady/GetTowerActiveSkillId/GetTowerActiveCooldown` 供 HUD 渲染 + 3 个新 SOA 字段 `TowerActiveSkillId` int/-1（-1=无技能 inert 零开销 fast path）/ `TowerActiveCooldown` float/0f（当前剩余）/ `TowerActiveCooldownMax` float/0f（配置最大）+ 5 个边界安全访问器 `SetTowerActiveSkill/IsTowerActiveReady/GetTowerActiveSkillId/GetTowerActiveCooldown/SetTowerActiveOnCooldown/TickTowerActiveCooldown`（全部 IsValidEntity 守卫 + cooldown floor 0f 防负漂移 + SetTowerActiveSkill 接受 cooldown<0 时 clamp 到 0f 而不是抛异常避免 config 错误导致玩家崩溃）+ 4 reset 路径（构造 / AddTower / DestroyEntity / _ResetEntity 同步 reset -1/0/0f 防 ID 复用泄漏）+ `TowerConfig.ActiveSkillId` int/-1（默认 -1 = opt-in）+ `TowerConfig.ActiveCooldown` float/0f（0=无冷却总是 ready）+ `TowerPlacementSystem.PlaceTower` 在 TimeScale 后 IsTrap 前插入 `if (tc.ActiveSkillId >= 0) store.SetTowerActiveSkill(towerId, tc.ActiveSkillId, tc.ActiveCooldown)` 桥接 + `SystemRegistry.TowerActiveSkill` 属性 + `Initialize` 中 `new TowerActiveSkillSystem(store, config)` 实例化 + `WireDependencies` 末段 `scheduler.Combat.TowerActiveSkill = TowerActiveSkill` 注入 + `CombatGroup.Execute` 末段（`Taunt.ResolveTauntAssignments()` 之后）`TowerActiveSkill?.Update(deltaTime)` 调用 + 修复 1 build bug：`store.IsValidEntity` 静态方法必须用 `ComponentStore.IsValidEntity` 限定符（partial class 外部类实例引用静态方法 C# 不允许）+ 807/807 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 8680, bench4: 3424, bench5: 3031 ⚠️ 全部低于阈值 (10000/4300/3800)）
- 每塔类型出售比率 Per-Tower Sell Ratio Override（Round 140 方向7：Systems/TowerPlacementSystem.cs + Data/Configs/tower_placement.json + BattleSystemECS.Tests/TowerPlacementSystemTests.cs。1 新增字段 `sellRatioOverrideByType` float[ComponentStore.MAX_TOWER_TYPES=11] 索引 = (int)TowerType，-1f sentinel = 用全局 sellRatio (0.5) backward-compatible；2 构造函数都初始化为 -1f；`LoadSellConfig` 解析 tower_placement.json 新 `sellRatioOverrideByType` map（try-parse key + 边界检查 typeIdx ∈ [0, MAX_TOWER_TYPES) + ratio clamp [0, 1] + ValueKind==Object 守卫）覆盖 AOE=0.7/Sniper=0.35/Frost=0.6/Palisade=0.4；`GetEffectiveSellRatio(level, towerTypeIndex)` 新增 towerTypeIndex 形参，override>=0 替换 baseRatio（level decay + time decay + salvage 全链路保留）；`SellTower` 改 1 行读 `(int)store.TowerType[towerId]` 传参；4 测试覆盖 `LoadedFromJson` 4 类型全部 sell 出 gold / `HigherOverrideYieldsHigherRefund` AOE 0.7 > Basic 0.5 / `LowerOverrideYieldsLowerRefund` Sniper 0.35 < Basic 0.5 / `TypeWithoutOverrideFallsBackToGlobal` Tesla==Basic exact equality；818/818 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8447, bench4: 3465, bench5: 3187 ⚠️ 全部低于阈值 (10000/4300/3800)
- Debuff 衰减豁免 Boss Debuff Decay Resistance（Round 141 方向8：Core/ComponentStore_Enemy.cs + BattleSystemECS.Tests/DebuffResistTests.cs。1 新增 SOA 字段 `EnemyDebuffResistMult` float[MAX_ENTITIES]/0f（0-1 uniform duration scaler，0=无衰减 fast path）+ 1 纯函数 helper `ApplyDebuffResistToDuration(enemyId, duration)`（resist≤0 早退 / resist≥1 全免疫返 0 / 中间段 (int)(duration*(1-resist)) 截断 + 防负）+ 4 处 CC apply 入口插入 multiplier：ApplyEnemyRoot 在 unstoppable 守卫后调用 / ApplyEnemyDisarm 在 DisarmResistance 之后调用（与已有 per-type resistance 串联：先 per-type 缩放 → 再 uniform 缩放，最终 stacking 设计）/ ApplySlow 在 SlowResistance 之后调用（仅缩放 duration 不影响 factor）/ ApplyPolymorph 在 multiplier clamp 之后调用（duration 缩放但 damageTakenMultiplier 完整保留）+ 16 测试覆盖 default 0 不变 / helper 0/0.5/1 边界 / 0.99 把 1 回合 debuff 削到 0 / 负 duration 返 0 / helper 纯函数不 mutate state / 4 CC apply 入口分别 0.5/1 验证 / DisarmResistance+DebuffResistMult 串联（10→5→2）/ Slow factor 不被 debuff resist 影响（factor=0.4 保持）+ 835/835 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8348, bench4: 3517, bench5: 3006 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 140 baseline 8447/3465/3187 处于 ±5% 噪声范围无回归
- 仇恨/聚焦 Aggro / Focus Fire（Round 142 方向5：Systems/AggroSystem.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/CombatGroup.cs + Core/SystemRegistry.cs + BattleSystemECS.Tests/AggroSystemTests.cs。2 新增 SOA 字段 `EnemyFocusTowerId` int/-1（-1=无仇恨 fast path）+ `EnemyFocusDurationLeft` float/0f（duration 倒计时，0=无仇恨 fast path）+ 6 公开 API `MarkFocusTower(enemyId, towerId, duration)` / `MarkFocusTowerBulk(IReadOnlyList<int>, towerId, duration)` / `ClearFocus(enemyId)` / `OnEnemyDestroyed(enemyId)` / `HasFocus(enemyId)` / `GetFocusTowerId(enemyId)` + 1 哨兵字段 `_hasActiveFocus` bool 让无仇恨 Update 帧走 O(1) 快速路径 + 4 reset 路径（构造 / AddEnemy / DestroyEntity / ResetEnemy 同步 reset 为 -1/0f 防 ID 复用泄漏）+ tower destroy 块添加 eager-clear 段遍历 ActiveEnemyIds 强制清掉指向该塔的 focus（防陈旧 ID 指向已销毁塔）+ DestroyEntity 顺序：先 ResetEntity（清 focus）再 push freeEntityIds（ID 回收），保证 ID 复用后 focus 字段已 reset + bulk API 走 `int[]` 池（MAX_ENEMIES 预分配）+ CombatGroup.SetTurn 同步 sim time / Update 末段调用 Aggro?.Update(dt) / SystemRegistry 构造 + AssignToGroups 注入 scheduler.Combat.Aggro = Aggro + 22 测试覆盖 MarkFocusTower 单 enemy/duration countdown/refresh-overwrite/ClearFocus/HasFocus/GetFocusTowerId/无效 ID 早退/duration 倒计时归零/无仇恨 Update 帧 O(1)/bulk 一致性/OnEnemyDestroyed 防御性 守卫/Tower 销毁 eager-clear 双向 reset/AddEnemy 默认 -1/0/DestroyEntity ID 回收无泄漏/重复 MarkFocusTower 累积语义/过期 focus 后续 Mark 重新激活/max duration 999/0/0f 边界 + 修复 1 Claude bug scan 发现 bug：OnEnemyDestroyed 添加 `if (!store.EnemyActive[enemyId]) return;` 防御性守卫防 ID 回收重用期间访问旧敌人槽 + 858/858 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8262, bench4: 3438, bench5: 3161 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 141 baseline 8348/3517/3006 处于 ±5% 噪声范围无回归
- 塔类型 vs 敌类型 effectiveness 矩阵（Round 143 方向1：Systems/TowerAttackSystem.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Data/Configs/tower_effectiveness.json + BattleSystemECS.Tests/TowerEffectivenessTests.cs。1 新增 `GameConfig.TowerEffectivenessMatrix` Dictionary<string,float> 字典（composite key "<towerTypeIndex>|<enemyType>"，缺键默认 1.0x）+ 1 配对 `TowerEffectivenessEntryCount` int 观测字段 + `GameConfigLoader.LoadTowerEffectiveness` 解析 JSON 数组（缺文件安全 no-op）→ AOE/Sniper/Frost/Palisade/Mine 5 类型 × 10 敌类型 = 50 条 multiplier 矩阵（AOE vs Swarm 1.30 / Sniper vs Armored 1.50 / Frost vs Fast 1.30 / Palisade vs Swarm 1.40 / Mine vs Swarm 1.50 等）+ `TowerAttackSystem.SetGameConfig(config)` 注入器 + 1 私有 `GetEffectivenessMultiplier(towerType, enemyId)` helper（`(uint)enemyId >= MAX_ENTITIES` 守卫 + `GetEnemyTypeName` 空字符串守卫 + Dictionary.TryGetValue 缺键返 1.0f）+ 1 哨兵字段 `_hasEffectiveness` bool 让空矩阵走 O(1) 零分配 fast path（生产未配置时热路径完全无开销）+ `TowerAttackSystem` finalDmg 计算链 armor/resist/ramp/falloff/wave/synergy/weather 之后插入 effectiveness 乘数（`if (effMult != 1.0f) baseDmg *= effMult` 乘性叠加而非替换）+ `SystemRegistry` 构造时 `TowerAttack.SetGameConfig(config)` 接线 + 7 测试覆盖 DefaultMatrix empty/KeyFormat 1|Swarm/Multipliers stored as-is/EntryCount tracks/JSON file exists/ComponentStore.GetEnemyTypeName round trip/Default empty string + Claude bug scan PASS 0 critical bug + 修复 1 低风险 nit：towerType 字段缺失时默认 0 而非 skip（仅配置 corruption 时影响，生产 config 不触发）+ 865/865 tests PASS）；bench2: 7498, bench4: 3537, bench5: 3148 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 142 baseline 8262/3438/3161 处于 ±10% 噪声边界（bench2 -9.2% 略偏大，可能与 hot path 字典查表开销相关）
- 英雄主动技能集 Hero Active Skill Set（Round 144 方向4：Systems/HeroSkillSystem.cs + Data/Configs/hero_skills.json + Core/CombatGroup.cs + Core/SystemRegistry.cs + BattleSystemECS.Tests/HeroSkillSystemTests.cs。1 新建 `HeroSkillSystem` class，4 槽位/hero（MAX_HEROES=5），flatten 2D → 1D 数组 [heroId*4+slot] 索引 0..19；2 内部 SOA 数组 `_heroSkillIds` int/20/-1 sentinel（-1=无技能 inert 零开销 fast path）+ `_heroSkillCooldowns` float/20/0f + `_heroSkillCooldownMax` float/20/0f（镜像最大 CD 供 HUD 显示用）+ `_slotInitialized` bool/20；1 哨兵字段 `_anySkillConfigured` 让全无技能时 Update 走 O(1) 早退；6 公开 API `TriggerHeroSkill(heroId, slot)` 4 守卫（heroId∈[0,5)/slot∈[0,4)/HeroIsDeployed/skillId≥0/cooldown<=0）→ 成功翻 CD + log 观测 / `IsHeroSkillReady` 读 / `GetHeroSkillId` 读 / `GetHeroSkillCooldown` 读 / `GetHeroSkillCooldownMax` 读 / `HasAnyConfiguredSkill` 哨兵；1 公开 `Initialize()` 加载 `Data/Configs/hero_skills.json` 4 技能（Cross Slash / Mega Explosion / Guardian Heal / Cold Nova — 全部 `GameConfig.Skills` 中已存在真实 name）→ 按 SlotIndex 写入所有 5 hero + 解析 cooldown；1 公开 `SetConfig(GameConfig)` 延迟绑定；per-frame `Update(dt)` O(MAX_HEROES) walk，O(1) early-out on `_anySkillConfigured=false` 或 `dt<=0`；自包含 JSON parser（无 Newtonsoft 依赖）：`HeroSkillsConfigLoader.Parse(string)` 找 "Skills" 数组 + brace-pair 提取 + ExtractInt/ExtractString 简单字段抽取；SystemRegistry 构造期 new + SetConfig + Initialize + AssignToGroups 注入 `scheduler.Combat.HeroSkill`；CombatGroup.Execute 末段（Aggro 之后）`HeroSkill?.Update(deltaTime)` 接线 — 镜像 Round 138 TowerActiveSkillSystem 简化模式（gate + cooldown + log，effect dispatch 留作后续轮次 SkillSystem 集成）+ 17 测试覆盖 5 parser (EmptyJson/NoSkillsArray/SingleSlot/MultipleSlots/SkipsEmptyName) + 12 system (Ctor defaults/Trigger rejects undeployed hero/Trigger rejects out-of-range/Trigger rejects unconfigured slot/IsReady/GetCooldownMax returns 0/Update inert when no skills/Update decrements cooldown floor 0/Trigger fails on active cooldown/Trigger succeeds and flips cooldown/Initialize loads real JSON file) + 881/881 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 7800, bench4: 3510, bench5: 3096 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 143 baseline 7498/3537/3148 处于 ±10% 噪声范围无回归
- 塔类型专精重随 Per-Tower Modifier Pool（Round 145 方向3 恢复完成：Systems/TowerModifierSystem.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Systems/TowerPlacementSystem.cs + Data/Configs/tower_modifiers.json + BattleSystemECS.Tests/TowerModifierSystemTests.cs。3 新增 SOA 字段 `TowerModifierId` int[MAX_ENTITIES]/-1（-1=无 modifier inert 零开销 fast path）+ `TowerModifierMagnitude` float/0f（cached magnitude 镜像免 hot path 查 def）+ `TowerModifierRarity` int/0（0=Common..4=Legendary for UI + 设计师 min-rarity gate）；6 边界安全访问器 `HasTowerModifier/GetTowerModifierId/GetTowerModifierMagnitude/GetTowerModifierRarity`（IsValidEntity 守卫）+ `SetTowerModifier`（sentinel <0 → reset all 0/0f/0；rarity clamp [0,4]）+ 4 reset 路径（构造 / AddTower / DestroyEntity tower 分支 / _ResetEntity 同步 reset -1/0/0f 防 ID 复用泄漏 + 重复 reset 无害）+ `GameConfig.TowerModifierDef` 6 字段类（ModifierId/Name/Stat/Magnitude/Rarity/Weight/Description）+ `TowerModifiers[]` 配置池 + 3 lookup helper `GetTowerModifierDef(string) / GetTowerModifierDef(int) / GetTowerModifierIndex(string)` + `GameConfigLoader.LoadTowerModifiers` JSON 解析（缺文件→空数组 no-op / 空文件→no-op / JSON 异常→catch / null 条目跳过 / 空 ModifierId 跳过 / Weight≤0 修 1 / Rarity clamp [0,4] / Array.Empty fallback）+ `TowerModifierSystem` 8 公开 API（RollAtPlacement / RerollModifier / ClearModifier 3 mutator + GetModifierName/Stat/Magnitude/Rarity 4 read helper + SetMinRarity/GetMinRarity 设计师 gate）+ 1 哨兵字段 `_gameConfig` 缺失时整 system inert（RollAtPlacement → -1）+ 加权随机（Sum=0 fallback 返 -1）+ `TowerPlacementSystem.PlaceTower` 末段 `RollAtPlacement(towerId)` 接入 + log 观测 + `SetTowerModifierSystem` 可选注入（null-safe fast path）+ `SystemRegistry` 构造期 new + `WireDependencies` 末段 `TowerPlacement?.SetTowerModifierSystem(TowerModifier)` 接线 + `tower_modifiers.json` 5 modifier 池（CritChance +0.05 / LifeOnKill +5 / GoldOnKill +3 / AttackSpeed +0.10 / CooldownReduction -0.20s）+ 24 测试覆盖 default inert / Pool-from-JSON loading / Weight clamp / Rarity clamp / Idempotent reset / RollAtPlacement 命中率 / Reroll drops previous / ClearModifier resets / MinRarity gate / Read helpers / ID-reuse safety / 防御性 invalid ID 守卫 / 905/905 tests PASS + Claude bug scan PASS 0 critical bug；bench2: 8211, bench4: 3464, bench5: 2955 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 144 baseline 7800/3510/3096 处于 ±5% 噪声范围无回归
- 方向文件状态修正（Round 146：本轮巡检发现方向四 HeroSkillSystem 状态在文件中仍标 ❌ 缺失，但代码已由 Round 144 commit 21fa61a 完整实现 — 1 新建 `HeroSkillSystem` 类、4 slot × 5 hero 平面化存储、`_anySkillConfigured` O(1) 哨兵 fast path、6 公开 API（TriggerHeroSkill 4 守卫 + 5 读 helper）、HeroSkillsConfigLoader 极简 JSON 解析器无 Newtonsoft 依赖、Data/Configs/hero_skills.json 就位、SystemRegistry 构造 + Initialize、CombatGroup 末段 `HeroSkill?.Update(dt)` 接线、12 测试覆盖。0 代码改动：仅修正方向文件状态行 + 重跑 bench 验证无回归；bench2: 7826, bench4: 3050, bench5: 3028 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 145 baseline 8211/3464/2955 处于 ±10% 噪声范围无回归
- 尸体效果/圣化地面 Corpse Effect: Hallowed Ground（Round 168 方向3 恢复完成：Systems/CorpseEffectSystem.cs + Core/GameConfig.cs + Data/Configs/corpse_effects.json + BattleSystemECS.Tests/CorpseEffectSystemTests.cs + BattleSystemECS.Tests/DebugHallowedTest.cs。1 新增 `CorpseEffectType.HallowedGround=6` 枚举值（positive-feedback 神圣伤害地面区，每 tick 对范围内敌人造成 DoT）+ `CorpseEffectDef.EffectType=6` 配置支持 + corpse_effects.json 新增 Hallowed 配置 + CorpseEffectSystem.ApplyDoTTick 改用 `GameplayEffectDef.Periodic` 工厂 + `AttributeSetDefinitions.ENEMY_HEALTH` 而非 legacy `ApplyDot(int,float,int)` overload（修复 1 bug: legacy overload leaves TicksRemaining=0 so DoT 永远不 tick） + `using BattleSystemECS.Core.GAS;` 引入 GAS namespace 让 `GameplayEffectDef/AttributeSetDefinitions` 可见 + 修复 1 build bug: CorpseEffectSystem.cs 引用 `GameplayEffectDef`/`AttributeSetDefinitions` 但缺 `using BattleSystemECS.Core.GAS;` 导致 2 个 CS0103 编译错误 → 添加 using 后 build PASS + 193 行新测试覆盖 HallowedGround spawn tick / kill enemy in range / heal in / multiple zones stack / friendly-tower buff v2 预留 / corpse effect queue 调度 + 909/909 tests PASS + Claude bug scan PASS 0 critical bug）；bench2: 8375, bench4: 3440, bench5: 3152
- 荆棘藤蔓地面 Thorny Bramble Zone（Round 169 方向10：Systems/CorpseEffectSystem.cs + Data/Configs/corpse_effects.json + BattleSystemECS.Tests/CorpseEffectSystemTests.cs。1 新增 `CorpseEffectType.ThornyBramble=7` 枚举值（DoT + slow 复合 zone，敌人进范围受 3 dmg/tick + 0.6x 减速 4s 持续时间） + `Update()` 末尾 DoT 条件分支扩展 `curEffectType == 0 || 3 || 6 || 7` 触发 tick + `ApplyContinuousEffect` 末尾新增 `else if (effectType == 7) ThornyBramble` 分支复用 slow 比较逻辑（`slowAmount < existingSlow` 才覆盖 + 与 type 1 Slow 共用 min-compare 规则）+ corpse_effects.json 新增 `thorny_bramble` 配置（duration 4s / radius 1.2 / damagePerTick 3 / tickInterval 1s / slowAmount 0.6 / monsterTypes [Troll/Demon/ThornedShambler]）+ 5 新测试覆盖 in-range 复合效果（DoT+Slow）/ out-of-range 无效果 / slow 强度不覆盖更强 slow / duration 到期过期 / JSON 配置文件含 thorny_bramble 关键字 + 914/914 tests PASS + Claude bug scan 1 false-positive 「scope 错位」指控 → 经核实 `else if (effectType == 7)` 已在 `if (distSq <= radius*radius)` 块内 (line 240) 与 type 1/2 同位置 → 5 个 in-range/out-of-range 测试全 PASS 反证代码正确 → 标记 PASS 0 critical bug）；bench2: 8091, bench4: 3615, bench5: 3040 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 168 baseline 8375/3440/3152 处于 ±5% 噪声范围无回归
- 霜冻 Frostbite 非叠加 %maxHP DoT（Round 170 方向6：Systems/FrostbiteSystem.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/SystemRegistry.cs + Core/SkillBuffGroup.cs + BattleSystemECS.Tests/FrostbiteSystemTests.cs。3 新增 SOA 字段 `EnemyFrostbiteMaxHpPct` float[MAX_ENTITIES]/0f（每敌人当前 frostbite % 0-1，0=无 frostbite fast path）+ `EnemyFrostbiteTimer` float/0f（距离下次 tick 倒计时，0=无 frostbite fast path）+ `EnemyFrostbiteDurationLeft` float/0f（持续时间倒计时，0=过期 fast path）+ ComponentStore 构造/AddEnemy/DestroyEntity/_ResetEntity 4 路径同步 reset 0/0/0f 防 ID 复用泄漏 + 1 新建 `FrostbiteSystem` 类（mirror `BleedSystem` 模式：并行 collect tick-due 敌人 → 串行 enqueue 到 ping-pong queue `_frostQueue0/1` + `_frostQueueLock` 线程安全 → `ResolveFrostbiteDamage()` 串行从 queue 读出应用 `pct × EnemyMaxHealth[enemyId]` 伤害到 `ApplyEnemyDamage` 链 → 1.0s tick 节流 + duration 衰减 + 过期清理 `Timer=0, Pct=0, DurationLeft=0`） + 公开 API `ApplyFrostbite(enemyId, pct, duration)` idempotent reapply（refresh duration 但 pct 保留已应用值 — 简化语义：再次应用时若新 pct > 旧 pct 才覆盖，否则保留较强者）+ 1 哨兵 `_hasActiveFrostbite` bool 让无活动时 `Update()` 走 O(1) 早退 + 1 公开 `_overflowDrops` 计数器暴露 queue overflow 观测 + SystemRegistry 构造期 new + `scheduler.SkillBuff.Frostbite = Frostbite` 注入 + SkillBuffGroup 末段（Frostbite?.Update(dt); Frostbite?.ResolveFrostbiteDamage();）接入 tick lifecycle + 8 测试覆盖 tick damage=1%×maxHP / 3-tick 累计 / duration 到期清理 / resistance=0.5 减半 / invulnerability 跳过 / kill in 1 tick / boss 大 HP 测试 / refresh 续 duration / 重复 Apply 保留较强者 + 修复 4 Claude bug scan 发现 bug：Bug 1 queue overflow 加 `_overflowDrops` 计数器 + Bug 3 过期时 `DurationLeft` 同步清零防 ID 复用残留 + Bug 4 跳过冗余 timer 复位 + Bug 5 删除 512 buffer 的 `Array.Clear`（queue tail index 已实现 LIFO 语义不需要 clear）+ Bug 2 (race condition) 经核实与 BleedSystem 共享语义，game phase 顺序保证 ApplyFrostbite 在 SkillBuffGroup.Update 之前运行，theoretical only 不需修复 + 928/928 tests PASS）；bench2: 7730, bench4: 3546, bench5: 3186 ⚠️ 全部低于阈值 (10000/4300/3800) 但与 Round 169 baseline 8091/3615/3040 处于 ±5% 噪声范围无回归
- 凋零地面 Blighted Ground（Round 171 方向4：Systems/CorpseEffectSystem.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/ComponentStore_World.cs + Core/ComponentStore.cs + Data/Configs/corpse_effects.json + BattleSystemECS.Tests/CorpseEffectSystemTests.cs。1 新增 `CorpseEffectTypeEnum.BlightedGround=8` 枚举值（DoT + 防御/速度 debuff 复合 zone，敌人进范围受 2 dmg/tick + 30% 护甲 reduction + 20% 移速 reduction 5s 持续时间）+ 2 新增 `CorpseEffectDef` 字段 `ArmorReduction` float/0f（additive 护甲 debuff）+ `SpeedReduction` float/0f（additive 移速 debuff）+ GameConfigLoader.LoadCorpseEffectDefs 加 2 行 `TryGetProperty("armorReduction"/"speedReduction")` 解析 + 2 新增 SOA 字段 `CorpseEffectArmorReduction` float[MAX_CORPSE_EFFECTS] + `CorpseEffectSpeedReduction` float[MAX_CORPSE_EFFECTS] + `AddCorpseEffect` 末段 2 行赋值 + `RemoveCorpseEffect` 末段 2 行 reset 0f + 核心 `CorpseEffectSystem.Update()` DoT 条件分支扩展 `curEffectType == 8` 触发 tick + `ApplyContinuousEffect` 早退守卫 `effectType != 1 && != 2 && != 7 && != 8 return` + 末尾新增 `else if (effectType == 8) BlightedGround` 分支 **additive 累加**到共享 `EnemyCurseArmorReduction / EnemyCurseSpeedReduction` SOA 字段（与 `CurseAuraSystem` 的 `+=` 累加语义一致，可堆叠）+ `HandleEnemyKilled` 末段多传 2 个 debuff 参数 + corpse_effects.json 新增 `blighted_ground` 配置（duration 5s / radius 1.4 / damagePerTick 2 / tickInterval 1s / slowAmount 1.0 / armorReduction 0.30 / speedReduction 0.20 / monsterTypes [BlightedHorror/Wraith/PlagueBearer]）+ **修复 1 关键 pre-existing bug**：`ComponentStore.BeginFrame()` 增加 4 行 `EnemyCurseDmgReduction[eid] = 0f; EnemyCurseSpeedReduction[eid] = 0f; EnemyCurseArmorReduction[eid] = 0f; EnemyCurseDmgTakenIncrease[eid] = 0f;` per-frame reset 迭代 `_activeEnemyIds`（10K enemies × 4 fields = 40K writes/frame sub-ms 开销）— 之前 4 字段从不在帧初清零 → `CurseAuraSystem` 的 `+=` 累加会在跨帧未清场下无限增长导致 curse auras 越来越强失控；本轮 reset 让 BlightedGround 的 `+=` 与 CurseAuraSystem 的 `+=` 都有明确定义（每帧从 0 开始 + 本帧所有 zone/tower 贡献 additively 累加）+ 7 新测试覆盖 apply debuff per-frame / out-of-range 无效果 / DoT 与 debuff 同时 tick / **多 zone 累加 0.30+0.30=0.60** / duration 到期过期 / JSON 配置文件含 blighted_ground/armorReduction=0.3/speedReduction=0.2 关键字 / 旧 effect types (6/7) 不受新字段影响 regression guard + 修复 2 Claude bug scan 发现 bug：Bug 1 stale-field contamination → 改用 `+=` 累加 + 配 BeginFrame reset 形成 "清零 + 累加" 帧内一致语义；Bug 2 overwrite `=` vs accumulate `+=` 冲突 → 统一所有 curse 字段为 `+=` 累加 + 935/935 tests PASS + Claude bug scan 修复后 PASS 0 critical bug）；bench2: 7687, bench4: 3221, bench5: 2895 ⚠️ 全部低于阈值 (10000/4300/3800) 且 bench4 -9.2% / bench5 -9.1% 相比 Round 170 略降但与 Round 168/169 baseline 处于 ±10% 噪声边界 — BeginFrame 新增 O(active_enemies) curse reset + BlightedGround 新 case 8 per-frame 路径是主要成本来源，仍在可接受范围

### 2026-06-05
- 末日时钟关卡 DoomClock Level Variant（Systems/DoomClockSystem.cs + Core/GameState.cs + Core/ComponentStore_World.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/ObjectiveSystem.cs + Core/PostDeathGroup.cs + Core/SystemRegistry.cs + Data/Levels/doom_clock.json + BattleSystemECS.Tests/DoomClockTests.cs，ObjectiveType 枚举 DoomClock=5 + 6 SOA 字段 DoomClockTimer/Duration/WavesCleared/CycleCount/FinalScore/Active + LevelConfig 6 配置 + DoomClockWaveTemplate 类 + GameConfigLoader 解析 + DoomClockSystem InitializeFromLevel/Update/OnWaveCleared/GetCurrentWaveSlot/GetCycleScalingMultiplier/ComputeFinalScore/EndRun/GetStatus + ObjectiveSystem 缓存 level + 种子 DoomClock + WavePhase 减计时器 + OnWaveCompleted 增量 + CheckObjective win 时计算 FinalScore + HUD 文本 + PostDeath 接入 + SystemRegistry 创建实例 + 31 测试）；bench2: 9318, bench4: 3805, bench5: 3448 ⚠️
- 预测瞄准 / 前置射击 Predictive Aim / Lead Targeting（Round 114 方向1：Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerPlacementSystem.cs + Systems/ProjectileSystem.cs + Systems/TowerAttackSystem.cs。1 新增 SOA 字段 `TowerLeadAimFactor`（float, 0f=零开销 fast path）+ 1 `SetTowerLeadAimFactor(towerId, factor)` 边界安全访问器（Math.Clamp [0, 2]）+ TowerConfig.LeadAimFactor 配置属性 + GameConfigLoader.ExtractFloat 解析 + 4 处 reset（AddTower/RemoveTower/DestroyEntity/_ResetEntity）防 ID 复用泄漏 + TowerPlacementSystem.PlaceTower 桥接 SetTowerLeadAimFactor 写入 + ProjectileSystem.Fire() 末尾新增 `leadAimFactor` 形参（默认 0f 零开销，>0 时按 `EnemyMoveDirX/Y × EnemyMoveSpeed × dist/safeSpeed × factor` 计算 leading point 并据此设置 `_projVelX/Y` 当帧初始速度，degenerate dist 退化情况回退到原 straight aim）+ TowerAttackSystem fragment fire 路径透传 `TowerLeadAimFactor[towerId]` 给 ProjectileSystem.Fire。设计哲学：Homing 弹道会"导弹式"过头，Lead 弹道则直线飞行但计算目标未来位置，更符合物理直觉；为新增 `aimType=3=lead` 与现有 0=straight/1=homing/2=arc 并列预留空间，hot path 仅在 leadAimFactor>0 时启用计算，零开销默认 fast path 保持原行为）；bench2: 9136, bench4: 3521, bench5: 3311 ⚠️
- 恢复完成：Boss 阶段技能切换 Boss Phase Skill Switching（Round 111 方向1：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Systems/EnemyAISystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/BossPhaseSystemTests.cs。6 新增 SOA 字段（`EnemyPhaseCount` int/0 + `EnemyPhaseThresholdsFlat[4*MAX_ENTITIES]` float/0f + `EnemyPhaseSpeedMults` float/1f + `EnemyPhaseDamageMults` float/1f + `EnemyPhaseAbilityIdsFlat[4,MAX_ENTITIES]` string[,]/null + `EnemyPhaseFiredMask` int/0 4-bit 位掩码）+ `BOSS_PHASE_MAX=4` 常量 + ComponentStore 构造预热所有 phase 字段为 1f（零开销 fast path）+ `_ResetEntity` 显式 null-init 全部 6 字段 + `WaveSpawningSystem` 在 Boss spawn 路径填结构化字段（threshold / speedMult / damageMult / abilityId 直接入 2D 数组，无 CSV 无每帧 Split）+ 旧 CSV `EnemyPhaseThresholds` 路径保留向下兼容 + `EnemyAISystem` 顺序段 + 并行段两路同步新增 phase transition 检测（HP < threshold → 一次性 mark `FiredMask` + apply SpeedMult/DamageMult + push 到 `ConcurrentBag<(int enemyId, string abilityId)>`）+ Update 末尾 `DrainPhaseAbilityEvents` 串行转交 `EnemyAbilitySystem.EnqueueAbility`（非线程安全 API）+ 17 测试覆盖 `BOSS_PHASE_MAX=4` 常量/默认 inert/DestroyEntity reset/CSV→2D ability/FiredMask 防重入/Speed-Damage mult 一次性/HP 阈值未到 no-op/empty AbilityId no-op/phase chain 顺序触发 + 403/403 tests PASS + Claude bug scan PASS 0 bug）；bench2: 8926, bench4: 3727, bench5: 3261 ⚠️
- 附魔 Enchant / Imbue（Round 116 方向3：Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/EnchantSystemTests.cs。4 新增 SOA 字段（`TowerEnchantedElement` int[MAX_ENTITIES]/0 + `TowerEnchantBonus` float/0f + `TowerEnchantDuration` float/0f + `TowerEnchantExpiresAtTurn` int/0）+ 5 边界安全访问器 `SetTowerEnchantment(towerId, element, bonus, duration, expiresAtTurn)`（clamp element∈[0,4]/bonus∈[0,10]/duration∈[0,60]/expiresAtTurn≥-1）+ `ClearTowerEnchantment(towerId)` + `GetTowerEnchantedElement/GetTowerEnchantBonus/GetTowerEnchantDuration` 3 读取器（**O(1) 自动过期**：`GetTowerEnchantedElement` 内 `if (expiresAtTurn>=0 && store.CurrentFrame>=expiresAtTurn)` 清零并返回 0，省专用 TickEnchant 系统）+ 3 reset 路径（AddTower init/RecycleTowerEntity/DestroyEntity Tower 分支 4 行 reset 防 ID 复用泄漏）+ `TowerAttackSystem` finalDmg 计算链中在 Anti-Summon 之后、vanguard 之前插入附魔分支：guard `enchantElem==0` fast path 零开销 → 激活时 `finalDmg *= 1+bonus`（bonus>0 才有乘）+ `EnemyElementStatus[enemyId] |= (1 << (elemIdx-1))` 位掩码写入 4 元素（Fire=bit0/Ice=bit1/Lightning=bit2/Poison=bit3，匹配 ElementType 枚举）+ `EnemyElementTimer[enemyId*4+elemIdx] = max(current, enchantDur)` 保留更长现有状态不缩短 + 14 测试覆盖 Set/Get round trip/Clear/InvalidId 早退/clamp 各字段/永久-1/过期 turn 自动清零/未过期读保持/数据路径 accessor+apply 同步/更长的 existing 不被覆盖/DestroyEntity 双向 reset/Recycled slot ID-reuse 无泄漏/默认元素 0 fast path + 433/433 tests PASS + Claude bug scan PASS 0 bug + 修复 1 bug：getter 过期判定 `expires>0` → `expires>=0` 且 `CurrentFrame>expires` → `CurrentFrame>=expires`，允许 expiresAtTurn=0 正常过期并匹配文档语义）；bench2: 8545, bench4: 3431, bench5: 3194 ⚠️
- 召唤阵 Summon Circle / 反召唤塔 Anti-Summon（Round 115 方向2：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_Tower.cs + Systems/NecromancerSystem.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/SummonCircleTests.cs。4 新增 SOA 字段（`EnemyInSummonCircleX/Y/Radius` 三个 float，0f=无 circle 零开销 fast path；`TowerAntiSummonMultiplier` float，0f=无 bonus 零开销 fast path）+ 2 边界安全访问器 `SetSummonCircle(enemyId, x, y, radius)`（Math.Max 0f 半径防负）+ `ClearSummonCircle(enemyId)`（reset 0/0/0）+ `SetTowerAntiSummonMultiplier(towerId, mult)`（clamp [0, 10]）+ ComponentStore 构造 + _ResetEntity 同步 null-init 4 新字段防 ID 复用泄漏 + DestroyEntity/AddEnemy/AddTower 三处 reset 循环补全 + `NecromancerSystem.TryResurrectCorpse` 成功 spawn reanimated minion 后追加 1 行 `SetSummonCircle(minionId, necromancerX, necromancerY, resurrectRange)` 注册召唤阵锚点 + 半径（复用 necromancer 自身 resurrectRange 作 circle 半径，初期同尺度简化）+ `TowerAttackSystem` finalDmg 计算链中在元素 affinity/exposure 之后、vanguard 之前插入反召唤 hook：三道零开销守卫（multiplier==0 / radius==0 / finalDmg<=0）→ 平方距离比较判断敌人在圆内 → 在则 `finalDmg *= antiSummonMult`（Chebyshev 风格 dx*dx+dy*dy <= r*r 一次乘法 + 4 减法 1 加法 + 1 比较）+ 15 测试覆盖 default-no-op / Set-Clear round trip / 负半径 clamp / DestroyEntity 双向 reset / multiplier clamp [0,10] / fast path 双零 / 7 个 Theory 验证 in-circle / out-of-circle 边界（中心 / 3-4-5 / 7 / 8 / 11 / 0,11 / 7,8 共 7 例） + 418/418 tests PASS + Claude bug scan PASS 0 bug）；bench2: 8723, bench4: 3724, bench5: 3328 ⚠️
- 每元素抗性 Per-Element Resistance (Round 117 方向1：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/ElementalResistanceTests.cs。3 新增 SOA 字段 `EnemyFireResist[]` / `EnemyIceResist[]` / `EnemyLightningResist[]`（float, 0-1 fraction reduction）+ 2 边界安全访问器 `SetElementalResist(enemyId, fire, ice, lightning)`（clamp [0,1]）+ `GetElementResist(enemyId, DamageType)`（按 type 派发，True/Physical/Magic 返回 0f）+ `AddEnemy` 新增 3 可选参数 `fireResist/iceResist/lightningResist=0f`（向后兼容 fast path）+ `DestroyEntity` 在 `EnemyDamageResistance` 之后 reset 三字段为 0 防 ID 复用泄漏 + `MonsterConfig` 3 字段 `FireResist/IceResist/LightningResist` 默认 0f + `WaveSpawningSystem` 3 处 spawn site（normal/boss-rush/regular）均在 `SetDamageImmunityMask` 之后调用 `SetElementalResist` + `TowerAttackSystem` finalDmg 计算链在 Magic 之后 Physical 之前插入 3 个 `else if (dmgType == Fire/Ice/Lightning) baseDmg *= Math.Max(0.01f, 1f - EnemyXxxResist[bestTarget]) * _damageTakenMult` 分支 + `PlayerTowerAttackSystem` 同步插入 3 对应分支（之前元素伤害错误地走 armor 现已对齐）+ 优先级链 True 伤害 → immunity-mask → elemental-resist → armor/magicResist + 22 测试覆盖 AddEnemy 种子/clamp/SetElementalResist 双向 clamp/GetElementResist 派发/OutOfBounds/DestroyEntity-reset/ID-reuse-safety/PlayerTowerAttackSystem 集成 30%Fire/50%Ice/70%Lightning 各折减 + 1% damage floor at 99% resist + True bypass + Physical 不受影响 + immunity-mask 优先级 + non-matching-immunity 不阻挡 + MonsterConfig JSON 字段默认 0 + 456/456 tests PASS + Claude bug scan PASS 0 bug)；bench2: 8694, bench4: 3666, bench5: 3256 ⚠️
- I 帧/无敌帧 Invulnerability Frames (Round 118 方向2：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/FrameScheduler.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + BattleSystemECS.Tests/InvulnerabilityFramesTests.cs。2 新增 SOA 字段 `EnemyInvulnOnHitFrames[]` (int, 0=零开销 fast path 配置值) + `EnemyInvulnFramesLeft[]` (int, 0=无 I 帧状态运行时倒计时) + `FrameScheduler.Tick` 头部 `DecrementInvulnFramesLeft()` 遍历 `ActiveEnemyIds` 减 1（BuildPhase 也减避免 Boss 跨 BuildPhase 永续 I 帧）+ `TowerAttackSystem` 3 处 I 帧守卫（Parallel.For lambda 内用 `return` 而非 `continue` 修复 CS0139：line 940 串行 finalDmg 入口 / line 1160 chain attack 之前 write-back 帧数 / line 1317+ 另一处 serial damage apply）+ `PlayerTowerAttackSystem` 同步 I 帧守卫 + write-back 保持对称（让 Boss I 帧对玩家和塔都生效）+ I 帧是时间锁非伤害类型 → True damage 不豁免 + 5 测试覆盖 I 帧阻断（默认 0=无 I 帧不阻挡攻击）+ 零配置时不装填 I 帧 + I 帧与 PhaseSystem EnemyIsInvulnerable 状态独立 + 1% damage floor 保护 + 470/470 tests PASS + Claude bug scan PASS 0 bug)；bench2: 8745, bench4: 3346, bench5: 3302 ⚠️
- Boss 阶段召唤增援 Boss Phase Minion Summon (Round 119 方向3：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/EnemyAISystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/BossPhaseMinionSpawnTests.cs。2 新增 SOA 数组 `EnemyPhaseMinionTypeIdFlat[BOSS_PHASE_MAX*MAX_ENTITIES]` / `EnemyPhaseMinionCountsFlat[BOSS_PHASE_MAX*MAX_ENTITIES]`（int，-1/0 = no summon，沿用 EnemyPhaseAbilityIdsFlat 的索引布局）+ 1 常量 `BOSS_PHASE_SUMMON_CAP=8` + 3 边界安全访问器 `SetEnemyPhaseMinion`（clamp typeId ≥ -1 / count ∈ [0, 8]）/ `GetEnemyPhaseMinionTypeId` / `GetEnemyPhaseMinionCount` + 构造 + `DestroyEntity` wasEnemy 分支 + `_ResetEntity` 三处 reset 为 (-1, 0) 防 ID 复用泄漏 + `BossPhaseDef` 加 2 字段 `MinionTypeId` / `MinionCount` 默认 0 = opt-out + `GameConfig.GetMonsterConfigByTypeId(typeId)` 新 typeId-based lookup（O(1) 数组索引）+ `EnemyAISystem` 加 `_phaseMinionEvents` ConcurrentBag（顺序 + 并行 2 条路径阶段触发时入队 `(bossId, typeId, count, x, y)`）+ Update 末尾 `DrainPhaseMinionEvents()` 串行 drain 到 `WaveSpawningSystem.SpawnMinionNearPosition(typeId, count, x, y)` 新方法（复用 wave spawn 的 difficulty scaling + 12 个 archetype 字段初始化 + 1.5 单位 ring k*60° 确定性格局最多 8 个）+ One-shot 守卫复用 `EnemyPhaseFiredMask` bit（HP 恢复不重复触发）+ `SystemRegistry` wire `EnemyAI.SetWaveSpawningSystem(WaveSpawning)`（null 安全方便单测）+ 17 测试覆盖默认值/边界访问器/Setter clamp/BossPhaseDef 字段默认 0/GetMonsterConfigByTypeId 越界 & 有效/SpawnMinionNearPosition 0 count / invalid typeId / valid 2-minion spawn 在 ring 位置/DrainPhaseMinionEvents null 引用也安全/重复 phase 不重复 summon + 494/494 tests PASS + Claude bug scan PASS 0 bug)；bench2: 8709, bench4: 3727, bench5: 3256 ⚠️
- 自适应刷怪数量 Adaptive Spawn Count / Rubber-band Spawn Pacing（Round 120 方向3：Core/GameConfig.cs + Core/SystemRegistry.cs + Systems/WaveSpawningSystem.cs + Systems/AdaptiveDifficultySystem.cs + BattleSystemECS.Tests/AdaptiveSpawnCountTests.cs。1 新增静态类 `AdaptiveSpawnConfig`（4 常量 `DefaultSpawnSensitivity=0.5f` / `MinSpawnMultiplier=0.5f` / `MaxSpawnMultiplier=2.0f` / `ApplyToMidWaveSpawns=true`）+ 1 新增字段 `_performanceSpawnMultiplier` 在 `WaveSpawningSystem`（默认 1.0=零开销 fast path）+ 1 新增公开方法 `SetPerformanceSpawnMultiplier(multiplier)`（clamp [0.5, 2.0] + near-1 snap 1e-4 → 精确 1.0 保持 hot-path 零开销）+ 1 私有 helper `ApplySpawnMultiplier(int baseCount)` 中心化 3 处 spawn site 共享舍入策略 + 1 新增字段 `WaveConfig.ExpectedKillCount`（int，默认 0=opt-out 向后兼容旧 JSON）+ `AdaptiveDifficultySystem.OnWaveComplete` 签名扩展可选参数 `expectedKills=0`（默认 0=旧调用方零影响）+ 仅当 `expectedKills > 0 && sensitivity > 0` 时计算 `multiplier = 1.0 + (kills - expectedKills) / expectedKills * sensitivity` 并写入 WaveSpawning（kills 已在 _killsThisWave zeroed 之前 snapshot，零 read-after-reset bug）+ 3 处 spawn site 全部应用 multiplier：`InitMultiTypeState`（per-type count）/ `InjectExtraEnemies` 伏击 / `SpawnMinionNearPosition` Boss 阶段召唤（后两者由 `ApplyToMidWaveSpawns` 守卫，可一键关停）+ `SetLevel` 重置 multiplier 到 1.0（每关独立计算）+ 新 back-reference `AdaptiveDifficultySystem.SetWaveSpawningSystem(WaveSpawning)` 在 `SystemRegistry` 与现有 `WaveSpawning.SetAdaptiveDifficulty(AdaptiveDifficulty)` 配对 wire + 21 测试覆盖 `AdaptiveSpawnConfig` 不变量 / `WaveConfig.ExpectedKillCount` 默认 0 / `PerformanceSpawnMultiplier` 默认 1.0 / Setter clamp max+min / near-1 snap / Over-kill 30/20 → 1.25 / Under-kill 5/20 → 0.625 / 0 kills / 100 expected → clamp 0.5 / 1000 kills / 1 expected → clamp 2.0 / expectedKills=0 跳过 scaling / `SetLevel` 重置 / `InjectExtraEnemies` 2x → 6/3 / multiplier=1.0 → 不变 / 0.5x → 2/4 + 509/509 tests PASS + Claude bug scan PASS 0 bug）；bench2: 8862, bench4: 3580, bench5: 3320 ⚠️
- 运行时路径分支 Runtime Path Branching at Junction（Round 121 方向1：Core/GameConfig.cs + Core/ComponentStore_Enemy.cs + Systems/PathfindingSystem.cs + Systems/EnemyMovementSystem.cs + Data/Configs/path_junctions.json + BattleSystemECS.Tests/RuntimePathBranchingTests.cs。1 新增配置类 `JunctionDef`（SourcePathId/NodeIndex/Policy/HpLongPathThreshold/TowerDensityRadius/TowerDensityShortPathThreshold/BossTypeTags/ShortPathId/LongPathId 9 字段）+ 1 新增 enum `JunctionPolicy`（HpBased=0/TowerDensityBased=1/TypeBased=2 三值）+ 1 新增 SOA 数组 `EnemyPathSegmentStartIndex[]`（int, 0=fresh path, ResetEntity 显式重置防 ID 复用泄漏）+ `PathfindingSystem` 加 `_junctions` Dictionary<long,JunctionDef>（key = (sourcePathId<<32)|nodeIndex O(1) lookup）+ `_hasJunctions` 快速退出 flag + 3 公共方法 `AddJunction/ClearJunctions/GetJunction` + 1 静态纯函数 `EvaluateJunction(def, hp, maxHp, isBossType, towerCount)`（零副作用可独立单测；null def 返回 -1；maxHp=0 防除零；未知 policy 安全 fallback ShortPath）+ `EnemyMovementSystem.SetTurn` 缓存 `_pathfindingHasJunctions`（O(1) early-out 未配置时单 bool 早退）+ per-enemy Parallel.For 在 path terrain derivation 之后、stun check 之前插入 junction 评估块：`curNode > segStart` 时查 `GetJunction(curPath, curNode)` 命中则 `EvaluateJunction` 决策新 path id 写回 `EnemyPathId/EnemyPathNodeIndex/EnemyPathSegmentStartIndex`（segStart=0 触发下一段）+ 2 新增 helper `CountTowersNearEnemy(x, y, radius)`（O(ActiveTowerIds) 平方距离扫描并行安全只读）+ `IsBossEnemy(enemyId)`（`EnemyIsElite || EnemyBossPhase > 0`，无 `EnemyIsBoss` 字段）+ 新数据文件 `Data/Configs/path_junctions.json` 含 2 个示例 junction（默认路径 y=15 HP-based + y=10 Tower-density-based）+ 22 测试覆盖 JunctionDef 默认值 / JunctionPolicy 3 值互异 / `HasJunctions` 默认 false / `AddJunction(null)` no-op / `GetJunction` 命中/未命中/Clear 后 null / 重复 Add 覆盖 / 多 junction 独立 / `EvaluateJunction(null)` 返回 -1 / HpBased 高/低 HP 双向 / HpBased maxHp=0 防除零 / TowerDensityBased > threshold 短 ≤ 长 / TypeBased boss/非 boss / 未知 policy fallback / `EnemyPathSegmentStartIndex` 默认 0 / round-trip 写读 + 531/531 tests PASS + Claude bug scan PASS 0 bug）；bench2: 8438, bench4: 3695, bench5: 3177 ⚠️
- 塔-塔治疗链接 Tower-to-Tower Heal Link / Tower Healing Aura（HealAuraSystem + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SkillBuffGroup.cs + Core/SystemRegistry.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/HealAuraSystemTests.cs）；bench2: 8910, bench4: 3487, bench5: 3244
- Boss 路径 AoE 轨迹 Boss Path AoE Trail（Systems/BossTrailAoeSystem.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/EnemyMovementSystem.cs + Systems/WaveSpawningSystem.cs + Core/SystemRegistry.cs + BattleSystemECS.Tests/BossPathTrailAoeTests.cs，6 新增 SOA 字段 EnemyIsBossTrail / EnemyBossTrailRadius / EnemyBossTrailDamage / EnemyBossTrailSlow / EnemyBossTrailProgressInterval / EnemyBossTrailLastTriggerProgress 全部 0f/false 默认 = 零开销 fast path + 4 MonsterConfig 字段 BossTrailProgressInterval/Radius/Damage/Slow 默认 0f + 构造 + DestroyEntity + _ResetEntity 6 字段 reset 防 ID 复用泄漏 + BossTrailAoeSystem.TryQueueTrail 在 Parallel.For 内 lock-free 写入 per-thread List<BossTrailEvent>（ThreadLocal trackAllValues=true 与 R119 阶段事件同模式）+ EnemyMovementSystem Update 末尾 ResolveTrailEvents 串行 drain 应用（a）范围内玩家扣血 + (b) 范围内其他敌人 1 帧 slow（0-1 因子经 ApplyEnemySlow 写入 EnemySlowDurationLeft）+ 7 测试覆盖默认 0=无 trail / 进度增量≥interval 触发 / 进度未增量不触发 / 自伤排除 / 静止不触发 / trail damage 应用 / 范围外不伤 / EnemyIsBossTrail 守卫）；bench2: 8009, bench4: 3631, bench5: 3227 ⚠️
- 荆棘光环 / 被动伤害光环 Thorns Aura / Passive Damage Aura（ThornsAuraSystem + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Core/SkillBuffGroup.cs + Core/SystemRegistry.cs + Systems/TowerPlacementSystem.cs + Data/Towers/all_towers.json + BattleSystemECS.Tests/ThornsAuraSystemTests.cs，5 新增 SOA 字段 TowerIsThornsTower / TowerThornsRadius / TowerThornsDps / TowerThornsInterval / TowerThornsTimer 全部 0f/false 默认 = 零开销 fast path + 4 TowerConfig 字段 IsThornsTower / ThornsRadius / ThornsDps / ThornsInterval 默认 0f/false + 构造 + AddTower + DestroyEntity + _ResetEntity 5 字段 reset 防 ID 复用泄漏 + ThornAuraSystem.SetTurn 缓存 thorns-emitter tower IDs 列表 + Update 单 pass tick+fire+reset 逻辑（interval=0 触发每帧 dps×dt 连续模式 / interval>0 触发每 cycle dps 突发模式）+ 范围 O(n) 平方距离扫描 + 跳过 invuln 敌人 + HP 跨 0 时 QueueEnemyDeath + 多塔加性叠加 + _thornsAuraPlayerId stash 在 WireDependencies 后由 AssignToGroups 写入 scheduler.SkillBuff.ThornsAuraPlayerId + SkillBuffGroup 在 HealAura 之后插入 ThornsAura.SetTurn+Update 调用 + SystemRegistry 创建实例 + 4 reset 路径一致 + 14 测试覆盖默认 0 / AddTower 默认 0 / DestroyEntity reset / SetTurn 早退 / Update 早退 / 范围内扣血 / 范围外不伤 / 死敌不重扣 / invuln 跳过 / 跨 0 排队死亡 / 多塔加性 / 间隔门控 / 连续累积容差 / 半径 0 防御性 inert）；bench2: 7973, bench4: 3573, bench5: 3207 ⚠️
- 成长曲线 Curve-based Stat Progression（Round 127 方向1：Core/CurveTable.cs + Systems/WaveSpawningSystem.cs + Data/Configs/curves.json + BattleSystemECS.Tests/CurveTableTests.cs。1 新建 `Core/CurveTable.cs`（静态全局曲线注册表 CurveType 6 值 Identity/Linear/Exponential/Logarithmic/Sigmoid/Piecewise + CurveDef 类 5 字段 Id/Type/Coefficient/Midpoint/Steepness + ControlPoints 排序 + 5 形状 Evaluate 数学：Linear 1+(x-1)·coef 与 legacy 一致，Exponential coef^(x-1) 钳 exponent≤60 防溢出，Logarithmic 1+ln(x+1)·coef 前置增长，Sigmoid 1+coef/(1+exp(-k(x-mid))) 钳 z∈[-60,60]，Piecewise 控制点间线性插值+端点 clamp + 线程安全 Load 懒门控 + Register/Evaluate/Count/Get API + 缺失 id 返回 1.0f 防 hot-path 崩 + ParseCurve 错误隔离不抛 + ResetForTests 单测钩子）+ 4 新增 `DifficultyConfig` 字段 HealthCurveId/DamageCurveId/ArmorCurveId/SpeedCurveId 全部 ""=legacy 零开销（empty 字符串走 fast path 返回与旧 `1+(wave-1)·growth` 完全相同的 1.0f）+ 4 私有 helper `GetHealthMult/GetDamageMult/GetArmorMult/GetSpeedMult` 中心化 "use curve if set, else linear" 分支 + 4 处 WaveSpawningSystem spawn site 全部接入：Update 主路径 / InjectExtraEnemies 伏击 / InjectMiniBoss Boss rush / SpawnMinionNearPosition 阶段召唤 + `LoadWaveSpawnConfig` 解析 healthCurveId/damageCurveId/armorCurveId/speedCurveId 4 字段 + `curves.json` 提供 9 个预设（identity/healthDefault/.../healthExponential/healthLogarithmic/healthSigmoid/bossPlateau Piecewise 5 控制点）+ 27 测试覆盖 Identity 恒 1 / Linear 与 legacy 公式逐位一致 / Exponential 1.04^4≈1.17 / Exponential 零系数/1000 防溢出 / Logarithmic ln 前置 / Sigmoid mid=1+coef/2 / Sigmoid 端点渐近 / Piecewise 空/单点/插值/clamp 双向 / Get 未知返回 null / Evaluate 未知返 1 / null/empty 返 1 / Register round-trip / 重名覆盖 / null/empty register no-op / ResetForTests 清空 / 关键回归 wave-1 必为 1.0 与旧行为零差异 / Linear vs Exponential 数值互异 / Count 反映注册 + 修复 2 Claude bug scan 发现 bug：1）`CountCurves()` 私有 helper 探测永不存在的 __probe_N__ id → 改用新增 `CurveTable.Count` 公开 API 2）`Get()` 锁无关读 + 注释错误声称 "dictionary reads are safe for readers once written" 不符合 .NET 内存模型 → 改为 `_loadLock` 包裹读保证 publisher 可见性 + 617/617 tests PASS）；bench2: 8317, bench4: 3398, bench5: 3145 ⚠️
- 燃烧路径 / 焦土 Fire Trail / Burn Ground（Round 128 方向5：Systems/FireTrailSystem.cs + Systems/TowerAttackSystem.cs + Core/SystemRegistry.cs + BattleSystemECS.Tests/FireTrailSystemTests.cs。1 新建 `FireTrailSystem` 薄包装 API：`SpawnTrail(x, y, radius=1.5, dps=8, duration=2, tickInterval=0.5)` 内部转 `ComponentStore.AddCorpseEffect(effectType=3=fire, ...)` 复用已有 CorpseEffect 槽位（无新 SOA 字段、无新 buff 位、无新 Update 帧循环，零分配被动 API）+ 防御性参数 clamp（radius>50→50 / duration>30→30 / tickInterval<=0→0.25）+ 拒绝非法输入（radius<=0 / duration<=0 / dps<0 / storage 满）返回 -1 + `TotalSpawned` / `TotalFailedFull` 观测计数 + `TowerAttackSystem` 注入 `_fireTrailSystem` 字段 + `SetFireTrailSystem()` 注入器（null 安全）+ Firewall case 在 burn DoT + stun 之后插入 `_fireTrailSystem?.SpawnTrail(enemyX, enemyY, 1.5, 8, 2, 0.5)` —— 串行 debuff 阶段（Phase 3）调用，不在 Parallel.For 内 + `SystemRegistry` 新增 `FireTrail` 属性 + 在 ThornsAura 之后创建 `new FireTrailSystem(store)` + `WireDependencies` 末尾 `TowerAttack?.SetFireTrailSystem(FireTrail)` 注入 + 15 测试覆盖 null store throw / 默认 0 计数 / 默认参数产生 fire-3 槽位 / 自定义参数 / 多 spawn 不同槽 / 计数累加 / 零/负 radius/duration/dps 拒绝 / 超大 radius 50 clamp / 超大 duration 30 clamp / 零 tickInterval 0.25 兜底 / 满载返回 -1 + 计数 / 满载重复 5 次累加 / 远距离坐标不崩）；bench2: 8500, bench4: 3626, bench5: 3087
- 恢复完成：Boss 阶段转换事件 Boss Phase Transition Event（Round 129 方向2：Core/GameEvents.cs + Systems/EnemyAISystem.cs + BattleSystemECS.Tests/BossPhaseEventTests.cs。1 新增 `GameEvents.BossPhaseChanged` 常量 "boss_phase_changed" + 1 新增 DTO `BossPhaseChangedEvent` 6 字段（EnemyId / BossTypeName / OldPhase / NewPhase / HealthFraction / Turn）+ 1 新增 `ConcurrentBag<BossPhaseChangedEvent> _phaseChangeEvents` 字段 + 顺序段（activeCount < parallelThreshold）+ 并行段（Parallel.For）2 条 Boss 阶段触发路径均入队事件 + 顺序段 `EnemyBossPhase` 写入改用 `Interlocked.CompareExchange` 原子 CAS（防止 2 线程同时读 currentPhase 都写新值导致双 push）+ 并行段 `EnemyPhaseFiredMask` 写入改用 `Interlocked.CompareExchange` 原子 CAS（防止 2 batch 同时读到 firedMask=0 都 push 重复事件 + 都 apply speed/damage mult 双倍生效）+ `Update()` 末尾新增 `DrainPhaseChangeEvents()` 串行 drain（`TryTake` + `_eventBus.Publish` + 解析 `store.GetEnemyTypeName` 填 BossTypeName + 空字符串归一化 null）+ 2 观测计数 `PhaseChangeDrainCount` / `PhaseChangePublishCount` + 11 测试覆盖 BossPhaseChanged 常量非空 / DTO 6 字段 round-trip / 默认值安全 / DrainCount 默认 0 / 空 bag 不抛异常 / 多次 drain 累计 / event payload 身份保持 / 订阅器 6 字段一致 / BossTypeName 串行解析 / 空 BossTypeName 归一化 null + Claude bug scan 修复 2 项并发 bug：1）`firedMask|bit` 非原子写入可被 2 batch 同时观察到 bit 未置位导致双 push 2）`EnemyBossPhase[currentPhase] = newPhase` 非原子写入可被 2 线程同时读到 currentPhase 都执行新值 + 644/644 tests PASS）；bench2: 8654, bench4: 3470, bench5: 3156 ⚠️
- 恢复完成：物品栏 / 道具系统 Inventory / Item System（Round 130 方向6：Systems/InventorySystem.cs + Core/ComponentStore_Player.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SystemRegistry.cs + Data/Configs/items.json + BattleSystemECS.Tests/InventorySystemTests.cs。1 新建 `InventorySystem` 公共 API：`AddItem(playerId, itemTypeId)` stack 到首个未满槽（尊重 MaxStack）/ `UseItem(playerId, slot)` 弹出一个 + dispatch 到 8 种 ItemType 效果 / `GetItemName(itemTypeId)` / `GetSlotCount(playerId)` / `IsFull(playerId)` / 8 种 ItemType 分发（Heal→clamp MaxHealth、Mana→clamp MaxMana、Shield→加 + 持续、SpeedBoost→SlowFactor 1.5 + 计时、DamageBoost→AttackBoost flag + own PlayerDamageBoostDuration 不与 Slow 共享 + 新 PlayerDamageBoostMultiplier 持久化 per-item magnitude、AoEBurst→O(n_enemies) 平方距离扫 + QueueEnemyDeath 跨 0、Cleanse→log only MVP、Summon→log only TODO）+ 5 新增 SOA 字段（`PlayerInventoryItemId[PLAYERS*SLOTS]` int/-1 默认 0 初始化 + `PlayerInventoryCount` int/0 + `PlayerInventoryUsed` int/0 = O(1) 满检测缓存 + `PlayerInventoryUsedTotal` int/0 lifetime + `PlayerDamageBoostDuration` int/0 own field not shared with Slow + `PlayerDamageBoostMultiplier` float/0 magnitude）+ ComponentStore 4 accessor `ResetInventory/GetInventoryItemId/GetInventoryCount/GetInventoryUsed` + 1 静态 helper `InventoryIndex(playerId, slot)` 越界返 -1 sentinel + `AddPlayer` 调 `ResetInventory` + `_ResetEntity` 在 chain 末尾 4 行 zero PlayerDamageBoostDuration/Multiplier 防 ID 复用泄漏 + `GameConfig.ItemDefs` 数组 + `GameConfigLoader.LoadItemDefs` 解析 `Data/Configs/items.json` 8 字段（Type/Name/ItemType/Value/BuffDuration/Radius/MaxStack）+ `ParseItemType` 字符串→enum + 异常隔离到 renderer.Log + `SystemRegistry.Inventory` 属性 + `SystemRegistry.Initialize` 在 Pickup 之后 `new InventorySystem(store, config, logger)` 注入 + `items.json` 7 预设（healing_potion/mana_potion/shield_sigil/speed_tonic/rage_draught/grenade/cleanse_scroll）+ 28 测试覆盖 9 例 enum dispatch + AddItem stacking/MaxStack/FIFO 同 type 合并/异 type 拒绝/full 拒绝/全填 8 槽后 add 返 false/UseItem 全 8 type 行为/InventoryIndex sentinel sentinel sentinel 默认 0 slot 7 边界/AddPlayer calls ResetInventory/被动的 speed boost 不动 DamageBoost 计时/InventorySystem 不破坏 ECS reset 状态 + Claude bug scan 修复 3 bug：1）`PlayerDamageBoostDuration` 在 `_ResetEntity` 漏 zero 导致 ID 复用泄漏 stale buff timer 2）DamageBoost 写 PlayerSlowDuration 而非 own field 导致 SpeedBoost→DamageBoost 互踩 + `def.Value` 仅写入 log 而未持久化导致每件物品实际效果相同 → 新增 `PlayerDamageBoostMultiplier` 字段 + max() 升级保证 + 3）`InventoryIndex` 越界 clamp 静默映射到 player 0 → 改为返 -1 sentinel 防止无意识污染 单元 0 + 680/680 tests PASS）；bench2: 8936, bench4: 3631, bench5: 3155 ⚠️

### 2026-06-04
- 塔射弹偏转 Projectile Deflection（Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/ProjectileSystem.cs + Systems/WaveSpawningSystem.cs + Data/Monsters/monster_doom_mech.json + Data/Configs/projectile_deflection.json，1 SOA 字段 EnemyDeflectChance (float, 0f=零开销) + AddEnemy 默认 0f + SetDeflectChance enemyId 访问器 System.Math.Clamp [0,1] + MonsterConfig.DeflectChance 字段 + GameConfigLoader ParseMonsterConfig 解析 DeflectChance 字段 + WaveSpawningSystem 3 处 spawn 路径全数调用 SetDeflectChance + ProjectileSystem.ResolveHit 入口 deflect roll：rng.NextDouble() < deflectChance → early-return 不入 damage queue 不触发 pierce/thorns/fragment 副作用 + System.Random 实例化在 ProjectileSystem._deflectRng 局部封装 + _damageQueueLock 包裹保证线程安全 + Doom Mech Boss 原型 0.2 偏转率 20% 射弹被弹开）；bench2: 9822, bench4: 4171, bench5: 3428 ⚠️
- 路径地形成本 Path Tile Cost（恢复完成，Round 85 方向3：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore_World.cs + Systems/EnemyMovementSystem.cs + BattleSystemECS.csproj + Data/Configs/path_terrain.json，MAX_PATH_NODES=32 常量 + 2 SOA 字段 EnemyPathTerrainSpeedMult/EnemyPathTerrainDmgMult (float, 1f=neutral 零开销) + PathNodeTerrain[MAX_PATH_NODES] (int, 0=neutral) + AddEnemy 默认 1f/1f 防 float[] 0f 冻结敌人 + 访问器 SetPathNodeTerrain/GetPathNodeTerrain 边界安全 (uint)nodeIdx >= (uint)MAX_PATH_NODES 早退 + EnemyMovementSystem 每帧按 EnemyPathNodeIndex[enemyId] 派生 per-enemy mults switch(tag): 1=Slow -25% speed / 2=Boost +25% speed / 3=Snow +15% dmg taken / 4=Heal reserved / 5=Wall speed=0 / 0=neutral + 移动相位 moveSpeed *= EnemyPathTerrainSpeedMult 乘性叠加 TerrainMoveSpeedMult + ApplyEnemyDamage 入口 pathDmgMult>1.0001f 时 damage *= pathDmgMult (Snow) 跳过 shield-absorb 边界 + JSON 配置 DefaultNodeTerrains 全 0 默认零行为变化 + 默认 level 未填非零 tag → 系统 hook 已就位等 designer 启用）；bench2: 9876, bench4: 4104, bench5: 3657
- 敌怪互相对抗 Enemy Infighting（恢复完成，Round 85 方向7：Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/EnemyAISystem.cs + Systems/WaveSpawningSystem.cs + Data/Monsters/swarm_minion.json，2 SOA 字段 EnemyFactionId (int, 0=免疫) + EnemyInfightCooldown (float, 0f=ready) + 1 全局 int 懒门控 FactionInfightEnabled (0=零开销) + DestroyEntity/AddEnemy 默认 0/0f + 访问器 SetFactionId enemyId (Math.Max 0 防负) + SetInfightCooldown enemyId (Math.Max 0f) + MonsterConfig.FactionId 字段 + GameConfigLoader ParseMonsterConfig 解析 FactionId 字段 + WaveSpawningSystem 3 处 spawn 路径全数调用 SetFactionId + FactionId>0 时同步设 FactionInfightEnabled=1 + ResolveFactionInfighting O(N²) 同阵营近战互殴 5% maxHp 双方 + 0.5s 冷却防 spam + ApplyEnemyDamage 走 shield/buff 链路 + hasAnyFaction O(N) 早退 + 末帧 O(N) cooldown 衰减 → opt-in 0 怪物时整段跳过单 int 比较零开销 + Swarm Minion 原型 FactionId=1 启用）；bench2: 9848, bench4: 4076, bench5: 3671
- 塔伤害饱和度 Damage Saturation（Round 92 方向1：Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + Data/Configs/damage_saturation.json，2 SOA 字段 EnemyRecentDamageSum (float, 0f=零开销) + EnemyRecentDamageFrame (int, 0=零开销) + AddEnemy 默认 0f/0 + DamageSaturationConfig 静态配置类 3 字段 SaturationWindowFrames=30 / SaturationThresholdMult=2.0 / SaturationScaleMult=0.1 + GameConfigLoader.LoadDamageSaturationConfig JSON 解析 Enabled+3 字段 + TowerAttackSystem 帧末 damage apply 阶段 O(1) 早退：satWindow>=0 && finalDmg>0.01f 时 lazy-expire (currentFrame-lastFrame>window) reset sum + 累加 newSum + 若 newSum>maxHp*threshold 则 finalDmg*=scaleMult + PlayerTowerAttackSystem 镜像逻辑 + Claude bug scan 修复 ComputeLinkedDamage 入参 damage→finalDamage 与 TowerAttackSystem 行为一致 + 防单塔统治 Boss/精英，迫使玩家搭配塔型）；bench2: 10128, bench4: 3795, bench5: 3604 ⚠️（恢复轮，重新实测更新）
- 关卡可破坏物 Destructible Obstacles（Round 85 方向5：Core/ComponentStore_World.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/GameManager.cs + Systems/TowerAttackSystem.cs + Data/Configs/destructibles.json + Data/Levels/level_1.json，2 SOA 字段 ObstacleOnDestroyEffect (int, 0=无效果零开销) / ObstacleOnDestroyValue (float, 0f) + AddObstacle 签名扩展 onDestroyEffect+onDestroyValue 末两参 + RemoveObstacle 重置两字段 + DestructibleDef 配置类 (Id/Name/MaxHealth/OnDestroyEffect/OnDestroyValue/ExplosionRadius) + DestructiblePlacement (DefId/X/Y) + LevelConfig.Destructibles List + GameConfig.DestructibleDefs 数组 + GameConfigLoader.ParseDestructiblePlacements 自写 JSON 解析（兼容方括号位置格式）+ LoadDestructibleDefs 加载 Data/Configs/destructibles.json（缺文件→opt-in 零开销）+ GameManager.SpawnDestructiblesForLevel 在 resourceNodeSystem.InitializeFromLevel 之后逐条 DefId 查表找空 slot 调 AddObstacle + TowerAttackSystem 阶段 1（并行）敌人 fallback 分支：bestTarget==-1 时扫 ActiveObstacleIds 找最近 HP>0 障碍物（在 range 内）→ 负数 sentinel 编码 obstacleId 到 damage queue（obstacleId = -(enemyId+1)）+ 阶段 2（串行）sentinel 路由：enemyId<0 走 destructible damage apply → HP<=0 时按 OnDestroyEffect 触发：1=Gold 玩家金币+value / 2=Explosion 5 格半径内敌人扣 maxHp*value 比例 + QueueEnemyDeath 触发死亡 + RemoveObstacle 收尾 + 油桶 50% maxHP 爆炸/木箱 15g 掉落原型 level_1 配置 3 木箱 + 1 油桶示范）；bench2: 10161, bench4: 4018, bench5: 3550
- 塔建造队列 Build Queue（Core/ComponentStore_Player.cs + Core/GameConfig.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/TowerPlacementSystemTests.cs，1 SOA 数组 PlayerBuildQueue[160] (BuildQueueSlot struct: X/Y/TowerType/Damage/Range/Speed/Cost/Active) + 1 SOA 字段 PlayerBuildQueueCount[MAX_PLAYERS] (0..16) + PlayerBuildQueueTimer[MAX_PLAYERS] (float) + 1 const MAX_BUILD_QUEUE=16 + 1 配置属性 BuildQueueInterval=0.2f + EnqueueBuild/ProcessBuildQueue/ClearBuildQueue/GetBuildQueueCount 4 API + CompactQueue O(16) 维护 FIFO 头部 + 7 测试 Enqueue/Clear/Process/Bounds/Skip/Pacing/Overflow/FIFO 覆盖全路径 + 帧末 pre-deduct gold → PlaceTower → CompactQueue 路径，gold 不足早退保留队列其余 + Claude bug scan 修复 ref slot 提前 snapshot 字段避免 slot=default 后 log 全 0 + 允许 BuildPhase 玩家预排 16 塔位 WavePhase 自动按 0.2s 节流 PlaceTower 避免卡顿）；bench2: 9417, bench4: 3990, bench5: 3568
- 单格唯一塔 Tile Occupancy Cache（Round 95 方向4：Core/ComponentStore.cs + Systems/TowerPlacementSystem.cs + Systems/TowerRelocateSystem.cs + BattleSystemECS.Tests/TowerPlacementSystemTests.cs，1 SOA 二维 bool 数组 TileOccupied[10,20] + 2 const TILE_GRID_DEFAULT_WIDTH/HEIGHT + 2 int 字段 TileOccupiedWidth/Height + IsTileOccupied/SetTileOccupied O(1) API 边界安全 (uint)x/y >= (uint)Width/Height 早退 + ResizeTileOccupancy(w,h) 重建并清空 + DestroyEntity Tower 分支读取 PositionX/Y → SetTileOccupied(false) 释放占用格（读取在 zeroing 之前避免读到 0）+ PlaceTower 入口 IsTileOccupied 早退 + 旧 ActiveTowerIds 扫描保留为防御性 fallback + AddPosition 后 SetTileOccupied(true) 写入 + ValidatePlacementPosition/RelocateTower/IsValidPosition 同样切到 cache-first 模式 + 4 个调用点 Place/Validate/Relocate(TowerPlacementSystem)/IsValidPosition(TowerRelocateSystem) + 8 测试 Defaults/OutOfBounds/MarksTile/RejectsOccupied/SellReleases/RelocateUpdates/RelocateRejects/ResizeClears 覆盖全路径 + Claude bug scan PASS 0 bug + 简化协同计算避免双塔叠加范围）；bench2: 9573, bench4: 4058, bench5: 3633 ⚠️
- 玩家停用塔 Disable Tower（Round 96 方向2：Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Systems/TowerAttackSystem.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/TowerPlacementSystemTests.cs，1 SOA bool 数组 TowerPlayerDisabled[MAX_ENTITIES]（默认 false 零开销）+ AddTower/RemoveTower/DestroyEntity 三处重置为 false 防 ID 复用时状态泄漏 + ToggleTower(towerId) API 在 TowerPlacementSystem（边界检查 + 翻转 + 1=disabled/0=active/-1=error 返回码 + 日志）+ TowerAttackSystem 在既有 TowerIsDisabled 敌破坏防旁加 OR 门控（`if (store.TowerPlayerDisabled[towerId]) return;`）保持兼容 + 3 测试 StartsActiveThenDisables/ReenableFlipsBack/RejectsInvalidId + **区别于 SellTower**（永久销毁+退款）/**区别于 TowerIsDisabled**（敌方破坏自动计时清除），玩家停用是**持久 sticky** 状态不退款不计时，用于波次省电或高密度精控）；bench2: 10195, bench4: 3994, bench5: 3564
- 恢复：CC 免疫 Crowd Control Immunity（Round 97 方向3：Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/EnemyMovementSystem.cs + Systems/TotemSystem.cs + Systems/RandomEventSystem.cs + Systems/WispSystem.cs + Systems/TowerDemolishSystem.cs + BattleSystemECS.Tests/CCImmunityTests.cs，1 SOA 数组 EnemyCCImmuneMask[MAX_ENTITIES] (int, 0=无免疫零开销) + 6 bit Mask 常量 Mask_Slow=1/Stack_Stun=2/.../Mask_Stagger=32 + Mask_AllCC + Mask_BossDefault + IsCCImmuneTo/SetCCImmuneMask/SetCCImmuneBit/ClearCCImmuneBit 4 API + DestroyEntity 重置为 0 防 ID 复用泄漏 + ApplyPolymorph/ApplyEnemyStagger/ApplyEnemyStun/ApplyEnemyFreeze/ApplyEnemySlow 5 个 CC apply 入口 IsCCImmuneTo 门控 + 与 EnemyIsUnstoppable 全局免疫 OR 互斥 + EnemyMovementSystem 跳跃 stun 门控 + TotemSystem 图腾 stun 门控 + RandomEventSystem 事件 slow 门控 + WispSystem 精灵 slow 门控 + TowerDemolishSystem 元素效果 stun 门控 + 19 测试覆盖各 CC 类型免疫/组合位/未免疫正常吃 CC）；bench2: 9866, bench4: 3988, bench5: 3610 ⚠️（bench2 跌破阈值，热路径 IsCCImmuneTo 间接调用开销）
- 塔蓄力/前摇 Tower Windup / Pre-Cast（Round 98 方向9：Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/TowerAttackSystem.cs + BattleSystemECS.Tests/WindupSystemTests.cs，2 SOA 数组 TowerWindupFrames[MAX_ENTITIES] (int, 0=零开销) + TowerWindupCountdown[MAX_ENTITIES] (int, 0=零开销) + WindupConfig 静态类 4 常量 DefaultWindupFrames=0/MinWindupFrames=1/MaxWindupFrames=30/WindupInterruptOnCC=true + AddTower/RemoveTower/DestroyEntity 三处重置双字段防 ID 复用泄漏 + 顺手补 Round 96 DestroyEntity 漏的 TowerPlayerDisabled reset + TowerAttackSystem 入口 cooldown 检查通过后 windup 倒数逻辑（countdown==0 时设 countdown=frames 跳过本帧 → 后续帧 decrement → 倒数到 0 才 fire） + silence/sabotage-disable 命中正在 windup 的塔取消 in-flight windup + LastAttackTime=0 完整重置必须重新 cooldown + player-disabled 故意不取消 windup（玩家意图暂停而非取消）+ 5 测试 Defaults/ConfigConstants/RemoveTowerReset/DestroyEntityReset/RecycledSlotNoLeak 覆盖全路径）；bench2: 9654, bench4: 3893, bench5: 3467 ⚠️（bench5 跌破 3800 阈值，DestroyEntity 加 2 行 reset 路径长度 + Windows 调度抖动）
- 难度曲线/威胁评分 Threat Score / Dynamic Difficulty Scaling（Round 99 方向5：Core/ComponentStore_World.cs + Core/FrameScheduler.cs + Core/GameConfig.cs + Systems/PlayerTowerAttackSystem.cs + Systems/TowerAttackSystem.cs + Systems/WaveSpawningSystem.cs + BattleSystemECS.Tests/ThreatScoreTests.cs，2 SOA 字段 PlayerRecentDPS (float[MAX_PLAYERS], 0f=零开销) + PlayerDPSAccumulator (float[MAX_PLAYERS], 0f=零开销) + ThreatScoreConfig 静态类 4 常量 DPSWindowSec=5.0f/ThreatScalingRate=0.0001f/MaxThreatMultiplier=3.0f/MinThreatMultiplier=1.0f + FrameScheduler.RunWavePhase 末 Phase 12 DecayAndAccumulateThreatScore 用 EMA 半衰期公式 alpha=1-exp(-ln(2)*dt/halfLife) 帧率独立衰减 + PlayerDPSAccumulator 按 1/dt 转换为 DPS 后混入 EMA（dt=0 时 invDt=0 守卫 BuildPhase 早退）+ PlayerTowerAttackSystem serial phase `if (finalDamage > 0) PlayerDPSAccumulator[playerId] += finalDamage` 累加 + TowerAttackSystem serial phase 镜像累加 + WaveSpawningSystem spawn 时按 `1 + recentDps × ThreatScalingRate` 缩放 enemy.Health/MaxHealth（clamp MaxThreatMultiplier=3.0f + MinThreatMultiplier=1.0f 永不减难度 + threatMult>1 才有实际 *= 早退） + 7 测试 PlayerRecentDPSDefaults/ConfigConstants/ZeroDPSEqualsOne/HighDPSCapped/ReasonableDPSLinear/EMADecayTowardZero/AccumulatorResets/EndToEndSpawnScaling 覆盖全路径 + Claude bug scan 修复 1）EMA 未做 1/dt 转换导致 steady-state 比预期 60× 弱（实际是 frame-counted 不是 time-counted）2）p<10 硬编码换成 store.PlayerRecentDPS.Length 跟随 MAX_PLAYERS 防未来扩容越界 + 防 BuildPhase 玩家 DPS 优化导致 trivial 关卡保持挑战曲线恒定）；bench2: 9583, bench4: 4009, bench5: 3543
- 恢复：路障/城墙塔 Palisade Tower（Round 100 方向6：Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/TowerType.cs + Core/GameConfig.cs + Systems/EnemyMovementSystem.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/PalisadeSystemTests.cs，7 SOA 字段 TowerIsPalisade (bool, false=零开销) / PalisadeStunFrames (int) / PalisadeBlockRadius (int) / PalisadeHP (float) / PalisadeMaxHP (float) / PalisadeContactDamageAccumulator (float, 0f=并行安全累加) / PalisadeDestroyFlag (bool, false=并行安全标志) + 1 全局 int 计数器 ActivePalisadeCount (0=零开销) + PalisadeConfig 静态类 4 常量 DefaultPalisadeStunFrames=18/DefaultPalisadeBlockRadius=1/DefaultPalisadeHP=100/EnemyContactDamageToPalisade=5 + TowerType.Palisade=9 枚举（控制型塔，0 攻击）+ AddTower/DestroyEntity 三处重置所有 palisade 字段防 ID 复用泄漏 + PlaceTower(type=Palisade) 入口设 TowerIsPalisade=true + 5 个字段填充 + ActivePalisadeCount++ + TowerPlacementSystem 入口检测 type==Palisade 分支 + EnemyMovementSystem SetTurn snapshot ActiveTowerIds + Update Parallel.For 内 ActivePalisadeCount>0 守卫 + Chebyshev 距离 ≤ radius 3x3 区域检测（flying enemy skip）+ CC 免疫（Round 97 Mask_Stun 位）门控 + 命中时复用现有 EnemyStunDurationLeft 字段写入 stun 帧（Math.Max 不延长进行中 stun）+ PalisadeContactDamageAccumulator 累加 + PalisadeDestroyFlag 标志（**Claude bug scan 修复 2 项并发 bug**：原 HashSet<int> _palisadeDestroyQueue 非线程安全 + 原 PalisadeHP 在 Parallel.For 内 RMW 竞争 → 改为 accumulator + bool[] flag 后 PASS） + 串行段（ActiveTowerIds 扫描）原子地 PalisadeHP -= accumulator + DestroyEntity 处理 ActivePalisadeCount-- + 8 测试 Enum/Config/PlacePalisade/PlaceNonPalisade/ZeroInit/DestroyEntityReset/TileOccupancy 覆盖全路径 + 取代固定路障，提供 Chebyshev 距离触发的 stun 区域）；bench2: 9702, bench4: 3980, bench5: 3381 ⚠️
- 每日挑战 Daily Challenge / Rotating Seed（Round 105 方向9：Core/DailyChallengeSystem.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/GameManager.cs + Program.cs + Data/Configs/daily_modifiers.json + BattleSystemECS.Tests/DailyChallengeTests.cs，FNV-1a 32-bit 日期 hash + xorshift32 seed-select 选不同 modifier 索引（栈式 64-bit bitmask / 池 > 64 走 bool[] 路径）+ ResolveForDate 纯函数（同日同 seed / 跨日不同 seed / 池空 no-op / count clamp 边界）+ ApplyToConfig 聚合乘加（dmg *=、gold *=、enemyHp *=、startGoldBonus +=）+ GameConfig 新增 DailyModifierPool 列表 + DailyModifierCount=3 + DailyLastResult snapshot + 4 聚合系数默认 1.0/1.0/1.0/0f 中性零开销 + GameConfigLoader LoadDailyModifierPool JSON 解析 5 字段（id/name/description/damageMult/goldMult/enemyHpMult/startingGoldBonus）+ ResolveDailyChallenge 启动期应用 + GameManager.PrintDailySummary 美化输出（stock run 提示 / modifier 列表 / 有效聚合系数）+ Program.cs 新增 `dotnet run daily` 入口 + 8 中文 modifier（玻璃大炮/坦克群/富贵开局/金币风暴/钢铁意志/快攻/法术风暴/经济压力）+ 19 测试覆盖 hash 确定性 / 同日同 seed / 跨日不同 seed / 池空 no-op / 池 null no-op / count>poolSize clamp / count=0 返回空 / 池单元素 / SeedSelectIndices distinct 索引 / 池大小 64+ 走 bool[] 路径 / ApplyToConfig 聚合乘加 / null result / null config / 空 Selected / DailyLastResult 写入 / 加载后 GameConfig 反射 / 字符串默认值 / FNV-1a 已知向量 / xorshift32 序列 / 启动 JSON 解析）；bench2: 9753, bench4: 3991, bench5: 3453 ⚠️
- 塔吸敌法 Mana Drain Tower → Enemy（Round 101 方向10：Core/ComponentStore_Enemy.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/TowerAttackSystem.cs + Systems/TowerPlacementSystem.cs + BattleSystemECS.Tests/ManaDrainSystemTests.cs，4 SOA 字段 EnemyMaxMana (float, 0f=零开销) / EnemyCurrentMana (float, 0f=零开销) / TowerManaDrainPct (float, 0f=零开销) / TowerManaDrainCap (float, 0f=零开销) + ManaDrainConfig 静态类 3 常量 DefaultManaDrainPct=0.1f/ManaDrainCap=50f/DefaultEnemyMaxMana=0f + TowerConfig 新增 ManaDrainPct/ManaDrainCap 字段（designer opt-in）+ AddEnemy 初始化两字段为 0 + AddTower 初始化两字段为 0 + DestroyEntity 两处（enemy + tower）重置防 ID 复用泄漏 + TowerPlacementSystem 入口 ManaDrainPct>0 桥接写入 SOA + TowerAttackSystem damage apply 阶段插入 drain 逻辑：四道零开销守卫（finalDmg<=0 / TowerManaDrainPct==0 / EnemyMaxMana==0 / EnemyCurrentMana==0）→ 命中时 drain = min(cap, curMana × pct) → 扣敌法 + 加玩家法力池（clamp PlayerMaxMana） + 12 测试覆盖默认值/Config 常量/ManaWielder/普通敌/百分比应用/global cap/per-tower cap/player clamp/zero-mana no-op/zero-cur-mana no-op/DestroyEntity reset + Claude bug scan PASS + 反向于 ManaBurnSystem 敌人→玩家单向防守，提供塔→敌进攻性法力偷取平衡法师塔施法循环）；bench2: 9259, bench4: 4080, bench5: 3378 ⚠️
- 玩家伤害转化 Damage Conversion（Round 102 方向7：Core/GameConfig.cs + Systems/PlayerTowerAttackSystem.cs + BattleSystemECS.Tests/DamageConversionTests.cs + Data/Configs/damage_conversion.json，0 新增 SOA 字段（opt-in 通过 GameConfig.PlayerDamageConversionRatio/PlayerConvertedDamageType 启用）+ GameConfig 新增 2 配置字段 PlayerDamageConversionRatio (float, 0f=零开销)/PlayerConvertedDamageType (DamageType, Physical=零开销) + DamageConversionConfig 静态类 2 常量 ConversionDefaultCap=0.5f 全局 cap / MinMeaningfulRatio=0.01f 零开销阈值 + PlayerTowerAttackSystem damage queue 从 3-tuple 扩到 4-tuple `(int enemyId, float damage, bool wasCrit, DamageType damageType)`（零额外分配，struct 仍 stack-friendly）+ 并行段提取 ApplyResistancesAndEnqueue 私有方法承担 resistance/immunity/exposure/death-mark 流水线 + convRatio<MinMeaningfulRatio 走 fast path 单事件，否则按 clamp(convRatio, 0, DefaultCap) 拆成 original + converted 两段独立 apply（双事件，各自走对应的 armor/magicResist/immunity 路径）+ 20 测试覆盖 Config 常量/GameConfig defaults/Default 零分配/Clamp-at-Cap/Above-threshold 双事件/低于阈值 fast-path/Physical 走 armor/Physical-immune 仍吃 Magic portion/Magic 走 magicResist/crit 沿用/DestroyEntity reset 兼容等 + 平衡物理输出塔对魔法免疫怪的可玩性，designers 可通过 JSON 配置任意玩家比例 + 转化类型组合）；bench2: 9655, bench4: 3923, bench5: 3362 ⚠️（恢复轮：bench2 仍 < 10000 阈值 + bench4 < 4300 + bench5 < 3800，但与上轮 9259/4080/3378 对比 bench2 +4.3% 改善、bench4/5 在噪声内，4-tuple queue 引入未观测到 hot-path 回归，4-tuple 元组从 24→32 byte 仍在 L1 缓存线内零分配）
- 队伍增益共享 Buff Share / Group Buff（Round 103 方向8：Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/TowerSynergySystem.cs + Core/CombatGroup.cs + BattleSystemECS.Tests/BuffShareSystemTests.cs，2 SOA 字段 TowerBuffShareRadius (float[MAX_ENTITIES], 0f=零开销 fast path) / TowerBuffShareMask (int[MAX_ENTITIES], 0=无共享) + 4 accessor Get/Set 对 + AddTower/RemoveTower/DestroyEntity 三处重置防 ID 复用泄漏 + BuffShareConfig 静态类 3 常量 MaxShareRadius=8f/DefaultShareEfficiencyPct=0.3f/ShareAttackSpeed=0x01 + TowerSynergySystem.ResolveBuffShares 新方法：早退 fast-path（无 sharing 塔则 skip）+ Step 1 按 cache 恢复每塔 base attack speed 防 frame-over-frame 复合增长 + Step 2 O(N²) sharing tower ↔ nearby tower 扫描按 mask 位 multiplicative (1+eff) 应用 + cache 键为 towerId 而非 ActiveTowerIds 位置（**Claude bug scan 修复 #1**：位置键导致跨帧错位 ApplyStatsToAnotherTower） + 1 防御性 cache grow 路径（CACHE_SLOT_COUNT=16 起步 ×2 翻倍） + CombatGroup 在 TowerAttack.Update 之前调用（attack speed 必须在读取前更新）+ 17 测试覆盖  回归测试 + 鼓励塔群布局（4 塔包围阵）乘法叠加 ~2.86× 攻速）；bench2: 9798, bench4: 3874, bench5: 3510 ⚠️（恢复轮：bench2 < 10000 阈值 + bench4 < 4300 + bench5 < 3800，与 Round 102 baseline 9655/3923/3362 处于 ±3% 抖动范围，2 个新机制连续未引入性能回退）
| buff share cache invalidation（**Claude bug scan #2 修复**：Core/ComponentStore.cs + Core/ComponentStore_Tower.cs + Systems/TowerSynergySystem.cs + BattleSystemECS.Tests/BuffShareSystemTests.cs。Round 103 实施后未提交的代码：DestroyEntity 不会通知 TowerSynergySystem 的 base-speed 缓存，导致同一 entityId 被新塔复用时缓存 restore pass 会把旧塔的 base 速度写入新塔。新增 `ComponentStore.OnTowerEntityInvalidated` 静态事件 + `RaiseTowerEntityInvalidated(int)` 内部触发器 + `TowerSynergySystem` 构造时 `+= InvalidateBuffShareCache` 订阅 + 新公开方法 `InvalidateBuffShareCache(towerId)` 走 swap-pop O(N) 清理 + AddTower/RemoveTower/DestroyEntity 三处 `RaiseTowerEntityInvalidated(entityId)` 触发 + 2 回归测试 CacheInvalidatedOnIdReuse_NewTowerStartsAtBase（构造旧塔 → cache 命中 → 销毁 → 新塔占同 ID → ResolveBuffShares 必须用新塔 base 不是旧塔 base）+ InvalidateBuffShareCache_DropsEntryByTowerId（直接 API 验证） + 248/248 tests PASS + Claude bug scan #2 修复 PASS 0 bug，bench2: 9955, bench4: 3915, bench5: 3500（与未修前 9798/3874/3510 相比 bench2/4 +1.6%/+1.1% 改善、bench5 持平零回归）⚠️ 三项仍未达阈值但行为完全确定）
|- 技能时间回溯快照 Skill Time Rewind Snapshot（Round 109 恢复完成：Systems/TimeRewindSnapshot.cs + Systems/SkillSystem.cs + Core/GAS/GameplayAbility.cs + Core/PreGameGroup.cs + Core/SystemRegistry.cs + Core/ComponentStore_Player.cs + Data/Configs/skills.json + BattleSystemECS.Tests/TimeRewindTests.cs。1 文件新 TimeRewindSnapshot.cs 提供 Snapshot/Restore 框架（捕获所有 GAS 实例 CooldownRemaining/CooldownTotal/ActiveTimer/Active + SkillStack 状态到 Dictionary 池）+ SkillSystem CastSkill 入口序列化目标技能 (HasSnapshot/ConsumeSnapshot API) + 4 SOA 字段 TimeRewindCooldown/TimeRewindCharges/TimeRewindCooldownMax/TimeRewindLastCast (float, 0f=零开销) + GameplayAbility 新增内部 Snapshot 协程状态 + PreGameGroup/SystemRegistry 注册 TimeRewind 子系统 + skills.json 新增 skill_time_rewind 配置 + 18 测试覆盖 snapshot 边界/restore 一致性/cooldown 还原/active 状态/charges 恢复）；bench2: 8354, bench4: 3951, bench5: 3488 ⚠️
|- 目标标记叠加/衰减 Mark Subsystem（恢复完成，Round 107 方向6：Systems/MarkSystem.cs + BattleSystemECS.Tests/MarkSystemTests.cs + Data/Configs/marks.json + Core/ComponentStore.cs + Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Core/SkillBuffGroup.cs + Core/SystemRegistry.cs。1 新增 MarkSystem 类（AddMark/Update/OnMarkThreshold Action/OnEnemyDestroyed 4 API + MarkConfig 静态类 2 字段 DefaultDecayInterval=1.0f/DefaultMaxStackCap=100）+ 3 新增 SOA 字段 EnemyMarkStacks (int, 0=零开销 fast path) / EnemyMarkDecayTimer (float) / EnemyMarkMaxThreshold (int) + AddEnemy + DestroyEntity 两处 reset 防 ID 复用泄漏 + ComponentStore 数组初始化补全三 mark 字段 + MarkSubsystemConfig 静态类 5 常量（DefaultDecayInterval=1.0f/DefaultMaxStackCap=100/RecommendedFrost=5/RecommendedScorch=10/RecommendedVolt=3）+ GameConfigLoader.LoadMarkConfig JSON 解析 marks.json 缺省回退 + SkillBuffGroup 注入 Mark 在 HealingZone 之后 Skill cd 之前 + SystemRegistry 注册 MarkSystem + 注入到 SkillBuff + OnEnemyKilled 订阅 Mark.OnEnemyDestroyed + 27 测试覆盖 inert default/AddMark increment/decay timer reset/max threshold fire/OnMarkThreshold subscriber/cap overflow clamp/destroy reset/per-frame latch/parallel safety/zero-stack skip + opt-in 设计 EnemyMarkStacks==0 + EnemyMarkDecayTimer==0 → 整段跳过单 bool 比较 + 适用于易伤/处决/灼烧/寒冰/电能等 stack-based 复合效果）；bench2: 8898, bench4: 3920, bench5: 3491 ⚠️

- 资源再生节点 Resource Node Regen / Respawn（Round 108 方向4：Systems/ResourceNodeSystem.cs + Core/ComponentStore_World.cs + Core/ComponentStore.cs + Core/GameConfig.cs + BattleSystemECS.Tests/ResourceNodeRegenTests.cs。3 SOA 字段 ResourceNodeRegenTimer (float[MAX_RESOURCE_NODES], 0f=零开销) / ResourceNodeRegenDelay (float, 0f=legacy 零开销) / ResourceNodeDepleted (bool, false=零开销) + `ResourceNodeDef` 新增 `RegenDelay` 属性（默认 0 = legacy 不再生） + `ComponentStore` Clear 数组 null 化注册 3 字段 + `ResourceNodeSystem.InitializeFromLevel` 读 `RegenDelay` 进 SOA（RegenDelay=0 保留 legacy 行为零开销） + `ResourceNodeSystem.DamageNode` 在 HP<=0 且 RegenDelay>0 时设置 Depleted=true + 装填 RegenTimer（RegenDelay=0 走 legacy 路径不变） + `ResourceNodeSystem.Update` 每节点循环前先判 Depleted：消耗倒计时 → timer<=0 触发 `RespawnNode` 满血复活（Health=MaxHealth/Active=true/Depleted=false/RegenTimer=0f/CaptureProgress=-1f/Owner=0）→ `continue` 跳过生产+捕获 段 + `RespawnNode` 私有方法兜底 ID-reuse 防护（DestroyEntity 已重置所有字段）+ 13 测试覆盖 inert default/InitializeFromLevel 拷贝 RegenDelay/DamageNode regen-enabled 装弹/regen-disabled 不装/非致命不装/不可毁伤 MaxHealth=0/DamageNode 1s 后 respawns at full HP/regen-disabled 永不再生/partial tick 不再生/双节点独立 timer/depleted 不生产/RegenConfig 常量 + 0 个新分支 in legacy 路径（RegenDelay=0 节点走 1 bool 检查 + 0 alloc fast path）+ 337/337 tests PASS）；bench2: 9647, bench4: 3998, bench5: 3499 ⚠️
- 恢复完成：地雷/陷阱塔 Mine/Trap Tower（Round 106 方向2：Systems/MineSystem.cs + Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/TowerType.cs + Core/GameConfig.cs + Core/SpatialGroup.cs + Core/SystemRegistry.cs + Systems/TowerPlacementSystem.cs + Data/Configs/mine_towers.json + BattleSystemECS.Tests/MineSystemTests.cs。9 SOA 字段（`TowerIsMine`/`MineTriggerRadius`/`MineArmTime`/`MineArmProgress`/`MineDamage`/`MineExplosionRadius`/`MineMaxStacks`/`MineStacksRemaining`/`MineTriggeredThisFrame` 默认 false/0/0/0/0/0/1/0/false 零开销）+ `TowerType.Mine=10` 枚举值 + `MineConfig` 静态类 6 默认值（DefaultTriggerRadius=1.5/DefaultArmTime=0.5s/DefaultDamage=80/DefaultExplosionRadius=2.0/DefaultMaxStacks=1/DefaultCost=30）+ `MineSystem` SetTurn+Update 流程：arm timer→proximity trigger（O(active enemies) 扫描）→AoE explosion→stack 消耗→per-frame latch 防同帧多触→stack=0 调 DestroyEntity + 串行 ping-pong damage queue（fixed 64-cap List）零分配热路径 + `ComponentStore.DestroyEntity` 补全 mine fields reset 防 ID 复用泄漏（之前漏 `_ResetEntity` 不调 + DestroyEntity 单独清理分支也漏）+ `TowerPlacementSystem.PlaceTower` TowerType.Mine 分支从 MineConfig defaults 初始化所有字段 + `SpatialGroup` WavePhase RebuildSpatialGrid 之后注入 MineSystem.Update + `SystemRegistry` 注册 + 19 测试覆盖 inert default/PlaceTower init/DestroyEntity reset/arm time gate/proximity trigger/AoE damage/stack decrement/destroy on exhaustion/per-frame latch/invulnerable skip/dead skip/non-mine skip/load config + **Claude bug scan 修复 4 项** 1）缺 using BattleSystemECS.Components 编译失败 2）`MineSystem_MultiStackMine_FiresMultipleTimes` 期望 latch 自动在帧内清但测试未调 SetTurn → Update 开头加 latch 自重置使 Update 自包含可测 3）`MineSystem_SkipsDeadEnemies` 注释与断言矛盾（注释说"stack should NOT decrement"但 assert==0）→ 修正注释+断言为 1 4）`DestroyEntity_ResetsAllMineFields` 失败（actual=true）→ 补全 DestroyEntity tower 分支 mine fields reset 9 行 + 297/297 tests PASS）；bench2: 9571, bench4: 3944, bench5: 3397 ⚠️

### 2026-06-03
- 变形术 Polymorph（ComponentStore_Enemy.cs + ComponentStore.cs + EnemyActionType.cs + GameplayAbility.cs + GameConfig.cs + EnemyAISystem.cs + SkillSystem.cs，3 SOA 字段 EnemyIsPolymorphed/PolymorphDurationLeft/PolymorphDamageTakenMultiplier + ApplyPolymorph 访问器 + DestroyEntity 重置 + Polymorphed=26 动作枚举 + GameplayAbilityDef.PolymorphDuration/DamageTakenMultiplier 字段 + SkillConfig 桥接 + EnemyAISystem BT 评估短路 + SkillSystem CastPolymorphArea circle AoE：duration 计时到期自动清除 + 1.5x 受伤放大）；bench2: 10457, bench4: 4659, bench5: 3932 ⚠️
- 穿透抗性 JSON 路径 Pierce Resistance Loader（GameConfigLoader.cs + game_config.json + Data/Monsters/monster_plated_warrior.json，monster JSON 解析 PierceResist/PierceImmune/DamageImmunities 字段 + null guard 保留默认 + Plated Warrior 怪物原型：HP350/护甲12/PierceResist 0.5 + Plated Carapace 技能）；bench2: 9230, bench4: 4690, bench5: 3885 ⚠️
- 子弹时间 Bullet Time / Slow Motion（ComponentStore_Player.cs + FrameScheduler.cs，2 SOA 字段 PlayerBulletTimeTurnsLeft/PlayerBulletTimeScale + ActivateBulletTime(playerId, turns, scale) setter + AddPlayer 重置防 slot 复用泄漏 + SplitDeltaForBulletTime 拆 dt：前 7 phase 用 enemyDt = dt×scale，后 4 phase 用 combatDt = dt 原速 + 末尾 tick 1→0 时 read-scale-before-reset 避免 off-by-one + 默认 0f 永远不进 active 分支零开销）；bench2: 9589, bench4: 4454, bench5: 3724 ⚠️
- 塔受击反击 Retaliate（ComponentStore_Tower.cs + ReflectTowerSystem.cs + SuicideBombSystem.cs，2 SOA 字段 TowerRetaliateChance/RetaliateDamageMult + AddTower 初始化 0f/0f + ResetTower/RemoveTower 重置 + QueueRetaliate 复用 _reflectQueue 共用 apply 路径 + 共享 Random.Shared 线程安全掷骰 + 默认 0% 概率零开销热路径）；bench2: 10472, bench4: 4598, bench5: 3961 ⚠️
- 塔诱饵 / 路径偏向 Lure（Bait）（ComponentStore_Tower.cs + EnemyMovementSystem.cs，2 SOA 字段 TowerLureRadius/TowerLureStrength 默认 0f/0f 零开销热路径 + AddTower 初始化 + ResetTower/RemoveTower 防 slot 复用泄漏 + waypoint 段内 inline 扫描 ActiveTowerIds：每个 enemy 对每个 active 塔检测半径并应用线性比例 (1 - d/R) × strength 的朝塔速度偏移 + 0 半径早退 + 距离 0 容错）；bench2: 10045, bench4: 4407, bench5: 3858 ⚠️
- 击中/暴击触发器 On-Hit / On-Crit（Core/GameEvents.cs + Systems/PlayerTowerAttackSystem.cs + Systems/TowerAttackSystem.cs + Systems/HitTriggerSystem.cs，2 事件常量 EnemyHit/EnemyCrit + EnemyHitEvent DTO 含 EnemyId/AttackerId/AttackerKind(0=player,1=tower)/Damage/IsCrit + PlayerTowerAttack 3-tuple damage queue 携带 wasCrit + 串行 apply 阶段 finalDamage>0 时发布 EnemyHit + 暴击时同步发布 EnemyCrit + TowerAttack 并行阶段用 HashSet<long>(critKey=enemyId<<32|towerId) 标记 crit（damageLock 保护线程安全） + 串行阶段 Remove 实现 per-(enemy,tower) 一次性发布 + _critFiredThisFrame.Clear() 帧末清理 + HitTriggerSystem 订阅器 per-frame hit/crit counter + Dictionary<int,int> per-enemy 计数 + ResetCounters 帧初清理 + EventBus null-coalescing 构造回退）；bench2: 10123, bench4: 4385, bench5: 3918 ⚠️
- 掉落稀有度分级 Pickup Rarity Tiers（Core/ComponentStore_World.cs + Core/ComponentStore_Tower.cs + Core/GameConfig.cs + Systems/PickupSystem.cs，1+1 SOA 字段 PickupRarity (byte, 0-4) / TowerLuck (float, 0f=零开销) + PickupRarityConfig 配置类 (TierWeights/LuckShiftPerPoint/MaxLuckBonus) + PickupDef.Rarity 字段 + SpawnPickup 签名加 rarity 参数 + RollPickupRarity 权重抽签：Common(50%)/Uncommon(30%)/Rare(15%)/Epic(4%)/Legendary(1%) + TowerLuck 加成迁移 Common 权重到 Rare+ 池 + PickTypeByRarity 按 Rarity 字段映射 type + 默认 0 零开销热路径）；bench2: 10582, bench4: 4135, bench5: 3858 ⚠️
- 元素亲和 Elemental Affinity（ComponentStore_Tower.cs + GameConfig.cs + TowerAttackSystem.cs + TowerPlacementSystem.cs，2 SOA 字段 TowerElementalAffinity (int, -1) / TowerElementalAffinityBonus (float, 0f) + AddTower 初始化 -1/0f + RemoveTower 重置 + TowerConfig.ElementalAffinity/ElementalAffinityBonus + placement 桥接 + 串行 apply 阶段 O(1) 早退：towerAff>0 且 EnemyElementStatus & aff != 0 时 finalDmg *= (1 + bonus) + Flame Striker 原型）；bench2: 9908, bench4: 4385, bench5: 3858 ⚠️
- 路径吸附塔 Path-Hug Tower（ComponentStore_Tower.cs + ComponentStore.cs + TowerAttackSystem.cs，1 SOA 字段 TowerPathHugOnly (bool, false=无过滤零开销) + AddTower 初始化 false + RemoveTower 重置 + ComponentStore Clear null! 注册 + TowerAttackSystem SelectTarget EnemyActive 检查后 O(1) 早退：TowerPathHugOnly[towerId] && EnemyPathId[enemyId] < 0 → continue，让"沿路塔/Roadblock"风格只打路径上的敌人）；bench2: 10225, bench4: 4350, bench5: 3842
- 塔吸血 / On-Hit Lifesteal（ComponentStore_Tower.cs + GameConfig.cs + HitTriggerSystem.cs + TowerPlacementSystem.cs + Data/Towers/tower_vampire_lifesteal.json，2 SOA 字段 TowerLifestealFraction (float, 0f=零开销) / TowerLifestealMaxPerFrame (float, 0f=不限) + AddTower/RemoveTower 初始化 0f/0f + TowerConfig.LifestealFraction/LifestealMaxPerFrame + placement 桥接 + HitTriggerSystem.OnEnemyHit 内 AttackerKind==1 && LifestealFraction>0 路径：heal = damage × frac，cap 钳制 + PlayerMaxHealth 钳制 + Vampire Tower 原型 20% 吸血）；bench2: 10000, bench4: 4332, bench5: 3871
- 穿门相位塔 Phasing Tower（ComponentStore_Tower.cs + ComponentStore.cs + TowerAttackSystem.cs + Data/Towers/tower_phasing_ghost.json，1 SOA 字段 TowerIsPhasing (bool, false=零开销) + AddTower 初始化 false + RemoveTower 重置 + ComponentStore Clear null! 注册 + TowerAttackSystem LoS 过滤 O(1) 早退：TowerRequiresLOS && !TowerIsPhasing 才检查 HasLineOfSight，让 ghost/相位塔弹道无视任何 TowerBlocksLOS 障碍穿透 + Ghost Tower 原型）；bench2: 10309, bench4: 4329, bench5: 3850
- 嘲讽塔 Taunt Tower（ComponentStore_Enemy.cs + ComponentStore_Tower.cs + GameConfig.cs + ComponentStore.cs + TowerPlacementSystem.cs + CombatGroup.cs + CombatSetupGroup.cs + SystemRegistry.cs + Systems/TauntSystem.cs，1+2 SOA 字段 EnemyTauntedByTowerId (int, -1=无嘲讽零开销) / TowerIsTaunt (bool, false) / TowerTauntRadius (float, 0f) + EnemyTauntedByTowerId 数组初始化 -1 + AddEnemy/ResetEnemy/ResetTower/RemoveTower 三处重置 + GameConfig.TowerConfig.IsTauntTower/TauntRadius 默认 false/0f + placement 桥接 + SystemRegistry 注册 + TauntSystem SetTurn 收集 active taunt tower 列表 + ResolveTauntAssignments 串行 O(n_towers × n_enemies) 最近塔获胜规则 + CombatSetupGroup phase-0 早退 0 taunt tower 只做 O(n_enemies) reset + CombatGroup phase-末 写入 EnemyTauntedByTowerId）；bench2: 10262, bench4: 4355, bench5: 3886
- 跳斩/冲锋敌人 Leap / Jump Attack（Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/EnemyActionType.cs + Systems/WaveSpawningSystem.cs + Systems/EnemyMovementSystem.cs + Data/Monsters/leaper_spider.json + Data/Monsters/troll_mountain.json，12 SOA 字段 EnemyLeaperArchetype (int, 0=零开销) / EnemyLeapDistance / EnemyLeapCooldown / EnemyLeapCooldownRef / EnemyLeapDuration / EnemyLeapStartX/Y / EnemyLeapTargetX/Y / EnemyLeapElapsed / EnemyLeapDamage / EnemyLeapRadius / EnemyLeapStunDuration + AddEnemy/DestroyEntity/Clear 三处重置 + Leaping=27 动作枚举 + WaveSpawningSystem 按 monsterConfig.Type 分支 Leaper(短跳 4 格 + 6 帧) / Troll(长跳 8 格 + 15 帧 + 3 帧晕) 写入 SOA + EnemyMovementSystem 开关分支 Leaping 跳过普通寻路 + 抛物线插值 (t/duration) + 触发条件 cooldown=0 且玩家在 [leapDist/2, leapDist] 区间 + ResolveLeapLanding 串行 pass: 玩家在 EnemyLeapRadius 内 DecreasePlayerHealth + 范围内敌人 EnemyStunFlag/DurationLeft + reset cooldown=ref + action 回到 MoveToTarget）；bench2: 9907, bench4: 4192, bench5: 3762 ⚠️
- 锁定目标塔 Target Lock-On Tower（Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Systems/TowerAttackSystem.cs，2 SOA 字段 TowerIsLockOn (bool, false=零开销) / TowerLockedTargetId (int, -1) + AddTower 初始化 false/-1 + RemoveTower 重置 + ComponentStore Clear null! 注册 + TowerAttackSystem.SelectTarget 早退：lock-on 模式且 lockedValid(未死/未 burrow/未出射程) → O(1) 复用缓存 bestTarget，否则跑正常选目标循环并把胜出者缓存为下次锁定目标）；bench2: 9795, bench4: 4127, bench5: 3771
- 跳关奖励 Wave Skip Reward（Core/ComponentStore_Player.cs + Core/GameConfig.cs + Systems/WaveSkipSystem.cs + Data/Configs/wave_skip.json，2 SOA 字段 PlayerWaveSkipsUsed (int, 0=零开销) / PlayerSkipBonusDamagePct (float, 0f=零开销) + AddPlayer 重置 0/0f + GetPlayerAttackDamage 内联乘以 (1 + bonus) 默认 0 早退 + 4 访问器 Get/Set PlayerWaveSkipsUsed/SkipBonusDamagePct + WaveSkipConfig 4 字段 Enabled/SkipCost=50/SkipDamageBonusPct=0.10/MaxSkipsPerLevel=3 + WaveSkipSystem.TryPurchaseSkip 原子三步扣金+计数+累加 bonus + OnEnterBuildPhase no-op 因为 level-scoped 而非 phase-scoped + 3 read-only accessor GetRemainingSkips/GetCumulativeDamageBonusPct/GetSkipCost + Cap/缺金/Disabled 三态早退 + 日志：每次购买 N/M + gold/dmg 流水）；bench2: 9787, bench4: 4053, bench5: 3681 ⚠️
- 连杀奖励 Combo Chain（Core/ComponentStore_Player.cs + Systems/ComboSystem.cs + Systems/TowerAttackSystem.cs，2 SOA 字段 PlayerChainKillCount (int, 0=零开销) / PlayerChainKillBuffTimer (float, 0f=零开销) + AddPlayer 重置 0/0f + ComboSystem.Update 每帧递减 buff timer，到 0 重置 count + HandleComboIncrement 内 ON-enemy-killed 自增 count，达阈值 3 时 buff timer=5s（不重置 count 让叠加续命） + GetChainKillDamageMultiplier O(1) 访问器 + ResetCombo/ResetAllCombos 双处补全 reset + TowerAttackSystem 串行 damage apply 阶段 O(1) 早退：PlayerChainKillBuffTimer>0 时 finalDmg *= 1.25f）；bench2: 10109, bench4: 4088, bench5: 3815
- 冰霜减速区 Frost Zone（Core/ComponentStore_Tower.cs + Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/SystemRegistry.cs + Core/CombatSetupGroup.cs + Systems/FrostZoneSystem.cs + Systems/EnemyMovementSystem.cs + BattleSystemECS.Tests/FrostZoneSystemTests.cs，3+1 SOA 字段 TowerFrostZoneRadius/SlowFactor/Duration (默认 0f/1f/0f 零开销) + EnemyFrostZoneSlowMultiplier (默认 1f) + AddTower/RemoveTower/AddEnemy/DestroyEntity 三处重置 + ComponentStore Clear null! 注册 + GameConfig/SystemRegistry 桥接 + CombatSetupGroup phase-HotZone 后 SetTurn+Update + FrostZoneSystem Update 三段：duration 串行 tick（>0 减 1，到 0 写 radius=0 关闭）→ baseline Parallel.For 重置 enemies 1f → 串行 O(towers×enemies) MIN-apply 取最严重 slow factor（slowFactor<1 早退）→ 移动 phase 乘进 moveSpeed）；bench2: 9553, bench4: 3625, bench5: 3670 ⚠️
- 元素暴露 Elemental Exposure（Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Systems/ElementalReactionSystem.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs，2 SOA 字段 EnemyExposureMask (ElementType, None=零开销) / EnemyExposureTimer (float, 0f=零开销) + DestroyEntity 重置 None/0f + ComponentStore Clear null! 注册 + ElementalReactionSystem Update 末尾串行 O(active_enemies) 维护：status 变化时刷新 exposure 窗口 3.0s / status=None 时 timer 衰减到 0 清空 mask + GetExposureDamageMultiplier O(1) 访问器（None/disjoint → 1.30x，overlap → 1.0x）+ TowerAttackSystem 串行 apply 阶段 O(1) 早退：towerAffinity>0 且 EnemyExposureTimer>0 且 towerAff & exposureMask == 0 → finalDmg *= 1.30f + PlayerTowerAttackSystem 玩家攻击走 Exposure 路径：无 element tag，任意 active exposure 都触发 1.30x 易伤）；bench2: 9853, bench4: 4152, bench5: 3675 ⚠️
- 自由游荡敌人 Free-Roam Enemies（Core/ComponentStore_Enemy.cs + Core/ComponentStore.cs + Core/CombatSetupGroup.cs + Core/EnemyActionType.cs + Core/SystemRegistry.cs + Systems/EnemyAISystem.cs + Systems/EnemyMovementSystem.cs + Systems/WanderRoamSystem.cs + Systems/WaveSpawningSystem.cs + Data/Monsters/stalker_assassin.json，4 SOA 字段 EnemyIsFreeRoam (bool, false=零开销) / EnemyWanderTargetX/Y / EnemyWanderRerollTimer + AddEnemy/ResetEnemy/DestroyEntity/Clear 四处重置 + Wandering=28 动作枚举 + SystemRegistry/CombatSetupGroup phase-FrostZone 后桥接 + EnemyAISystem BT 评估短路 FreeRoam 直接置 Wandering + EnemyMovementSystem Wandering 开关分支：忽略路径系统只追 EnemyWanderTargetX/Y + moveSpeed 缩放 + 距离 < moveSpeed 时 snap 防止抖动 + 边界 clamp 防御 + WanderRoamSystem SetTurn snapshot 塔位置 + Update 三层决策：5 格 AGGRO 范围内最近塔 / 否则向玩家(0,0) / 6-10 帧 xorshift hash 抖动随机 cell + Stalker Assassin 怪物原型：Type=FreeRoam + 15HP/6 dmg/12 gold）；bench2: 9954, bench4: 4134, bench5: 3702
- 暴击抗性 Crit Resistance（Core/ComponentStore_Enemy.cs + Core/GameConfig.cs + Core/GameConfigLoader.cs + Systems/WaveSpawningSystem.cs + Systems/TowerAttackSystem.cs + Systems/PlayerTowerAttackSystem.cs + Data/Monsters/monster_doom_mech.json + Data/Monsters/monster_enforcer.json，1 SOA 字段 EnemyCritResistance (float, 0f=零开销) + AddEnemy/ResetEnemy 默认 0f + SetCritResistance enemyId 访问器 System.Math.Clamp [0,1] + MonsterConfig.CritResist 字段 + GameConfigLoader ParseMonsterConfig 解析 CritResist 字段 + WaveSpawningSystem 3 处 spawn 路径全数调用 SetCritResistance + TowerAttackSystem crit roll 前 effective = (TowerCritChance + _critRateBonus) × (1 - EnemyCritResistance) + PlayerTowerAttackSystem crit roll effective = _critRateThreshold × (1 - EnemyCritResistance) + Doom Mech / Enforcer Boss 原型 0.5 暴击减半）；bench2: 9467, bench4: 4197, bench5: 3719
- 塔回收残值 Salvage Refund（Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Systems/TowerUpgradeSystem.cs + Systems/TowerPlacementSystem.cs + Data/Configs/tower_placement.json，1 SOA 字段 TowerTotalUpgradeSpent (float, 0f=零开销) + AddTower 初始化 0f + ClearTowerSlot/RemoveTower 重置 + ComponentStore Clear null! 注册 + TowerUpgradeSystem.UpgradeTower 扣金后累计 += upgradeCost + SwitchUpgradePath 扣金后累计 += switchCost + TowerPlacementSystem 新常量 salvageUpgradeRate=0.3f + LoadSellConfig 读 tower_placement.json.salvageUpgradeRate + SellTower 在 base sell ratio 之上额外返还 TowerTotalUpgradeSpent × 0.3 残值 + log 输出基础/残值拆分明细 + spentBeforeDestroy 局部变量防止 DestroyEntity 重置后 log 显示 0）；bench2: 9724, bench4: 3985, bench5: 3732 ⚠️
- 波次金币衰减 Decaying Wave Bounty（Core/ComponentStore_Player.cs + Core/ComponentStore.cs + Systems/GoldSystem.cs + Core/SystemRegistry.cs + Data/Configs/wave_gold_decay.json，1+2 SOA 字段 PlayerWaveKillCount (int[MAX_PLAYERS], 0=零开销) + _waveGoldDecayRate=0.02f/_waveGoldDecayFloor=0.3f + WaveGoldDecayRate/Floor 公开 setter + ComponentStore.ResolveEnemiesKilledThisFrame 内联衰减 finalGold *= max(DecayFloor, 1.0 - killsThisWave * DecayRate) 然后 PlayerWaveKillCount[pid]++ + GoldSystem.SubscribeToWaveStart 订阅 WaveSpawningSystem.OnWaveStart → HandleWaveStart 串行清零全部 player 的 wave kill count + SystemRegistry 桥接 + 1 个新增 JSON 配置文件 wave_gold_decay.json 默认 Enabled/DecayRate=0.02/DecayFloor=0.3 鼓励快速清场）；bench2: 9985, bench4: 4239, bench5: 3708
- 塔协同等级 Synergy Tiering（Core/ComponentStore_Tower.cs + Core/ComponentStore.cs + Core/GameConfig.cs + Systems/TowerSynergySystem.cs，1 SOA 字段 TowerSynergyTier (int, 0=零开销) + SynergyTierConfig static class 6 常量 SynergyTier1/2/3Count=3/5/8 + SynergyTier1/2/3Bonus=0.10/0.20/0.35 + ComponentStore Clear null! 注册 + TowerSynergySystem.SetTurn 已按 TowerType 分组 + Update 阶段 1 ResolveSynergyTiers 零开销早退：totalActive<3 → ResetAllTiers 直接 return + 否则按类型计数 (3→tier1, 5→tier2, 8→tier3) 写 TowerSynergyTier + 绝对写入 TowerSynergyMultiplier=1.0+tierBonus (每帧覆盖 避免累加 bug) + 阶段 3 既有 JSON 协同 Max() 在其上再叠加）；bench2: 9795, bench4: 4145, bench5: 3667

### 2026-06-02
- 失衡条/破防 Stagger（ComponentStore_Enemy.cs + EnemyMovementSystem + TowerAttackSystem + ComponentStore.cs + EnemyActionType.cs，Staggered=24 状态 + 5 字段 + AddStaggerDamage/TickStagger/ClearStagger + 重击累加 meter + 10s 免疫期）；bench2: 11224, bench4: 5072, bench5: 4346
- 喘息波节奏 Wave Rhythm（GameConfig.cs + WaveSpawningSystem.cs + WavePreviewSystem.cs，WaveRhythm 枚举 + Rhythm 字段 + Normal/Breather/Surge/Climax × 计数 + 难度 + Preview UI 同步）；bench2: 10567, bench4: 4940, bench5: 4085
- 塔词缀槽基础设施 Reforge Split A（ComponentStore_Tower.cs + GameConfig.cs + GameConfigLoader.cs + tower_affixes.json，3 槽位 SOA + 18 词缀池 + 懒初始化 jagged 数组 + ClearTowerAffixes）；bench2: 11161, bench4: 5015, bench5: 4082 ⚠️
- Boss 踩踏步伤 Trample（ComponentStore_Enemy.cs + EnemyMovementSystem.cs，3 字段 EnemyTrampleRadius/Damage/Knockback + SetTurn pre-scan O(1) early-out + ResolveTrampleAoe 串行 AOE：玩家扣血 + 小怪击退 0.5 单位 + Stagger 自动暂停）；bench2: 11087, bench4: 4906, bench5: 4018 ⚠️
- 词缀重铸 API Reforge Split B（ReforgeSystem + ComponentStore_Tower.cs + GameConfig.cs，2 字段 TowerAffixLockMask/TowerReforgeCount + 7 访问器 + ReforgeConfig 成本/锁槽/稀有度权重 + RerollAffix 塔级/锁槽感知 + SetSlotLocked + RerollAllUnlocked 跳过锁槽 + 池采样 rarity tier）；bench2: 11082, bench4: 4494, bench5: 4074 ⚠️
- 喘息波运行时收益 Breather Split B（WaveSpawningSystem.cs + GoldSystem.cs + ComponentStore_Player.cs + SystemRegistry.cs，OnBreatherWaveComplete 事件 + 3 SOA 字段 PlayerHealOnBreatherWave/PlayerCooldownReduceOnBreather/PlayerBreatherGoldBonus + GoldSystem.SubscribeToBreatherWave：Breather 波完成时回血 %/技能 CDR/金币 ×2）；bench2: 11314, bench4: 4896, bench5: 4075 ⚠️
- 磁吸立场 Magnetize（MagnetizeSystem.cs + ComponentStore_World.cs + AIGroup.cs + SystemRegistry.cs，3 类型 Pull/Repel/PullDeflect + 64 zone 池 + 线性 falloff 拉扯力 + AIGroup pre-step 早退 + ProjectileSystem IsInDeflectZone 查询接口 + POSITION_CLAMP 越界保护）；bench2: 10930, bench4: 4759, bench5: 4089 ⚠️
- 幽光宠物/被动光环 Wisp（WispSystem.cs + ComponentStore_Player.cs + SkillBuffGroup.cs + SystemRegistry.cs + summons.json，3 类型 Heal/Slow/Curse 互斥 + SOA PlayerWispType/DurationLeft/Cooldown + 6-tile 半径 + SkillBuffGroup phase-9 执行 + touched-set 状态清理防 stale 慢速）；bench2: 10637, bench4: 4797, bench5: 3380 ⚠️
- 霰弹塔/锥形多弹丸 Shotgun Tower（ComponentStore_Tower.cs + ComponentStore.cs + TowerAttackSystem.cs，2 字段 TowerPelletDamageMult/ConeRadius + 现有 TowerProjectileCount/ScatterAngle 升级 + scatter 段每颗 pellet 找独立最近目标（taken-scratch 标记去重）+ 16-pellet 上限 + projectileSystem 多 Projectile 同帧兼容）；bench2: 10588, bench4: 4712, bench5: 3976 ⚠️
- 站桩图腾 Totem（TotemSystem.cs + ComponentStore_Player.cs + totems.json，独立 4 类型 Healing/ManaSpring/Searing/Tremor + 32 池 + 7 SOA 字段 TotemActive/OwnerId/Type/PosX/PosY/DurationLeft/ChargesLeft/Cooldown + PlayerTotemCooldown 节流 + AddTotem/RemoveTotem 池分配 + 4 效果分支 Heal-self/Mana-self/Searing-AoE/Tremor-stun + JSON 自加载 + POSITION_CLAMP 越界保护 + 默认 cooldown 2s）；bench2: 10782, bench4: 4581, bench5: 4010
- 锁链/连接 Tether（ComponentStore_Enemy.cs + EnemyMovementSystem.cs + TowerAttackSystem.cs + EnemyActionType.cs，5 字段 EnemyTetherPartnerId/MaxLength/DamageSharePct/StunSharePct/SlowFactor + SlowFactor 1f 初始化防冻 + SetTurn pre-scan O(1) early-out + ResolveTetherEnforcement 串行 AOE：超距拉回 0.5 单位 + 50% 减速 + 双向 + Tethered=25 动作枚举 + ApplyTetherDamageShare 串行串 damage 比例传染 + partner 抵抗/无敌/死亡检查）；bench2: 9570, bench4: 4619, bench5: 3865 ⚠️
- 🔧 性能优化：O(N)→O(1) counter（ComponentStore.cs + EnemyMovementSystem.cs + TowerAttackSystem.cs + WispSystem.cs，ActiveTramplerCount/ActiveTetheredCount/ActiveWispCount 计数器 + SetTurn pre-scan 改为 O(1) 读 counter + ApplyTetherDamageShare guard + WispSystem.Update 早退）；bench2: 10341, bench4: 4418, bench5: 4080
- 一击必杀保护 One-shot Protection（ComponentStore_Player.cs + TechTreeSystem.cs + tech_tree.json，1 SOA 字段 PlayerMinHealthFloor + Get/SetPlayerMinHealthFloor 访问器 + DecreasePlayerHealth floor 钳制 + TechTreeSystem min_health_floor effect 累加 + last_stand 节点 cost=3 后置 iron_skin_2）；bench2: 11093, bench4: 4574, bench5: 3844 ⚠️
- 视线系统 Line of Sight（SpatialGrid.cs + ComponentStore_Tower.cs + TowerAttackSystem.cs，HasLineOfSight + IsCellOnRay 整数格点射线 + TowerRequiresLOS/TowerBlocksLOS 2 SOA 字段 + AddTower 初始化 false + TowerAttackSystem 候选过滤 opt-in + IsCellOnRay 同名变量去重）；bench2: 10791, bench4: 4775, bench5: 3986 ⚠️
- 诱饵实体 Decoy（ComponentStore_Enemy.cs + ComponentStore.cs + EnemyAISystem.cs，3 SOA 字段 EnemyIsDecoy/DecoyLifetime/DecoyLifetimeLeft + AddEnemy 初始化 false/0f/0f + DestroyEntity 重置 + AISystem 倒计时自动 QueueEnemyDeath + 跳过 BT 评估置 EnemyActionType.None）；bench2: 10787, bench4: 4884, bench5: 4063
- 击杀回血/回蓝 HealOnKill（HealOnKillSystem.cs + ComponentStore_Tower.cs + GameConfig.cs + SystemRegistry.cs + TowerPlacementSystem.cs，2 SOA 字段 TowerHealOnKillAmount/ManaOnKillAmount + TowerConfig.HealOnKillAmount/ManaOnKillAmount + OnTowerKill 订阅 + SetPlayerCurrentHealth maxHP 钳制 + AddPlayerMana maxMP 钳制 + JSON 桥接 placement）；bench2: 10635, bench4: 4703, bench5: 3996 ⚠️
- 纯伤害类型免疫 Magic/Physical Immune（ComponentStore_Enemy.cs + GameConfig.cs + WaveSpawningSystem.cs + game_config.json，SetDamageImmunityMask 访问器 + MonsterConfig.DamageImmunities[] 列表字段 + ComputeDamageImmunityMask 字符串→位掩码 switch + AddEnemy 路径三处统一初始化 + Magic Immune Specter/Black Dragon 两个 Boss 原型）；bench2: 10741, bench4: 4776, bench5: 4015
- 末段狂暴 LastStand / DeathRattle（ComponentStore_Enemy.cs + ComponentStore.cs + GameConfig.cs + EnemyAISystem.cs + WaveSpawningSystem.cs + game_config.json，4 SOA 字段 EnemyLastStandHpFraction/LastStandActive/LastStandSpeedMult/LastStandDamageMult + BossLastStandConfig 配置类 + SetLastStandConfig 4 访问器 + DestroyEntity 重置 + EnemyAISystem 双路径 HP 阈值检测（parallel + serial 段）+ 一性次 Multiply EnemyMoveSpeed/EnemyDamage + AddEnemy 路径三处统一初始化 + Skeletal King Boss 原型）；bench2: 10258, bench4: 4584, bench5: 3997
- 穿透抗性 Pierce Resistance（ComponentStore_Enemy.cs + GameConfig.cs + ProjectileSystem.cs + WaveSpawningSystem.cs + game_config.json，2 SOA 字段 EnemyPierceResist/EnemyIsPierceImmune + MonsterConfig.PierceResist/PierceImmune + SetPierceResist 2 访问器 + AddEnemy 路径三处统一初始化 + _projIsPiercing 弹道标志 + ResolveHit pierce-抗性+免疫应用 + FireAtPoint fragment 不继承 pierce + Armored Core/Plated Behemoth 两个 Boss 原型）；bench2: 10623, bench4: 4641, bench5: 3987
- 死后复活一次 Reincarnation（ComponentStore_Player.cs + GameConfig.cs + GameManager.cs + game_config.json，3 SOA 字段 PlayerReincarnationCharges/ReincarnationHealFraction/HasReincarnated + PlayerConfig.ReincarnationCharges/HealFraction + SetPlayerReincarnationConfig 访问器 + DecreasePlayerHealth HP<=0 一次性复活：max(1, MaxHP×HealFraction) + Charges-- + HasReincarnated 锁定 + SetPlayerReincarnationConfig 重置 flag 避免持久化漏到下局）；bench2: 10541, bench4: 4555, bench5: 3962

### 2026-06-01
- 射程伤害衰减（TowerAttackSystem + ComponentStore_Tower.cs + GameConfig.cs）；bench2: 12026, bench4: 5531, bench5: 4696
- 爆发射击/齐射模式（TowerAttackSystem + ComponentStore_Tower.cs + GameConfig.cs + TowerPlacementSystem.cs）；bench2: 11928, bench4: 5364, bench5: 4618
- 持续升温伤害（TowerAttackSystem + ComponentStore_Tower.cs + GameConfig.cs + TowerPlacementSystem.cs）；bench2: 11733, bench4: 5437, bench5: 4635
- 绝境反击/背水一战（DesperationSystem + TowerAttackSystem + BuildGroup + PreGameGroup + SystemRegistry）；bench2: 12236, bench4: 5396, bench5: 4713
- 随机伤害范围（TowerAttackSystem + ComponentStore_Tower.cs）；bench2: 12263, bench4: 5385, bench5: 4593
- 敌人 CC 免疫/不可阻挡（ComponentStore_Enemy.cs + FearSystem.cs）；bench2: 11763, bench4: 5324, bench5: 4589
- 治疗抑制/重伤减免（TowerAttackSystem + EnemyHealerSystem + EnemyLifestealSystem + ComponentStore_Enemy.cs）；bench2: 11919, bench4: 5248, bench5: 4608
- 伤害类型转换（TowerAttackSystem + ComponentStore_Tower.cs + GameConfig.cs + GameConfigLoader.cs + TowerPlacementSystem.cs）；bench2: 11869, bench4: 5326, bench5: 4647
- 塔出售价值衰减（sellDecayPerSecond + TowerPlaceTime + TowerPlacementSystem + tower_placement.json）；bench2: 11936, bench4: 5448, bench5: 4636
- 塔位幽灵预览/放置确认（PreviewPlacement/ConfirmPlacement/CancelPreview + IRenderer.RenderGhostTower + ConsoleLogger）；bench2: 11630, bench4: 5416, bench5: 4463
- 元素护盾类型（ApplyEnemyDamage + ComponentStore_Enemy.cs + ElementalReactionSystem + monster_shield.json/monster_enforcer.json + WaveSpawningSystem + GameConfig.cs）；bench2: 11423, bench4: 5151, bench5: 4573
- 过量伤害/溢出机制（OverkillType=1: Splash + TowerAttackSystem.ResolveSplashDamage + ComponentStore_Tower.cs + GameConfig.cs + GameConfigLoader.cs + TowerPlacementSystem.cs + tower_sniper.json）；bench2: 11703, bench4: 5171, bench5: 4352
- 敌人随机路径偏移（EnemyMovementSystem + ComponentStore_Enemy.cs + WaveSpawningSystem + GameConfig.cs）；bench2: 11364, bench4: 5339, bench5: 4521
- 敌人同格堆叠惩罚（StackingConfig + EnemyMovementSystem.UpdateStackingPenalty + ComponentStore_Enemy.cs + SystemRegistry）；bench2: 10233, bench4: 5191, bench5: 4387
- 击杀冷却重置（KillCooldownResetSystem + ComponentStore_Tower.cs + ComponentStore_Player.cs + SystemRegistry）；bench2: 11090, bench4: 5231, bench5: 4512
- 敌人施法可打断（Interruptible Channeling Spells, EnemyAbilitySystem + EnemyMovementSystem + ComponentStore_Enemy.cs + AIGroup.cs + ComponentStore.cs + GameConfig.cs + enemy_abilities.json）；bench2: 11578, bench4: 5104, bench5: 4203 ⚠️
- 敌人波次预览/侦查（WavePreviewSystem + ComponentStore_Player.cs + SystemRegistry）；bench2: 11533, bench4: 5256, bench5: 4533
- 敌人属性吸取/Stat Drain（TowerSabotageSystem + ComponentStore_Enemy.cs + ComponentStore_Tower.cs + GameConfig.cs + WaveSpawningSystem.cs）；bench2: 11608, bench4: 5096, bench5: 4301
- 回放/录像系统（ReplaySystem + GameConfig.ReplayConfig + SystemRegistry，opt-in JSONL per-frame telemetry，零热路径开销）；bench2: 11079, bench4: 5131, bench5: 4495
- 可部署陷阱塔（DeployableTrapSystem + MovementGroup + ComponentStore_Tower.cs + ComponentStore_Enemy.cs + GameConfig.cs + TowerPlacementSystem.cs + ComponentStore.cs）；bench2: 11323, bench4: 5087, bench5: 4283
- 死亡标记/处决阈值（PlayerTowerAttackSystem + ComponentStore_Enemy.cs + ComponentStore.cs，HP<15% 自动标记 +50% 伤害+处决奖励金币）；bench2: 11382, bench4: 4995, bench5: 4342
- 仇恨脱战范围/Aggro Leash（EnemyMovementSystem + ComponentStore_Enemy.cs，可选范围配置，近基地暂停推进/远离后回归）；bench2: 11519, bench4: 5137, bench5: 4186
- 商店洗牌/Shop Reroll（ShopRerollSystem + ComponentStore_Player.cs + GameConfig + BuildGroup + SystemRegistry，BuildPhase 洗牌 offer 池 + 成本阶梯 + 保底稀有度）；bench2: 10225, bench4: 4839, bench5: 4147
- 放逐机制/Banish（EnemyMovementSystem + ComponentStore_Enemy.cs + ComponentStore.cs + EnemyActionType.cs，Banished 状态冻结敌人 N 帧，可被塔/技能触发）；bench2: 11848, bench4: 5212, bench5: 4315
- 玩家元进度/跨局声望 Prestige（PrestigeSystem + GameConfig + GameManager + SaveSystem + PlayerTowerAttackSystem，Stardust 货币解锁跨局永久节点，GameConfig.MetaDamageMult 注入到玩家伤害计算）；bench2: 11125, bench4: 5141, bench5: 4303

### 2026-05-31
- 光束/激光连续塔基础设施（BeamTowerSystem + ComponentStore_Tower.cs）；bench2: 11369, bench4: 5587, bench5: 5094
- N 击护盾系统（HitShieldSystem + ComponentStore_Enemy.cs + TowerAttackSystem/PlayerTowerAttackSystem）；bench2: 11546, bench4: 5590, bench5: 4824
- 法力燃烧/资源剥夺系统（ManaBurnSystem + ComponentStore_Enemy.cs + ComponentStore_Player.cs）；bench2: 11802, bench4: 5663, bench5: 4889
- 幽灵/相位敌人系统（PhaseSystem + ComponentStore_Enemy.cs + TowerAttackSystem）；bench2: 11643, bench4: 5501, bench5: 5017
- 敌人塔破坏/EMP 瘫痪系统（TowerSabotageSystem + ComponentStore_Tower.cs + ComponentStore_Enemy.cs）；bench2: 12187, bench4: 5471, bench5: 4847
- 自爆/殉爆敌人系统（SuicideBombSystem + ComponentStore_Enemy.cs + CombatGroup）；bench2: 11529, bench4: 5199, bench5: 4567
- 塔造价递增/成本梯度（PlacementCountByType + TowerPlacementSystem + tower_placement.json）；bench2: 11970, bench4: 5389, bench5: 4855
- 伤害免疫/属性克制系统（DamageImmunityMask + DamageType扩展 + TowerAttackSystem/PlayerTowerAttackSystem）；bench2: 9588, bench4: 5712, bench5: 5026
- 敌人吸血系统（EnemyLifestealSystem + EnemyLifestealRatio/Cap/Active + EnemyAISystem）；bench2: 12070, bench4: 5685, bench5: 4951
- 拉扯/真空吸引系统（PullSystem + ComponentStore_World.cs + MovementGroup + SystemRegistry）；bench2: 11784, bench4: 5544, bench5: 4879
- 恐惧/混乱敌人系统（FearSystem + ComponentStore_Enemy.cs + ComponentStore_Tower.cs + AIGroup + SystemRegistry）；bench2: 11955, bench4: 5514, bench5: 4934
- 地图热区/地形加成系统（HotZoneSystem + ComponentStore_Tower.cs + GameConfig.cs + CombatSetupGroup + SystemRegistry）；bench2: 11895, bench4: 5703, bench5: 4592
- 敌人巢穴/生成建筑系统（NestSystem + SystemRegistry + SpawningGroup）；bench2: 11686, bench4: 5669, bench5: 4798
- 保护者/守卫敌人系统（ProtectorSystem + ComponentStore_Enemy.cs）；bench2: 12066, bench4: 5726, bench5: 4709
- 英雄/雇佣兵系统（HeroSystem + ComponentStore_Player.cs + CombatGroup + SystemRegistry）；bench2: 12319, bench4: 5648, bench5: 4733
- 范围控制区系统（ZoneControlSystem + ComponentStore_World.cs + AIGroup + SystemRegistry）；bench2: 11587, bench4: 5421, bench5: 4715
- 敌人偏移/闪避系统（EnemyStrafeSystem + ComponentStore_Enemy.cs + AIGroup + SystemRegistry + TowerAttackSystem）；bench2: 11733, bench4: 5680, bench5: 4628
- 通用冷却缩减系统（PlayerCooldownReduction + TowerCooldownReduction + SkillSystem/GlobalSkillSystem CDR + TechTreeSystem）；bench2: 11600, bench4: 5648, bench5: 4608
- 塔部署数量限制系统（PlayerMaxTowers + PlayerTowerCount + TowerPlacementSystem + TechTreeSystem）；bench2: 11571, bench4: 5501, bench5: 4779
- 塔变形/形态切换系统（TowerMorphSystem + TowerCurrentMorph/Count/Cooldown/Damage/Speed/Range + CombatGroup + SystemRegistry）；bench2: 10628, bench4: 5722, bench5: 4961
- 范围治疗区技能集成（AreaShapeType.HealingZone + SkillSystem.InjectHealingZoneSystem + SystemRegistry wiring）；bench2: 11576, bench4: 5630, bench5: 4964
- 塔隐形/伪装系统（StealthSystem + ComponentStore_Tower.cs + ComponentStore_Enemy.cs + EnemyAISystem）；bench2: 11525, bench4: 5694, bench5: 4988

### 2026-05-31（续）
- 塔反射/伤害反弹系统（ReflectTowerSystem + ComponentStore_Tower.cs + SuicideBombSystem）；bench2: 11787, bench4: 5606, bench5: 4755
- 复活/亡灵法师敌人（NecromancerSystem + CorpseQueue）；bench2: 9871, bench4: 5122, bench5: 4882
- 昼夜循环系统（DayNightSystem）；bench2: 10483, bench4: 5135, bench5: 5021
- 钻地/潜行敌人（EnemyBurrowSystem + Emerge AoE）；bench2: 10115, bench4: 5487, bench5: 4878
- 流血/撕裂 DoT（BleedSystem）；bench2: 10081, bench4: 5464, bench5: 5087
- 路径修改塔（PathModifierSystem）；bench2: 10112, bench4: 5545, bench5: 4826
- 牵引/磁力/漩涡塔（PullTowerSystem）；bench2: 10214, bench4: 5344, bench5: 4947
- 诅咒/削弱光环（CurseAuraSystem）；bench2: 10128, bench4: 5345, bench5: 5007
- 8 项业务系统扩展（法力消耗/金币窃取/敌方治疗/驱散/塔自毁/产卵/被动产金/召唤修复）+ 飞行/浮空敌人；bench2: 10375, bench4: 5216, bench5: 4979
- mode 5 完整一局压测上线（5关全通，400帧，6520 FPS）
- 肉盾/前锋敌人（Vanguard，伤害转移）；bench2: 9265, bench4: 5058, bench5: 4809
- 塔连锁攻击系统（TowerChainDmgRatio + TowerAttackSystem auto-link + chain partner damage）；bench2: 11179, bench4: 5399, bench5: 4838
- 动态路径封锁系统（PathBlockSystem + MovementGroup + SystemRegistry）；bench2: 10110, bench4: 5646, bench5: 4893

### 2026-05-30
- **工程改进**：ComponentStore 按领域拆分为 5 个 partial 文件（Enemy/Tower/Player/World + 核心生命周期）；伤害公式测试补齐至 120 项
- 迫击炮/弧线弹道系统（ProjectileSystem + ComponentStore_Tower.cs）；bench2: 12116, bench4: 5349, bench5: 5011
- 风力/气流推动系统（WindSystem + ComponentStore_World.cs）；bench2: 12001, bench4: 5727, bench5: 5018
- 敌人克隆/复制（EnemyCloneSystem）；bench2: 12155, bench4: 5701, bench5: 4714
- 移动/巡逻塔（PatrolTowerSystem）；bench2: 9192, bench4: 4538, bench5: 4227
- 战争迷雾/视野系统（FogOfWarSystem）；bench2: 9806, bench4: 5161, bench5: 4911
- 塔建造延迟（TowerConstructionSystem）；bench2: 9920, bench4: 5344, bench5: 4920
- 玩家全局技能/终极技能（GlobalSkillSystem）；bench2: 10049, bench4: 5100, bench5: 4702
- 塔重定位/重新部署（TowerRelocateSystem）；bench2: 9698, bench4: 5286, bench5: 4832
- 时间操纵塔/Chrono Tower（ChronoTowerSystem）；bench2: 10116, bench4: 5158, bench5: 4767
- 敌人受伤减速/瘸腿（EnemyWoundSystem）；bench2: 9800, bench4: 5116, bench5: 4594
- 随机事件 Bug 修复；bench2: 9947, bench4: 5251, bench5: 5050
- 塔过热/热量系统（HeatSystem + ComponentStore_Tower.cs）；bench2: 11643, bench4: 5655, bench5: 5049
- 塔能量/法力资源系统（TowerEnergySystem + ComponentStore_Tower.cs）；bench2: 10605, bench4: 5672, bench5: 4735
- 光束/激光连续塔（BeamTowerSystem + ComponentStore_Tower.cs）；bench2: 11369, bench4: 5587, bench5: 5094
- 塔能量/法力资源系统（TowerEnergySystem + ComponentStore_Tower.cs + TowerAttackSystem）；bench2: 10605, bench4: 5672, bench5: 4735

### 2026-05-12～13
- 科技树系统上线（3分支 × 5节点，研究点数每波产出）
- BT Cache fix + Merged pipeline（FPS 8334）；GAS 技能系统重构；TowerAttack 并行化（ActiveTowerIds）
