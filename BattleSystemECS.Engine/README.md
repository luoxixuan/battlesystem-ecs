# Engine Contracts

This assembly is a pure API boundary. It references no Core, Systems, GAS, or content implementation. Existing Core types remain compatibility facades until later migration slices.
